# 13 · UI 重置版 定稿（v8 系列收敛）— 2026-09-17

> 状态：**用户已确认"这次很好，即刻开始"** → 进入设备实施阶段。
> 原型（唯一权威稿）：`docs/design/phywear-v8-prototype.html`
> 静图：`docs/design/v8-01-home.png`（主页）· `v8-02-theme.png`（主题与布局）· `v8-03-read.png`（读数）
> `v8-04-flow.png`（卡片流）· `v8-05-settings.png`（设置）
> 回退锚点：tag **`pre-full-redesign-20260917`**（= UI v2）+ 整包 **`~/桌面/PhyWear-rollback-20260917-2140/`**

---

## 1. Design Read（taste-skill §0.B 要求的一句话）

> Reading this as: 手持科学仪器 UI（导航 + 实时读数 + 图表）for 实验/课堂用户，
> with a **Swiss-Modernist 技术**语言，leaning toward `Swiss Modernism 2.0` + `Data-Dense Dashboard` on a `Dark Mode (OLED)` 基底。
> 随后按用户反馈两轮修正：**形态适配手表**（圆润 + 大字号）→ **风格来源换成穿戴向实名风格**；
> **去 AI 味**（删紫色渐晕、单一强调色）→ **补灵动**（数据/手指驱动）→ **少圆角** → **背景放开**。

旋钮（final）：`VARIANCE 4 / MOTION 4 / DENSITY 6`。

## 2. 实名风格来源（可查 `third_party/ui-ux-pro-max/data/styles.csv`）

| 实名风格 | CSV 关键词 | 落点 |
|---|---|---|
| **Dark Mode (OLED)** | deep black, power efficient, vibrant accents | 纯黑底 + 强调色 |
| **Exaggerated Minimalism** | oversized typography, high contrast | 读数页 46px 数字 / 标题 20–28px |
| **Executive Dashboard** | large key metrics, minimal detail | 每屏一个焦点 |
| **Bento Box Grid** | modular cards, Apple-style | 主页方形应用格 |

taste-skill 硬规则落地：**单一强调色**（§4.2 Color Consistency Lock）· **无装饰渐变/阴影**（§0.D/§4.4）·
**只动 transform/opacity**（§6.A）· **无滚动监听/常驻 rAF**（§5.D）· **prefers-reduced-motion 退化**（§6.B）·
**容器只在表达层级时用**（§4.4，"少圆角矩形"的依据）。

## 3. 设计令牌（设备侧以此为准）

| 令牌 | 值 | 说明 |
|---|---|---|
| 背景 | `#000000` | OLED 纯黑 |
| 表面 | `#141414` / `#1E1E1E` | 只用于"格子/行/浮层" |
| 细线 | `#2C2C2C` / `#3A3A3A` | 内容分组（替代卡片） |
| 主文字 | `#FFFFFF` | 对比 21:1 |
| 次文字 | `#C2C2C2` | |
| 三级文字 | `#9C9C9C` | **（v2 的 `PW_COL_FAINT 0x5b6875` 要提亮到这个量级）** |
| **强调色（4 主题）** | 玫瑰 `#D96A78` / 青 `#5AA9C9` / 琥珀 `#E0A458` / 单色 `#E8E8E8` | 全局**只此一色** |
| 圆角 | 内容容器 **0** / 格子 **10px** / 交互 **999** | 三级，已文档化 |
| 字号 | 12.5 / 13 / 15 / 17 / 20 / 22 / 28 / 46px | 最小 12.5（不用 11px 微字） |
| 间距 | 14px 页边距 · 10px 格间距 · 12px 块内边距 | |
| 底晕 | 顶部 `--wash-top .16` / 底部 `--wash-bottom .10` | **2 段竖直线性渐变（EPIC 唯一支持形态）** |
| 光流体 | 两团 430×360，透明度 .34 / .20 | 跟随模块位置漂移 |

## 4. 四种页型（30+ 子页按此套用）

| 页型 | 覆盖页面 | 版式 |
|---|---|---|
| **① 实验读数页** | 单摆/弹簧/向心力/倾角/频谱/磁场频谱/轨迹/IMU | 页头 → **大数字 46px + 单位列** → 次级结果 → 曲线 → 参数步进（图标块 + −/＋）→ 取值芯片 → 主/次按钮 |
| **② 参数操作页** | 磁性标尺/秒表/光门/声门/音调 | 大计数居中 → 阈值步进 → 曲线 → 主/次按钮 |
| **③ 向导标定页** | 六面法/磁椭球/陀螺零偏/构建器 | 步骤圆点 → 大字指令 → 进度条 → 双按钮 |
| **④ 设置列表页** | 设置/关于/AI 教练 | 图标块 + 名称/说明 + 右侧当前值（细线分组） |

页面树：主页（宫格 / 卡片流 双布局）→ 板块列表 → 四类子页；**所有页共享同一顶栏规则**：
右侧固定槽位 = 麦克风（`-20`），时间/版本/⋯ 让到 `-66`。

## 5. 交互与动效（全部非循环、由手指/数据驱动）

| 项 | 规则 |
|---|---|
| 按压 | scale .965 → 1.035 → 1（C3 过冲）+ 一次性强调色涟漪 |
| 入场 | 错峰 32–40ms，带轻微过冲 |
| 数值 | D1 脉冲（变化时闪一次，180ms）——**就是板上已实现的 `pw_motion_pulse_opa`** |
| 转场 | 共享元素展开（源矩形→目标矩形）/ 右拖返回（距离+速度判定 + 磁吸）/ 分页 = 圆点 + 液态拉伸 |
| 主题切换 | 全局 CSS 变量（板侧=同时改一组样式）+ 600ms 渐变 + 洗色遮罩（只动 opacity） |

## 6. 全局语音（用户明确要求：不做"主题页里的一个按钮"）

| 入口 | 行为 |
|---|---|
| **每页顶栏 🎙** | 按一下即唤起（全局，任意页面） |
| **始终聆听（开关）** | 开启后免按键；只对含唤醒词「手表/PhyWear」或明确指令动词的句子动作；麦克风角标小圆点指示 |
| **指令** | 切主题（玫瑰/青/琥珀/单色）· 打开某实验 · 回主页 · 打开设置 |
| **离线兜底** | Agent 不可用 → 长按弹出 **3–5 个指令芯片**（保证功能不丢） |

**设备现实**：无网络、无端侧 ASR 模型 ⇒ 真识别需 **Agent/宿主链路**；链路不通时降级为指令芯片。
始终聆听会持续占麦并增加功耗 ⇒ **默认关**，上板后实测待机电流与帧率再定。

## 7. 上板实现要点（避免"网页好看上板做不到"）

| 网页做法 | 设备做法 | 代价 |
|---|---|---|
| 底晕（2 段竖直渐变） | **LVGL 竖直 2 段渐变填充**（radius 0） | EPIC 直通，≈0 |
| 光流体（radial-gradient） | **预渲染光斑位图** 96×96 RGB565（PSRAM）+ 缩放/位移/透明度动画 | 一次 blit，不增 SRAM |
| 圆角 10/999 | 圆角≠0 会回退 CPU 光栅；P0 实测 14→0 **零 fps 增益**，故保留观感 | 已知、可接受 |
| 缩放（聚焦格 1.05） | LVGL 子件不随父缩放 ⇒ 降级为**框宽高 + 邻项降透明 + 描边** | 用户已确认 |
| 速度形变 skew | 降级为**强调条伸缩 + 卡片宽度微变** | 用户已确认 |

## 8. 实施顺序（即刻开始）

| 步 | 内容 | 验收 |
|---|---|---|
| **S1** | 主题/令牌层：`pw_theme.{c,h}`（4 主题 + 强调色/ink 查询）+ 令牌宏进 `phywear_ui.h` | 编译通过、不影响现有页面 |
| S2 | 顶栏新规则（麦克风槽位 `-20`，时间 `-66`）+ 麦克风按钮（先接指令芯片） | 真机截图 |
| S3 | 主页（宫格版：4×2 方形格 + 聚焦卡 + 圆点分页 + 底晕/光斑） | 真机截图 + fps A/B |
| S4 | 板块列表页（图标块行） | 真机截图 |
| S5 | 四页型样板各一页（单摆 / 标尺 / 六面法标定 / 设置） | 逐页截图 |
| S6 | 其余子页按页型铺开（28 页） | 逐页截图 |
| S7 | 主题与布局页 + 全局语音（含始终聆听开关与指令芯片兜底） | 截图 + 功耗/帧率实测 |
| S8 | 卡片流布局（v3 交互）作为可选布局 | 截图 + A/B |

**每步**：本地提交（不推送）· 帧率同口径 A/B · SRAM 核对 · 失败即回退 tag。

## 8.1 已锁定的三项默认值（2026-09-17 用户拍板）

| 项 | 决定 | 落地位置 |
|---|---|---|
| 默认主题 | **玫瑰 #D96A78** | `pw_theme.c`：`g_theme = PW_THEME_ROSE`（原型 `data-theme="rose"`） |
| 默认主页布局 | **宫格** | 原型默认 `LAYOUT='grid'`；设备侧 S3 先做宫格、卡片流放 S8 |
| 底晕强度 | **就这个**：顶部 `.16` / 底部 `.10` | `pw_theme.h`：`PW_WASH_TOP 16` / `PW_WASH_BOTTOM 10` |

### 8.2 进度

| 步 | 状态 | 产物 |
|---|---|---|
| S1 主题/令牌层 | ✅ 完成 | `pw_theme.{c,h}`（4 主题 + 令牌宏），已加入 `CMakeLists.txt` SRCS，编译通过 |
| S2 顶栏新规则 + 指令面板 | ✅ 完成（未烧录） | `phywear_ui.c`：时间标签 `-18→-66`、顶栏新增麦克风按钮 `-20`、`pw_voice_cb` 指令面板（4 主题芯片 + 选中高亮，调 `pw_theme_set`）；flash 2,488,476 B（+1,024 B） |
| S3 主页方形格 + 底晕/光斑 | ⏳ 下一步 | —— |

## 9. 回退与开关

- tag `pre-full-redesign-20260917`（UI v2 全量）+ 整包回退点 `~/桌面/PhyWear-rollback-20260917-2140/`
- 计划新增开关：`PW_UI_THEME`（0=旧色板/1=新主题）· `PW_UI_HOME`（0=旧宫格/1=新方形格）· `PW_UI_WASH`（底晕）· `PW_UI_GLOW`（光斑）· `PW_VOICE_ALWAYS`（始终聆听，默认 0）

---

## 10. 任务规划与自审清单（自主执行用）

### 10.1 任务分解（每步独立可回退）

| # | 任务 | 关键实现 | 验收（缺一不可） |
|---|---|---|---|
| **S3b** | 主页方形格 | 4 列 × 2 行（86×92）· 字形在上/短标签在下 · 聚焦格=强调色描边+底色 · 聚焦卡（模块名/说明/最近值）· 圆点分页替代旧分页 | 编译 ✓ 截图与 HTML `v8-01-home.png` 同构 ✓ fps A/B ✓ |
| **S3c** | 光斑 | 预渲染 96×96 RGB565 径向光斑（PSRAM）→ `lv_image` 缩放 + `translate` + 透明度动画；跟随聚焦模块 | 截图可见光斑 ✓ fps A/B ✓ SRAM 不增 ✓ |
| **S4** | 板块页 | 图标块行（32px 图标 + 名称/说明 + 右侧状态词）+ 细线分组；`pw_theme_accent()` 统一 | 截图与 `v8-…板块` 对照 ✓ |
| **S5** | 四页型样板 | ①单摆（大数字+单位列+曲线+步进器+芯片+主次按钮）②标尺（大计数+阈值步进）③六面法标定（步骤圆点+大字指令+进度条）④设置（图标块行） | 四页逐页截图 + 与 `phywear-v71-subpages-spec.html` 对照 |
| **S6** | 其余 28 页 | 按四页型套用；每页只改版式不改逻辑 | 逐页截图（实时页允许 1 次重试） |
| **S7** | 主题与语音页 | 4 主题列表 + 主页布局切换 + 🎙 全局入口 + **始终聆听开关（默认关）** + 离线指令芯片 | 截图 + 切主题可见 ✓ + 功耗/帧率实测 |
| **S8** | 卡片流布局 | v3 交互：横向拖动 + 中心聚焦 + 磁吸 + 共享元素展开；与宫格共存可切 | 截图 + fps A/B |

### 10.2 自审清单（每步执行，结果写入证据目录）

1. **编译**：无 error / 无 undefined reference（注意：新 .c 必须加进 `apps/examples/phywear/CMakeLists.txt` 的 SRCS）
2. **真机截图**：`pwshot.py run "phywear lang zh cap <page> --shot"`（一个 boot 一个 GUI；失败可重试）
3. **帧率**：同口径 `cap raw` + 30s 心跳；**判据 fps 不变**；loops/s 掉幅 >5% 触发降级
4. **降级顺序**：`PW_WASH_BOTTOM=0` → 砍光斑 → 最后才动内容层
5. **SRAM**：必须不新增（当前 489,136 B）
6. **与 HTML 对照**：逐屏比对 `docs/design/phywear-v8-prototype.html`（形态/层级/强调色数量）
7. **提交**：本地提交（`submit_427.sh --execute`，不推送）；每步一条，出问题可单独回退
8. **回退**：tag `pre-full-redesign-20260917` + `~/桌面/PhyWear-rollback-20260917-2140/`

### 10.3 已知待办与风险

- 主页/子页的**事件与数据逻辑不动**，只改版式与着色 —— 降低回归风险
- 光斑需要**生成位图并转换**（RGB565）→ 走 `docs/07` 的图片流程；若不成功则先用"两段竖直渐变叠层"近似并如实标注
- 始终聆听会持续占麦 → **默认关**，实测功耗后再定
- 语音真识别依赖 Agent/宿主链路；离线时用指令芯片兜底（已上线）

---

## 11. 执行状态与遗留（截至 2026-09-17 目标第 12 轮）

### 11.1 已完成并真机验证

| 步 | 内容 | 证据 |
|---|---|---|
| S1 | 主题层 `pw_theme.{c,h}`：4 主题 + ink 对比色 + 三级圆角/底晕/光斑令牌（默认玫瑰） | 编译 + `[ui] theme -> …` 串口输出 |
| S2 | 顶栏新规则（时间 `-66`、麦克风 `-20`）+ 指令面板（4 主题芯片，离线兜底） | `01_home_wash_mic.png` |
| S3a | 底晕：`pw_scr_new()` 内两层 **2 段竖直渐变**（EPIC 原生形态），全 App 生效 | 同上 |
| S3b | 主页 **4×2 方形格** + **聚焦卡**（按住某格即更新，走 i18n 静态表避免栈指针） | `03_home_s3b_grid.png` |
| S3c | **光斑**：96×96 **A8 遮罩** + recolor（flash +9.8 KB / SRAM +8 B）+ **8 点分页** | `04_home_s3c_glow.png`、`05_home_s3c_dots.png` |
| S4 | 板块页 **细线分组 + 32px 序号图标块**（SRAM 0 增长） | `06_board_s4_rows.png` |
| S5-④ | 设置列表页（细线 + 图标块 + 全圆交互按钮） | `07_settings_s5.png` |
| — | **24 处页头强调条统一为主题色**（清除最后的板块彩虹色） | `08_incline_accent_unified.png` + **像素级确证**（实测 (222,105,123) vs 玫瑰 (217,106,120)，距离²=35） |
| S6(首项) | 关于页去容器（细线分组） | `09_about_flat.png`（容忍模式） |
| 工具 | `pwshot` 加 **`PWSHOT_TOLERANCE`** 容忍模式 → 重绘页可抓帧（此前单摆 5 次、关于 3 次 MISSING） | `10_pendulum_flat.png` |

### 11.2 实测数字（同口径 `phywear lang zh cap raw`，30 s 心跳）

| 阶段 | loops/s | fps |
|---|---|---|
| 底晕前（v2 基线） | 197 | 12 |
| S3 收口 | 192 | 12 |
| S4 板块页 | 194 | 12 |
| S5-④ 设置页 | 195 | 12 |
| 强调条统一后（受扰读数，判为离群） | 173 | 14 |
| **复测 ×2（第 8 轮）** | **193 / 193** | **12 / 12** |
| 关于页改造后（第 10 轮） | 193 | 12 |

**结论：fps 全程 12 不变；loops 197→193（−2%），未触发 5% 降级规则。**
**SRAM**：489,136 → **489,416 B（+280 B）**，逐项可归因（主题表 32 + 语音面板/名称表 20 + 聚焦卡句柄 8 + 字符串表 64 + 光斑句柄 8 + 分页句柄 32 + 对齐余量）；余量 34 KB。
**固件**：2,500,660 B（14.91%），已烧板。
**提交**：本地 98 个，**全部未推送**。

### 11.3 与 HTML 定稿的差异（如实）

1. **光斑只在主页**：HTML 里底晕/光斑是全站氛围，设备上目前只在主页创建（其余页仅底晕）。
2. **主题切换是"部分换肤"**：切换会改强调色相关元素（底晕/光斑/强调条/序号/图标块），但**历史遗留的板块色常量**（`PW_ACC_*` 用于图标字形与部分标题）仍在部分页面生效 —— 未做全量换肤。
3. **卡片流布局（S8）未做**：设备只有宫格布局。
4. **语音**：网页端是真识别（Web Speech API）；**设备端只有指令芯片兜底**（离线无 ASR，真识别需 Agent/宿主链路），**始终聆听开关未做**。
5. **圆点分页**只在主页；HTML 的分页语义更广（读数页多页屏）。

### 11.4 未做清单

- **S5-①②③**：单摆读数页（页型①）正式样板化、标尺参数页（页型②）、六面法标定向导页（页型③）
- **S6 其余 ~27 页**按四页型铺开
- **S7**：主题与布局页（设备端）+ 全局语音（始终聆听开关 + Agent 真识别 + 指令芯片整合）
- **S8**：卡片流布局（v3 交互）共存

### 11.5 已知限制

1. `PWSHOT_TOLERANCE` 容忍模式**填充行不是真实像素** ⇒ 只能用于版式评审，不能用于像素级验证（像素级仍走严格模式）。
2. 严格模式下**实时页仍无法抓帧**（行一致性判据的固有限制）。
3. 始终聆听会持续占麦，**功耗未实测**，故默认关。
4. 本目标内**未做**逐页与 HTML 的并排比对（只有已列证据页做过）。


---

## 12. 决定：回退到重置前（2026-09-18）

用户判定"重置版当前 BUG 多"，决定**回退到重置前设计（UI v2）**。执行方式：以 tag `pre-full-redesign-20260917` 恢复代码，
**新增一个正向提交**（不改写历史、不 rebase、仍只提交本地）；重置期新增文件移除。
校验：重建后 **flash 2,487,452 B，与重置前固件字节数一致**；真机截图见 `docs/evidence/rollback-20260918/`。
本文档与 `docs/design/*`、`docs/evidence/ui-v9-*` **保留为历史记录**（不影响固件）。
