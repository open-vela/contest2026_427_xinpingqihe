# PhyWear 腕上智慧物理工坊 · 技术报告

> 2026 首届 openvela AI 硬件开发者大赛 · 作品技术报告（v2，2026-09-13 整合）
> 队伍：contest2026_427_xinpingqihe（xinpingqihe） · 硬件：立创·黄山派（SF32LB52-MOD-1-N16R8）
> 专属仓：https://github.com/open-vela/contest2026_427_xinpingqihe
>
> **本报告如实撰写：有就是有，没有就是没有。** 官方提供的成果（EPIC 硬件加速）与本队原创成果明确区分；
> 未实现/已验证环境一律标注清楚，不把"模拟器实测"写成"真机实测"，不把官方成果写成本队自研。

---

## 1、概述与工作基础归属

| 项目 | 内容 |
|---|---|
| 作品名称 | openvela 腕上智慧物理工坊（PhyWear）—— 手表版 phyphox |
| 队伍 | xinpingqihe（编号 427）。**XPQHyue**：技术负责人（驱动 / 板级 bringup / LVGL 应用 / 算法 / i18n / 性能 / 真机验证 / 提交）；**程秋霞**：文档校对、演示视频、线上传播（B 站） |
| 选题 | **③ 新硬件平台适配**（主打），并集成官方 AI Agent 做语言交互 |
| 平台 | openvela（NuttX）· 立创黄山派 SF32LB52-MOD-1（AMOLED + 触控 + 6 轴 IMU + 地磁 + 环境光 + 麦克风 + 扬声器） |

**一句话**：把 phyphox 的手机端物理实验搬到腕上，用板载真实传感器实时采集、端侧算法分析、LVGL 全中文界面呈现，
并可用自然语言（AI Agent）驱动页面与读数。

**工作基础归属（重要，务必区分）**

| 模块 | 来源 | 说明 |
|---|---|---|
| `vendor_sifli` **PR #31** | **官方（非本队）** | NuttX LCD/framebuffer 驱动、双缓冲、EPIC 图形加速 HAL、DMA 拷贝 |
| `apps_graphics_lvgl` **PR #41** | **官方（非本队）** | LVGL 注册 SiFli EPIC draw unit（fill/border/image/label/layer 卸载到 EPIC） |
| `nuttx-apps` **PR #121** | **官方（非本队）** | 编译构建支持 |
| `packages/ai_agent`（openvelaClaw） | **官方（非本队）** | AI Agent 引擎、工具注册框架、消息总线、Skill 加载器 |
| 传感器驱动 / 板级 bringup / 触控修复 / LVGL 应用 / 算法与图表控件 / i18n 与 CJK 字体 / 性能优化 / AI 工具与 Skill | **本队原创** | 见第 4、5、6 节 |

> 本队**未自研** EPIC 硬件加速与 AI Agent 引擎；本队工作是在官方基础上做**集成、驱动、应用、算法、优化与验证**。
> 玩法与实验场景参考 **phyphox**（RWTH Aachen，GPL v3），本作以 **C + LVGL 独立重实现**，未复制其 Android/Kotlin 代码体，源码 Apache-2.0。

---

## 2、系统架构与数据流

**图 1：系统总体架构**（绿=本队原创，橙=官方提供）

![系统总体架构](figures/fig1_architecture.png)

```
应用层（本队）  phywear_ui / phywear_raw / phywear_pend|spring|centri|incline|ruler|spec|time|life|tone
                pw_graph + pw_scope（图表控件）/ pw_analysis（算法库）/ phywear_i18n + pw_font_*
AI 层（官方引擎 + 本队工具）  packages/ai_agent ⇄ pw_ai 邮箱桥 ⇄ GUI 线程；Skill → /data/agent/skills/
图形栈（官方 EPIC 后端 + 本队用法优化）  LVGL 9.1.0 → EPIC draw unit → 双缓冲
显示/板级（官方 PR + 本队修复）  LCD/FB + co5300 AMOLED + FT6146 触控（本队修 EPIC 栈上触控）
驱动层（本队）  mmc5603(/dev/mag0) · ltr303(/dev/light0) · sf32lb52_mic(/dev/mic0) · sf32lb52_spk(/dev/spk0)
系统  openvela(NuttX) · SiFli SF32LB52 · 黄山派硬件
```

**图 2：数据流与关键流程**

![数据流与关键流程](figures/fig2_dataflow.png)

传感器 → `/dev/*` 字符设备 → `phywear_sensors`（单位换算与线程安全访问）→ 页面缓冲 →
`pw_analysis`（FFT / 自相关 / 最小二乘）→ `pw_scope`/`pw_graph`（CPU 光栅进 RGB565 小缓冲）→
`lv_image` → EPIC **IMAGE** 硬件 blit → AMOLED；AI 侧：Agent 工具 → `pw_ai` 互斥邮箱 → GUI 线程执行切页/采样。

---

## 3、硬件平台

| 部件 | 规格 | 本队使用情况 |
|---|---|---|
| SoC / 存储 | SF32LB52-MOD-1-N16R8：16 MB NOR（XIP @0x12010000）+ 8 MB OPI-PSRAM | PSRAM 已纳入堆（`MM_REGIONS=2`），运行空闲堆约 7.7 MB |
| 显示 | 1.85" 390×450 AMOLED（CO5300，QSPI），双缓冲 | 16 主页面 + 声学页全中文渲染 |
| 触控 | FT6146 电容触控（I2C1），单点（`maxpoint 1`，不支持双指捏合） | 官方 EPIC 栈上曾失效，**本队修复**恢复 |
| IMU | LSM6DS3TR-C（官方驱动 `/dev/lsm6dsl0`，acc mg / gyro mdps） | 摆测 g、弹簧、向心、频谱、秒表、摆动检测 |
| 地磁 | MMC5603NJ（**本队驱动** `/dev/mag0`，20 bit ×0.0625 mG） | 磁性标尺、磁场频谱 |
| 环境光 | LTR-303ALS（**本队驱动** `/dev/light0`，CH0/CH1 → lux） | 环境光页、光学快门 |
| 麦克风 | 板载模拟 MEMS + 片内 AUDCODEC ADC（**本队驱动** `/dev/mic0`，16 kHz/16 bit） | 声音频谱、声学快门、掌声计、麦克风电平页 |
| 扬声器 | 片内 AUDCODEC DAC0 → 外部功放（PA42 = `AUDIO_PA_CTRL`）→ 喇叭（**本队驱动** `/dev/spk0`，2026-09-13 新增） | 音频发生器、扬声器页、声学标定 |
| 控制台 | UART1 + CH340N，1 Mbps 8N1；**RTS 直连 SoC 复位** | 烧录/自测自动化（脚本"脉冲复位→等提示符→发命令"） |

---

## 4、软件实现

### 4.1 LVGL UI 框架与页面（本队）

- **导航 3 层**：主菜单宫格 → 板块实验列表 → 实验页；`操作 ≤3 步`、深色底（#101418）、数值大字号（28/48px）。
- **页面规模**：**16 个主页面**（根菜单 / 原始传感器 / 12 个实验与工具页 / 设置 / 关于）
  + 本日新增声学**「音频发生器」**页、以及原始传感器板块的第 5/6 子页（**麦克风、扬声器**）。
- **统一实验页骨架**：顶栏 →（工具行）→ 多视图横滑区（值卡 / 图卡 / 参数卡 / 说明卡）→ 状态行；
  参数内嵌实验页，保证"≤3 步到结果"。
- **代码结构**：`phywear_ui.c`（屏幕栈管理/宫格/列表）、`phywear_raw.c`（原始传感器 6 页横滑）、
  `phywear_<experiment>.c`（各实验页）、`phywear_sensors.c`（设备访问与单位换算）。
  截至 2026-09-15：**50 个非字体源文件 / 19,144 行**，另有 5 档 CJK 字体文件（生成物）。

**UI 第二批 (a)(b)(c)（2026-09-15；同日按用户反馈做过一次修订）**

三个三轴页（加速度/陀螺/地磁）有**两种视图**，轻点页面任意处或右下角自绘图标按钮切换；
**默认是"数值视图"**（= 改动前的原版风格，与「环境光」页一致）：

| 视图 | 内容 |
|---|---|
| **数值（默认）** | 3 行「轴字母 X/Y/Z（28 px，暗色）+ 大数字（48 px，带单位，如 `+0.98 g`）」，右下角切换按钮 |
| **图线** | 页眉名+单位（单位紧跟名字）→ 一行三列数值（轴字母 20 px 系列色 + 数值 28 px）→ **358×186 三泳道曲线** → 左下**每轴满量程**（如 `+/-1 / 0.20 / 1 g`）+ 右下时间窗 `8 s` |

- **为什么加双视图**：用户反馈"曲线优化不行、变化不明显，体验不如原来的大数字风格"。
  根因是**三轴共用一个满量程**被最大的那根顶住（重力让 Z≈1 g → 量程 2 g），
  于是 0.12 g 的 Y 轴在 62 px 泳道里只剩 **~3 px** 摆动，看着就是直线。
- **修订一：每轴独立量程**（`pw_scope_set_lanes_i16()` 改为收 `inv[]` 数组）——
  同一条 Y 轴改后幅度到 **±16 px**，肉眼可见；量程下限同时压低
  （加速度 0.2..8 g、陀螺 5..2000 dps、地磁 20..30000 mG），否则下限会把小信号压回直线。
- **修订二：零新增控件**：两种视图**复用同一批标签对象**（只改字体/位置/宽度/颜色），
  不新增 label，避免吃 LVGL 堆。
- **修订三：切换按钮用自绘图标**（折线 = 点它看图线；三横条 = 点它看数值）——
  本队 CJK 字体子集里**没有「曲」字（U+66F2）**，写"曲线"会显示方框，而重生成字体子集有 ~0.5 天流程成本。
- **零新增 SRAM 缓冲**：曲线仍复用 `pw_scope`（CPU 光栅化 RGB565 → `lv_image` → EPIC 硬件 blit），
  3 条泳道共用 **1 块**缓冲、每页只占 **1 个 scope 槽位**。
- **历史样本**用 `int16_t g_raw_hist[3][80]`（80 × 100 ms = 8.0 s），**三轴页共用一份**；
  进页即清零。存储时 ×k（加速度 4000 / 陀螺 16 / 地磁 1）饱和到 int16，绘制时统一换算 ——
  这样自动量程变化不会污染历史数据的量纲。
- **自动量程**按 **整条窗口**的峰值取 1-2-5 步进档（涨快落慢：回落需低于半量程），
  且**每轴各算各的**；用瞬时值会因正弦过零点掉档（曾出现"数值写 ±50 dps、曲线按 20 画"）。
- **泳道颜色**固定 X=红 / Y=绿 / Z=蓝（跨三页一致，便于对照），色板取自本队自定义系列色
  （已整体平移避开 phyphox 色板，见 `NOTICE.md`）。
- **无头验证**：新增 `phywear tap <x> <y>`（模拟器专用，往 `/dev/utouch` 注入
  DOWN→150ms→UP 的真实点击）与 `phywear cap rawcurve`（直接开图线视图），
  因此"点击切换"是**注入事件验证过**的，不只是看两张静态图。
- 真机截图见 `docs/evidence/ui-batch2-20260915/`（`rev-*` 系列）；帧率/内存实测见该目录 `README.md §4`。

**(b) 倾角页：指针式角度盘**

- **13 根刻度短线**（`lv_line`，-90°..+90° 每 15°，每 30° 一根长粗刻度）+ **指针**（`lv_line`）
  + 盘心圆点；0° 在正上方、±90° 满偏、正值向右；刻度按当前角度**逐格点亮**。
- **不用 `lv_arc`**：实测软件圆弧绘制把本页真机帧率从 ~26 拉到 23；改刻度线后回到 25
  （落在改动前 25~27 区间内）。
- 两个关键优化：① `lv_line` 的**对象框收紧到线段自身包围盒**（`lv_line_set_points()` 失效的是
  整个对象框，保持 168×168 会让每次移针重画整盘）；② 指针 12.5 Hz 节流 + 位移 <1° 不重绘
  （静止的手表零绘制开销）。

**(c) 秒表页：环形计时进度**

- `lv_arc` 满环 + **环心实时秒数**：满环 = "上次间隔"（还没有就取 2.0 s），跑动中按
  `elapsed/ref` 填充；超过上次间隔 → 满环 + 换"偏慢"色（橙）；停下时满环但用暗色。
- **只在值与颜色真的变了才写对象**：`set_style_arc_color()` 每次调用都会让整个圆弧对象
  重绘（156×156），闲时每 100 ms 白刷一次很吃亏。
- 真机帧率 10.3 → 9（稳定少 1 帧）：环心读数落在圆弧对象框内，读数每 100 ms 变一次会连带
  重绘圆弧。**可选修法**（未做）：圆弧也改成分段刻度环。

**本批帧率与内存（真机实测，同页同 bench，30 s 窗口）**

| 页面 | fps 前 | fps 后 | loops/s 前→后 |
|---|---|---|---|
| 原始传感器（三轴曲线） | 12 | 12 / 12 | 63 → 59 |
| 倾角（指针盘） | 27 / 27 / 25 | 25 / 25 / 25 | 27 → 25 |
| 秒表（环形进度） | 11 / 10 / 10 | 9 / 9 / 9 | 61 → 59 |

SRAM 489,440 → **490,648 B（93.58%）**，**+1,208 B**；flash 2,062,680 B（12.29%）。

> 为拿到真机帧率，本批把原来"只在 `bench` 模式打印"的帧率探针接进 30 s 心跳，并修掉一个
> 真机缺陷：原 `pw_fps_init()` 只在 `/dev/fb0` 可 mmap 时装回调，而**真机没有 `/dev/fb0`**
> （只有 `/dev/lcd0`），真机 `fps` 恒为 0。现改为挂 `LV_EVENT_RENDER_READY`
> （`lv_refr.c` 只在本轮真的有区域重绘时才发），模拟器/真机都成立，且不再替换 flush 回调。

### 4.2 传感器与声学驱动（本队）

| 驱动 | 节点 | 实现要点 |
|---|---|---|
| `mmc5603.c`（550 行） | `/dev/mag0` | SET/RESET 消偏、`CTRL0.AUTO_SR` 偏置修复（对照 Zephyr 主线）、寄存器语义按数据手册校准 |
| `ltr303.c`（398 行） | `/dev/light0` | CH0=可见+红外、CH1=红外；`lux=(CH0−CH1)×0.6` |
| `sf32lb52_mic.c`（318 行） | `/dev/mic0` | AUDCODEC 单端 ADC + DMA1_Channel4 单次采集，16 kHz/16 bit/1024 样本；本日把增益 0 → **+30 dB**（见 6.5） |
| `sf32lb52_spk.c`（411 行，**本日新增**） | `/dev/spk0` | AUDCODEC DAC0 + DMA2_Channel1 **循环播放**，功放使能脚 PA42；ioctl `TONE/STOP/VOLUME/PA`；默认 +12 dB |

三个传感器驱动合计 1,266 行（含板级 I2C/AUDCODEC 注册与两套板级配置约 1,505 行，已提 **nuttx PR #378**）。

### 4.3 物理算法与图表控件（本队）

- `pw_analysis.c`（627 行）：**radix-2 迭代 FFT**（位反转+蝶形）、**自相关测周期**（首峰粗估 + 末谐波峰精化）、
  过原点**最小二乘**拟合、峰值**抛物线插值**、阈值状态机（滞回 + re-arm）。
- `pw_graph.c` + `pw_scope.c`（1,274 行）：实时滚动曲线与可缩放/平移折线-散点图（多序列、坐标轴、Fit/Auto）；
  2026-09-15 增加 `pw_scope_set_lanes_i16()`（多泳道 + int16 样本 + 环形/线性历史，仍只用 1 块缓冲）。
- 典型链路（摆测 g）：IMU 角速度 50 Hz × 500 点（≈10 s）→ 自相关求周期 T → `g = 4π²L/T²`；真机实测误差约 **1.8%**（L=0.5 m）。

### 4.4 国际化与 CJK 字体子集（本队）

- **i18n**：单份 ID 枚举 + EN/ZH 双表，运行时可切换（`PW_STR()`）；当前 **282 条 ID**（EN/ZH 等长）。
- **CJK 字体**：Droid 子集 + Montserrat 合并，5 档字号（14/16/20/24/28）。
  本日新增 `tools/phywear/gen_font_ranges.py`（从中文表与应用字面量收集码点）与 `gen_fonts.sh`
  （lv_font_conv 重生成），本次覆盖 **534 个中日韩码点**，解决新增声学文案的缺字方框（见 6.4）。

### 4.5 声学功能（本日完成，真机验证）

- **扬声器通路**：`/dev/spk0` + 命令行自检 `phywear tone <Hz> [ms] [dB]`；连续播放 440/660/880/1760/220 Hz
  **五段全部成功**（修复前只有第一段能响，见 6.5）。
- **音频发生器页**：频率 40 Hz–4 kHz（±20 Hz 步进 + 220/440/880/1760 预设）、音量 −36~+30 dB、播放/停止，
  离开页面自动停声；界面截图见 `evidence/acoustic-20260913/tone.png`。
- **麦克风**：原始传感器板块新增「麦克风」页（dBFS 峰值 + 32 段电平柱，后台线程读 `/dev/mic0`）；
  自检命令 `phywear micread [n]` 打印 peak/dBFS/rms。
- **声学通路实测**（真机）：静音环境噪声约 570 LSB；扬声器放 1 kHz 正弦时麦克风峰值最高 **12,000 LSB（−8.4 dBFS）**（贴近、最大音量）。**注意：同板喇叭→麦克风耦合弱，常规音量下与环境噪声同量级，低频段谱峰常落在二次谐波上——不能当"已知声源校准频谱"用**
  → 喇叭→空气→麦克风链路成立。

### 4.6 AI Agent 集成（官方引擎 + 本队工具/Skill/桥）

- **4 个 PhyWear 工具**（本队，`tool_phywear.c` 541 行，注册进 `tool_registry`）：
  `phywear_list_experiments` / `phywear_open_screen` / `phywear_read_sensor` / `phywear_run_experiment`。
- **线程安全解耦**：LVGL 非线程安全，Agent 线程**不直接调 LVGL**；切页请求写入互斥邮箱（`pw_ai.c`），
  由 GUI 主循环轮询执行。
- **自定义 Skill（大赛硬性项）**：`phywear-physics-coach.md`（5,165 B）——
  页名对照表、4 个工具用法、标准流程、物理关系（g/L/T、lux 量级、频谱主频）、边界条件。
  正本随应用源码入库，固件启动时由 `pw_skill.c` 安装到 `/data/agent/skills/`（真机 `/data` 是 tmpfs，重启即空），
  内容由 `tools/phywear/gen_skill_blob.py` 从 `.md` 生成 C 数组（`--check` 可做漂移自检）。
  **模拟器实测**：技能摘要 784 B（10 个内置）→ 1135 B（+351 B，正好一条），设备副本与仓内正本 md5 一致。
- **离线可用**：Agent 的关键词直通表（命中即本地执行工具、不经 LLM）已补 PhyWear 意图，
  无 Key / 断网也能"打开单摆""读加速度""跑单摆实验"。
- **真机状态**：真机已烧入含 `ai_agent` 的固件 —— Agent 启动、40 个工具注册（含本队 4 个）、
  **Skill 自动装机成功**、Agent loop 运行、事件经消息总线被消费（见 6.6、6.7）。
  **真机 LLM 对话尚未联调**（真机无 WiFi；BT/PPP 已通到 IP 层但未接 LLM）；**LLM 路径已在 goldfish 模拟器实测通过**（MiMo Token Plan，含 LLM 自主工具调用，`docs/evidence/llm-20260913/`）。

---


### 4.7 「主动 + 执行」场景（本队，2026-09-15 打通）

赛道要求"至少一个主动 + 执行的场景"（纯对话不算）。PhyWear 的实现是：

```
晃表 → GUI 循环 10Hz 检测"持续摆动"(窗口振幅 + 连续 2.5s + 60s 冷却)
     → 事件写进手表 AI 消息日志，并 pw_ai_ask() 推给端侧 Agent
     → Agent 离线意图表命中关键词 → 调 phywear_run_experiment{screen:"pendulum",seconds:10}
     → 经邮箱请求 GUI 线程打开单摆页（LVGL 只在 GUI 线程动）
     → 单摆页自己 50Hz 采样/解算 g，算完 pw_ai_publish_result() 登记结果
     → 工具【只等结果、读结果】(自己不采样 IMU) → 回复经 humanize 进手表日志
```

**架构关键**：I2C 上**永远只有 GUI 那一路采样**。09-13 那次"启动十几秒整机卡住"的真因就是
Agent 任务里也按 50Hz 采同一颗 IMU（oneshot 还每样本 open/close 一次），两路抢同一条 I2C；
本次把 Agent 侧采样彻底删掉，改成"开页 → 等页面登记结果 → 读结果"。

**本次修掉的两个真崩溃**（都是之前没人查到底的）：

| 现象 | 根因 | 修法 |
|---|---|---|
| Agent 没起来时一推事件就 panic（`task: phywear`） | `velaclaw_client_open()` 在总线未初始化时也会成功，随后 `velaclaw_ask → msg_queue_push → pthread_mutex_take` 撞 `DEBUGASSERT(NXSEM_IS_MUTEX)`（`semaphore.h:625`） | 新增 `message_bus_ready()`，本地客户端 `open()` 阶段即拒绝并返回 NULL；`pw_watch` 改走受保护的 `pw_ai_ask()` |
| Agent 起来后工具末尾 ASAN 双重释放 | `pw_tool_emit()` 内部已 `cJSON_Delete(root)`，新写的分支又删一次 | 去掉两处多余 delete |

> 崩溃 1 不只影响主动场景：**AI 教练页的按钮**同样走 `velaclaw_ask`，"Agent 没起来时点按钮"
> 一样会把整机打崩。现在两条路都安全（模拟器实测：干净记一条"not connected"并继续存活）。

**「Agent 开机自启」缺口**：文档原写"端侧 loop 开机启动"，但板级 `etc/init.d/rcS`/`rc.sysinit`
都是 0 字节，`ps` 里从来没有 `ai_agent`（要手敲）。本次把 `ai_agent &` 写进板级
`etc/init.d/rc.sysinit`（该文件由 `src/CMakeLists.txt:52-53` 打进 ROMFS `/etc`）。
⚠️ 该文件会被 **C 预处理器**处理，**行首不能用 `#` 注释**（会当成非法预处理指令导致构建失败）。

**结果上报的三道有效性门**（只决定"要不要上报给 Agent"，页面上的原始数值不过滤、不隐藏）：
① 独立数据（≥1s 新样本，否则连续两次分析的是同一段数据，稳定性检查形同虚设）；
② 可复现（连续两次估计相差 ≤8%）；③ 合理性（g ∈ 5..15 m/s²）。
效果：真机静止 70s 内，上报垃圾值从 **~3 条/秒** 降到 **1 条被拦 + 1 条漏过**（拦截可审计）。
**残余风险如实写**：窗口内仍可能漏（实测漏过 7.52 m/s²）；桌面振动能连续产生各种垃圾值，
**主动场景的测量值只有在用户真的摆动时才有意义**。

证据与复现：`docs/evidence/proactive-20260915/`（含真机开机自启日志、`ps`、拦截日志、复现脚本）。


### 4.8 惯性标尺 / 位置标定（③，2026-09-15 起）

**目标**：把 IMU 全链路变成可测量的"标尺"——姿态可信、零偏可标、位移有限度地可测，
并且**每个数字都有真值来源**。分两块：

**(1) 姿态解算 `pw_ahrs.{c,h}`：Mahony 显式互补滤波（MARG 9 轴）**

选它而不是 Madgwick/EKF 的理由：零偏是**显式积分状态**（可以报出来、可以标）、
算力最低（~100 flop/次）、磁力计只修偏航（权重 `wmag` 默认 0.5，且**默认不进积分**——
磁噪声被积进零偏会让偏航游走，实测 σ_yaw 从 2.7° 涨到 12°）。状态 76 B，无静态数据。

实现要点（都是踩过坑才定下来的）：
- **误差项符号**：`e = a×v + wmag·(m×w)`，`ω = gyr − bias + kp·e`，`bias −= ki·ei·dt`；
  其中 `v = Rᵀ·[0,0,1]`、`w = Rᵀ·[bx,0,bz]`、`bx=√(hx²+hy²)`（只让偏航可观测）。实测无零偏
  静置收敛 **0.88°**，验证了符号与坐标约定。
- **真实 dt**：调用方用 `CLOCK_MONOTONIC` 前后差算 dt，**不用固定标称值**（参考包那份
  MATLAB 就是固定 `1/200 s`，上游丢帧全被当成理想均匀网格）。
- **磁限速**：早期用"占空比限速"（只让 1/5 的更新带磁），冷启动偏航收敛慢一个数量级；
  改成"每次都用、但权重小"后 30 s 内收敛。
- **零偏的正解是静态标定**：滤波器自己的积分项 60 s 只能恢复 22%/88%/16%（偏航轴最差），
  未标定 0.5~0.7 dps 零偏会让偏航残留 ~11°。所以推荐流程是
  **先 `pw_calib_bias_*` 静止标定零偏 → 预置进滤波器**，实测残差回到 ~1°。

**(2) 标定数学 `pw_calib.{c,h}`：全部流式充分统计，不存原始样本**

| 方法 | 真值来源 | 本批实测（合成数据自检） |
|---|---|---|
| 一维/二维最小二乘 | 尺子/卷尺读数 | 系数还原到 1e-7，rms=0 |
| **六面法** 加速度零偏+刻度 | **重力**（理想读数 ±1 g） | 零偏误差 <1e-3 g、刻度 <1e-3、串扰 <1e-3 g |
| **磁椭球** 硬铁+软铁（9 参数代数拟合 + Jacobi 特征分解） | 当地 WMM/IGRF 的 \|B\|/倾角（**不得用"典型 50 µT"**） | 球心误差 **0.00 mG**、校正后模长离散 **0.000%** |
| **陀螺静态零偏** | 静止时角速度为零 | 还原准确；±2.9 dps 抖动被"不够静"判据拒绝 |

两个必须写清的边界：
- 磁椭球拟合**只能定出软铁矩阵到一个未知旋转**（`A` 与 `A·R` 给同一个二次型），
  本实现取对称平方根为规范选择；残留的偏航常数偏移要靠"对准已知方向/转台"消掉。
- 没有真值装置时，本库只能给"重复性"，**给不出"标定后精度"**。

**(3) 本批验证（三层，见 `docs/evidence/imu-ahrs-20260915/`）**

| 层 | 结果 |
|---|---|
| 主机单测 `tools/phywear/hosttest_math.sh` | 两个自检 rc=0（<2s，改算法不必烧板子） |
| 模拟器 `phywear ahrs` / `phywear calib` | rc=0；无 `/dev/mag0` 时自动退化 6 轴 |
| **真机** `phywear ahrs 20` | 1000 样本 50 Hz、1000 带磁；**AHRS 的 roll/pitch 与加速度计直接解算的倾角相差 ~0.5°**；mag 关时 yaw 以 ~2°/s 漂（与估出的 −2.18 dps 零偏吻合），mag 开后 **yaw 稳定在 75.6° 不漂** |

**已知限制**：精度指标尚未用转台真值验证（"静态 ≤0.5°"目前只是"与加速度计一致"的间接证据）；
合成噪声下姿态误差 2~3°、yaw σ≈2~3°，**增益仍需按真机数据标定**。
UI 板块归属（生活/工具）与真值测量参与方式待用户拍板，故本批只交付与 UI 无关的算法核心。


### 4.8.1 轨迹页：相对位移 `pw_traj.{c,h}`（2026-09-15）

**做什么**：把比力方程 + **零速修正（ZUPT）** + 梯形积分做成"短窗相对位移"模块，第 5 页
「轨迹」把它显示成 XY 平面内的落点轨迹 + X/Y/Z 数值 + 速度 + 已运行时间 + ZUPT 状态。

**为什么只做这三步**（不做完整惯导）：本机是消费级 MEMS，没有 GNSS/视觉/气压等外部观测，
完整惯导的初始对准、地球自转补偿、圆锥/划桨补偿在这里**远小于零偏与噪声**，做了也只是好看。

**实现要点（都是踩过坑才定下来的）**

| 机制 | 取值 | 为什么 |
|---|---|---|
| 静止判据 | 10 点滑窗上三条：`|mean\|a\|-1|<60 mg`、`mean\|ω\|<3 dps`、`std(\|a\|)<45 mg` | 只判 `\|a\|` 会把**水平推力**误判成静止（2 m/s² 只让 `\|a\|` 变 20 mg）——主机单测第一版就抓到轨迹恒为 0 |
| 出静止（快） | **两速计票**：角速度超限记 2 分、仅比力超限记 1 分，够 4 分才退出 | 滑窗均值滞后 0.2 s，只用滑窗会抹掉起步前沿（实测少 40% 位移）；只看瞬时又会被单点野值骗（真机偶发 0.11 g 模长跳变）。分权后：真转动 2 拍即出，野值只 2 分不退出 |
| 进静止（慢） | 窗口与瞬时都判静、连续 5 拍 | 宁可少 ZUPT，不可误 ZUPT |
| 物理有效性门 | `\|a\| ∈ [0.3, 2.5] g`，越界样本整拍丢弃 | 真机驱动启动期会先给零值样本，`\|a\|=0` 是**自由落体**，会被积成 −g（实测 1.4 s 到 9 m/s、Z −2.2 m） |
| 角速度输入 | **零偏改正后**（用 AHRS 自己的 `bias` 估计） | 真机未标定零偏实测 **2.48 dps**，与静止门限 3 dps 同量级，不改正会让状态反复抖动 |
| 姿态对准 | 开页后 3 s 只收敛姿态、不积分 | AHRS 从单位四元数收敛到真实倾角；不这么做对准瞬态被积成米级位移（实测 4 s 内 X 2.4 m、Z 4.8 m） |

**主机单测**（`bash tools/phywear/hosttest_math.sh`，已并入常规单测）：四关全过，
`rc=0 max_disp_err=0.0248 m` —— 20 cm 整周期正弦推手（无零偏误差 **2.5 cm**）、注入 1 mg 零偏、
启动期零样本必须被丢弃、静止 60 s（噪声 + 每 50 拍 0.11 g 模长野值）**零误退出**。

**真机实测**（固件 md5 `b0ddff97a78508ff6ce48e7e82c0c0b3`，2,119,696 B；证据 `docs/evidence/traj-20260915/`）

| 项 | 结果 |
|---|---|
| 静置 40 s | 对准期后 **still=1、v=0.0、x/y/z 全程 0.0 cm** —— 静止不漂 |
| bench 推-停循环 | 推的时段（`\|ω\|≈16.3 dps`）位置前进；静止时段（`\|ω\|=0`）**位置冻结** —— ZUPT 生效 |
| 帧率 | 轨迹页 **fps 8 / loops 58~63**（图线按 10 Hz 有意节流）；同固件实时数值页 **fps 27/29**，对基线 26 **无退化** |
| 资源 | 本页静态 SRAM **+64 B**（446,640 → 446,704，85.20%）；轨迹点缓冲与算法状态都在堆（PSRAM），不进 BSS |

**诚实的边界**（界面与文档同时标注「相对位移，非全局定位」）：
- 误差随时间按 t²（加速度零偏）/t³（陀螺零偏）增长：**1 mg 零偏 60 s 漂 17.6 m**、**0.1°/s 陀螺零偏 60 s 漂 616 m** ——
  所以本模块只主张**短窗（1~2 s）**相对位移，且靠 ZUPT 把静止段钉住。
- **恒定且纯平移的加速度**与"倾斜"在单加速度计上不可区分（固有不唯一性），那种理想化运动本模块不保证能测到。
- **尚无真机手推的位移真值**：精度真值需按 `真值测量操作协议.md`（直尺 300 mm 往返）由人做，本次不宣称 cm 级精度。

### 4.10 蓝牙阶段 A：片内 LCPU 的 HCI 通了（2026-09-16）

**背景修正**：此前结论是"蓝牙 no-go"，依据是"片内 LCPU 控制器与协议栈都在，但断在
`UART_BTH4` 未开与端口 H4 驱动缺 CMakeLists 接线"。阶段 A 就是去**证伪/证实**这一点：
不发散地做功能，只回答一个可证伪的问题——**这颗片内蓝牙控制器能不能通过 HCI 说上话**。

**做法（三层隔离，失败也知道断在哪）**：刻意**绕开 zblue host 栈**，用本队自己的探针
`phywear bthci`（`pw_btprobe.c`）直接开 `/dev/ttyHCI0`，发一条 `HCI_Reset`（`01 03 0C 00`）等回包。
三层判据：① 端口打不开 = 传输层没起来；② 打得开无回包 = 端口在、对端不应答；③ 回 Command Complete = 链路通。

**结果：判据③通过。** 真机回 `04 0E 04 06 03 0C 00` —— HCI Event/Command Complete、
opcode `0x0C03`、**status=0**（成功）。即**片内 LCPU 蓝牙控制器活着且应答正常**。

**代价（真机实测）**：flash **+84,760 B**（12.63% → 13.14%）、SRAM **+40,756 B**
（446,704 → 487,460，85.20% → **92.98%**）。其中 SRAM 的大头是 zblue 的 `sys_work_q_stack`
（32,768 B，已按实测降到 4096 腾出 28 KB）；UART_BTH4 一旦打开，zblue 的缓冲/工作队列会被一起链进来。

**源码改动（3 处，均可一行回退）**：
① 板级 defconfig `CONFIG_UART_BTH4=y`；
② `external/zblue/CMakeLists.default.txt` 把 `h4.c` 加入构建（`ZEPHYR_REPO_DEFAULT` 分支漏了，`3_0_1` 分支本来就有）；
③ 新增 `.../hci/bt_snoop_stub.c`（`btsnoop_log_capture` 空实现——唯一实现在被 `CONFIG_BLUETOOTH` 关掉的 framework 里）。

**阶段 B 当时（09-16）的阻塞点**：zblue host 栈在 `ZEPHYR_REPO_DEFAULT` 分支编不过
（`CONFIG_BT_SHELL=y` 让 CMake configure 失败；拉进 host 栈后报 `CONFIG_BT_MAX_PAIRED undeclared`）。
**这两处已于 09-18 解决，阶段 B 也已打通 —— 见 §4.10b。**

> 证据：`docs/evidence/bt-stageA-20260916/`（探针原始串口输出 + 代价表 + 阻塞点实测记录）。

### 4.10b 蓝牙阶段 B：host 栈打通（B1/B2），根因是 NuttX 的 fd 表按 task_group（2026-09-18）

**现象**：同一 boot 内裸探针 `phywear bthci` 通（`01 03 0c 00` → `04 0e 04 06 03 0c 00`，status=0），
而走 zblue host 栈的 `phywear cap bt` 只打印 `dev:0: Sending HCI_RESET` 就返回
**`[bt] bt_enable rc=-5`（-EIO）**。表象是"控制器不回包"。

**排查方法（先要能看见，再谈定位）**：本板 defconfig 原先**没有** `CONFIG_BT_DEBUG_LOG`，
而 `port/include/zephyr/logging/log.h` 用 `CONFIG_BT_DEBUG_LOG_LEVEL` 门控 `LOG_*` ——
于是**整个 host 栈与 H4 驱动的日志被编译成空语句**，失败时串口上一个字都没有（这才是第一道坎）。
打开后 + 在 H4 驱动的 fd 边界加字节级收发打印，拿到决定性原文：

```
[H4] TX (4 B): 01 03 0c 00                      ← 与裸探针**一模一样**的 4 字节，确实发出去了
[H4] h4_send_data -> -1 (want 4, errno 9)       ← errno 9 = EBADF
<err> Unable to send to driver (err -22)
<wrn> opcode 0x0c03  status 0x1f                ← 这一行是**伪造**的，见下
[bt] bt_enable rc=-5 (FAILED) elapsed=4ms       ← 4 ms，不是 10 s 超时
```

**根因（两层）**：
1. **`-EIO` 是假象**。`hci_core.c` 在 `bt_send()` 返回错误后调用
   `hci_cmd_done(hdev, opcode, BT_HCI_ERR_UNSPECIFIED /*0x1F*/, buf)` 把一个**合成 status**
   写回命令缓冲，`bt_hci_cmd_send_sync()` 再把这个 status 翻成 `-EIO`。
   所以"控制器回了 0x1F"是误读 —— 控制器**一个字节都没收到**。
2. **真因是"谁在写 fd"**。zblue 的 NuttX port 用 `bt_tx_irq_raise() → k_work_submit(&hdev->tx_work)`
   把 HCI 命令发送放在 **Zephyr 系统工作队列线程**里执行；而 NuttX 的**文件描述符表挂在
   `task_group` 上**（`fs/inode/fs_files.c` `file_get2()` → `nxsched_get_fdlist()` →
   `sched/sched/sched_getfiles.c` `ret = &group->tg_fdlist;`），系统工作队列是 `z_sys_init()`
   在**板级启动那个任务组**里 `pthread_create` 出来的 —— 它的 fd 表里没有 `h4_open()`
   打开的 `/dev/ttyHCI0`，`write()` 恒返回 `EBADF`。
   决定性证据（打印任务号与 fd 表指针）：

   ```
   [H4T] h4_open      tid=9  fdlist=0x600ad1e4 h4->fd=8    ← 打开者
   [H4T] h4_rx_thread tid=21 fdlist=0x600ad1e4 h4->fd=8    ← pthread 与创建者同组 ✓
   [H4T] h4_send      tid=5  fdlist=0x20010120 h4->fd=8    ← **不同组** ✗
   ```

   这也解释了为什么裸探针能通：它是**同一个任务里 open + write**。

**修法（`port/drivers/bluetooth/hci/h4.c`，一处）**：让**打开 fd 的那个线程组独占 fd I/O**。
`h4_send()` 只把缓冲挂进新增的 `tx_fifo`；真正的 `btsnoop_log_capture() + write() + net_buf_unref()`
放进 `h4_hw_tx()`，由 `h4_rx_thread`（`h4_open()` 亲手创建、与 open 同组）在每轮循环开头清队列执行。
代价：`h4_data` **+24 B**，TX 最坏 +1 ms（RX 线程本身 1 ms 轮询节拍）。

**B1 实测**：`[bt] bt_enable rc=0 (host stack up) elapsed=1227ms`，同 boot 内 H4 原文
`TX 01 03 0c 00 → RX 04 0e 04 06 03 0c 00`。

**B2（GATT 服务 + 可发现广播）**，新增 `apps/examples/phywear/pw_btgatt.c`：

| 项 | 值 |
|---|---|
| Service | `e0f1a000-1b2c-4d5e-8f90-a1b2c3d4e5f6`（PhyWear Physics） |
| Sensor 特征 | `e0f1a001-…` READ \| NOTIFY，16 B 小端包：`int16 ax,ay,az (mg)`、`int16 gx,gy,gz (mdps/10)`、`uint16 lux`、`uint16 cmd_count`；带 CCC |
| Command 特征 | `e0f1a002-…` WRITE \| WRITE_WITHOUT_RESP，ASCII 命令，串口回显 |
| 广播 | `BT_LE_ADV_CONN`：AD = Flags + Complete Local Name `"PhyWear"`；SD = 128 位服务 UUID |

**服务端自检（把"不需要空口"的那部分自动化）**：`pw_btgatt_start()` 末尾跑 `pw_btgatt_selftest()`，
逐条 dump 属性表 + 走真实 `bt_gatt_attr_read()` 读一次 + 走一遍命令写回调。实测：

```
[bt] SELFTEST attr[2] e0f1a001-1b2c-4d5e-8f90-a1b2c3d4e5f6 perm=0x01 read=Y
[bt] SELFTEST attr[3] 2902 perm=0x03 read=Y write=Y
[bt] SELFTEST attr[5] e0f1a002-1b2c-4d5e-8f90-a1b2c3d4e5f6 perm=0x02 write=Y
[bt] SELFTEST read sensor -> 16 B (want 16)
[bt] SELFTEST decode ax=-20 ay=36 az=-1012 mg |a|=1013 mg     ← 平放，重力压 Z 轴
[bt] SELFTEST write ret=8 (want 8) / SELFTEST PASS (fails=0)
```

**实测踩到并修掉的 5 个真问题**（都留了证据）：
① **自检抓到我自己重构引入的 BUG**：改采样函数时把 `pw_sensors_read_imu_oneshot()` 整行删了，
`imu` 成了未初始化栈变量，把栈垃圾当加速度上报（`|a|≈15.4 g`）。已修 + 加**物理量纲判据**
（合矢量必须落在 300~3000 mg）——**"读数看起来像数"不等于"对"**。
② **判定器假 PASS**：B3 判定器用 `[bt] cmd #n` 判"手机写进来了"，而**自检自己也会写一条**⇒
没手机也判 PASS。已把自检改用专用命令名 `selftest`，并把该判定限定在 `B3 connected` 之后；
负对照现在正确报 FAIL ——**判定器报 PASS 本身也要有负对照**。
③ **阻塞预算**：GATT 回调与周期通知都跑在 `k_sys_work_q`（栈 4096 B），而
`pw_sensors_read_imu_oneshot()` 最坏 ≈160 ms；原来再套 6 次重试最坏能堵队列 ~1 s，
会连带推迟 HCI 命令 TX 与收包。改成 `pw_sample_once(int strict)`：周期路径**只读一次**、
样本不合理就不更新（保留上一个好值）；只在 GATT 读/自检时重试。
④ **广播地址**：实测 `LE Set Ext Advertising Parameters` 的 `own_addr_type=0x01 (RANDOM)`，
且先发 `LE Set Advertising Set Random Address` ⇒ 手机**必须按名字找**，按 MAC 找不到。
⑤ **H4 RX 的静默哑弹**：`h4_rx_thread` 里判据写成 `if (len == -EAGAIN)`（**永假** ——
   `read()` 出错返回 -1、EAGAIN 在 `errno` 里），于是任何一次瞬时读错误都会
   `close(fd) + return`；而 B1 的修复把 **TX 也交给了这个线程** ⇒ RX/TX 一起死。
   又因为出货配置没开 `CONFIG_BT_DEBUG_LOG`、那句 `LOG_ERR` 被编译成空语句，
   现场只会表现为"手机突然连不上"，无从归因。已改为 EAGAIN 重试、其它错误累计
   连续失败（上限 50）再重试、放弃时用**无条件 printf** 留原文。
   代价 +4 B（被对齐吸收，SRAM 未变）。

⑥ **通知帧的 value handle 是 0x0000（B3 收尾时抓到，比 ①~⑤ 都隐蔽）**：
   设备侧 `[bt] notify #N rc=0 len=16` 连打 56 条一切正常，宿主侧**一个包都收不到**。
   打开 H4 字节级 trace 后把空口原文按 ACL→L2CAP→ATT 解码，看到通知帧是
   `… 1b 00 00 …`（value handle **0x0000**），BlueZ 找不到该句柄的特征 ⇒ **静默丢弃**。
   代码级根因在 zblue：上游 Zephyr 在 `bt_gatt_notify_cb()` 里就地在已填好
   `data.handle` 的 `struct notify_data` 上做 CCC 遍历；zblue 为多控制器抽出的
   `gatt_notify_mc()` 里 `struct notify_data data = { 0 };` **漏了 `data.handle = handle;`**
   （`gatt_indicate_mc()` 同）。修两行后同一脚本解出 `… 1b 10 00 …`（0x0010），
   宿主立刻收到。**只能靠字节级 trace + 分层解码发现**，见 §4.10c。

**稳定性**：`phywear cap bt` 后连续 185 s，心跳 6 次全在、`fps=3`（root 页正常区间 3~7）、
**无 panic/assert/重启/watchdog**。

**B3 已完成（2026-09-18 晚，无人化）**：见 §4.10c。

> 证据：`docs/evidence/bt-h4-20260918/`（44 个证据文件，`SHA256SUMS.txt` 全量登记、可用 `sha256sum -c` 自校验）。
> 回退：tag `pre-bt-fix-20260918` + `~/桌面/PhyWear-rollback-20260918-btfix/`。

### 4.10c 蓝牙 B3：连接/读/订阅/写全部打通（2026-09-18 晚，**不需要手机**）

**转折点**：VM 的蓝牙启用后（宿主 Intel AX201 → `hci0`），B3 不再依赖人工 ——
宿主自己当 BLE 中心设备，把"用手机点的那几下"原样做一遍。

**一条命令**（复位 → `phywear cap bt` → 扫描/连接/读/订阅/写 → 双向判定 → 落盘 sha256）：

```bash
python3 tools/phywear/pw_bt_b3.py --central --out docs/evidence/bt-b3-$(date +%Y%m%d-%H%M)
```

**实测结果（退出码 0）**：

| 侧 | 检查项 | 结果 |
|---|---|---|
| 设备 | 广播 rc=0 / 服务端自检 PASS / 已连接 / 订阅 Notify / 收到写命令 / 通知无报错 | **6/6 PASS** |
| 宿主 | 扫描到 PhyWear / 连接+服务解析 / 找到服务与两条特征 / 读 `…a001` 16 B / `\|a\|` 在 300~3000 mg / **收到 ≥2 个 Notify** / 写 `…a002` 'ping' | **9/9 PASS** |

原文（宿主侧）：`PASS 读 …a001 得到 16 B` → `ax=-21 ay=35 az=-1014 mg |a|=1015 mg`；
`PASS Notify 收到 >= 2 个包  收到 2 个，首个 e9ff25000bfc620004ff7e0000000100`。

**空口前后对照（字节级，同一解码脚本同一口径）**：

| | 订阅窗口内通知帧 | value handle | 宿主侧收到 |
|---|---|---|---|
| 修复前 | 56 | **0x0000 × 56** | **0 个包** |
| 修复后 | 2 | 0x0010 × 2 | **2 个包** ✅ |

**新增工具**：
`tools/phywear/pw_ble_central.py`（宿主侧中心设备，BlueZ + D-Bus，不需要 `bleak`）、
`tools/phywear/pw_h4_notify_decode.py`（`[H4] TX` 分层解码 + `--expect-handle` 断言）；
`pw_bt_b3.py` 加 `--central`，并把宿主侧 PASS 表**并进判定** ——
设备说"我发了"不等于对端真收到了（§4.10b ⑥ 正是如此），两侧都过才给 ✅。

**踩到的两个工具坑（都已修，值得记）**：
① 中心设备**边等边必须抽干串口**：第一版用 `subprocess.run()` 死等不读串口，
宿主侧 9 项全 PASS，而设备侧证据全留在 tty 缓冲区没被收走 ⇒ 判定成"手机没连上"。
**串口取证器不读，就等于没有证据**。
② 扫描前必须清掉 BlueZ 里同名的**缓存设备记录**：手表广播用随机地址，
每次上电可能换一个，而 BlueZ 会把上次那个 `dev_XX…` 连同 `Name="PhyWear"` 一起缓存 ——
不清就会连到一个早已不广播的死地址上。

**回退开关**：诊断用的 `PW_H4_TRACE`（`h4.c`）与 `PW_BT_NOTIFY_TRACE`（`pw_btgatt.c`）
均已归 **0**，并在此基础上**重新构建、烧录、复跑 B3** —— 出货固件上仍是 6/6 + 9/9 全过。
蓝牙这条线的 SRAM 增量为 **0**（修复本身是两行赋值）。

### 4.10d 蓝牙连接标识 + 手表版蓝牙串口（2026-09-18 晚，用户需求）

**需求**：主界面要有"连没连上"的标识（未连接灰、已连接蓝）；要有蓝牙连通性测试；
手表上要能直接看到蓝牙信息（手表版蓝牙串口）。

**实现**：

| 项 | 内容 |
|---|---|
| 主界面标识 | 标题右侧 10px 圆点 + 「蓝牙」：未连接 `0x5B6875` 灰 / 已连接 `0x3B82F6` 蓝（带静态外发光）。250 ms 的 LVGL 定时器轮询 `pw_bt_link()` 自动切换；**可点**，进去就是蓝牙页 |
| 新 GATT 文本特征 | `e0f1a003-…`（READ \| WRITE \| WRITE_NR \| NOTIFY + CCC）：手机写自由文本 → 设备记 `[RX]` 并回 `echo: …`；手表「发送测试」→ notify 给手机。句柄追加在表尾（`0x0014/0x0015/0x0016`），**旧的 0x0010/0x0011/0x0013 一个没动** |
| 蓝牙串口页 | 状态（未连接/已连接/已连接·已订阅）+ 对端地址 + `RX/TX/echo/MTU` + 10 行 TX/RX 滚动日志 + 发送测试/清空；打开页面时若 host 栈没起会自己起（`pw_bt_init()` 已改幂等） |

**唯一的硬约束是线程**：BT 回调在 Zephyr 系统工作队列线程、界面在 phywear 主线程，
所以回调只写共享标志与环形日志（`struct pw_bt_link_s`），**绝不碰 LVGL**；界面只读
`pw_bt_link()` 且只在 `lv_timer` 里改控件。两边不加锁（单字宽读写 + `seq` 标定），
代价是 UI 极端情况下会看到半行文本、下次刷新自愈 —— 换取的收益是 HCI 回调不阻塞。

**空口连通性测试的判据是"收到 echo"而不是"写成功"**：BlueZ 的 `WriteValue` 成功只说明
本地协议栈发出去了，空口丢没丢、设备收没收到、设备能不能主动发回来它都不知道。
宿主 `pw_ble_central.py --text-test` 写一条带时间戳的文本、要求收到原样 echo。
实测 13/13 PASS（写 `hello-1789723972` → 收到 `echo: hello-1789723972`）；
设备侧 8/8（`[bt] msg in` + `[bt] echo tx rc=0`，且都限定在"已连接之后"，
避免把自检自己写的那条误算成手机写的）。

**真机截图**（`docs/evidence/btui-20260918/`，4 张）：主页灰点 / 主页蓝点 /
蓝牙页未连接 / 蓝牙页已连接（`已连接`(蓝) · `对端 04:EC:D8:F3:73:8D (public)` ·
`RX 2 · TX 1 · echo 1 · MTU 23` · 日志 `[RX] hello-…` → `[TX] echo: hello-…`）。

**顺手修掉一个字体流水线 bug**（不修则这一步根本走不通）：`gen_font_ranges.py` 把所有
"码点 ≥ 0x2E80"的字面量字符都当要渲染的字，而 **U+FE0F（变体选择符）** 恰在此区间、
且字体里没有该字形 ⇒ `lv_font_conv` 退出 1、**五个字号一个都不生成**；触发条件只需要
源码里有一个 `⚠️`（`pw_skill_blob.c` 里就有）。它还误扫 `*_blob.c`（一篇 markdown
文档的数据块，**从不渲染**，却贡献 565/725 个汉字）。修后汉字子集 725 → **508**，
5 个字号源文件合计 −385 KB。**所以这一版加了整个功能，flash 反而少了 55 KB。**

**回归**：设备侧一键验收 **18/18**；fps **12 不变**（loops 200）；SRAM
495,688 → **496,372 B（+684 B，如实报数：本项目第一次为功能新增静态 RAM）**；
flash 2,609,752 → **2,554,544 B（−55,208 B）**；主页金标 sha256 由 `d552c57b…`
换为 **`e356689d…`**（**有意改版**，已在 `pw_accept.py` 注释里写明新旧对照与理由）。

### 4.11 动效查表（P1-2）与 LVGL 版 UI Skill（2026-09-16）

**动效为什么用查表**：曲线形状固定、每秒被算几百次。离线按帧率采样存 flash、运行时只做
"索引 + 线性插值"，代价 **516 B flash / 0 B SRAM**；**真机实测 151.25 ns/次 vs 解析式 2088.40 ns/次（×13.8，每次省 1937 ns）**、主机 ×10.8。
但**查表不是帧率杠杆**：path 每帧每对象只调一次，一次 320 ms 动效仅省 ~37 µs（一帧预算的 0.2%），
价值在成本确定性与免 libm 抖动。**⚠️ 本节曾写「真正压帧率的是每帧全屏推屏 351 KB ≈ 14 ms」——该判断已于 2026-09-16 实测推翻**：
`fb_flush_start()`（`drv_lcd_fb.c:393-465`）送给面板的窗口是 `p_fb->fb_clip`（**脏区**），全屏 `fb.area` 只用于行偏移换算，
即**脏区推屏 vendor 早已实现**；且把卡片圆角 14→0 做 A/B，实时页与轨迹页 fps 完全不变（34/36、10/11），
**圆角回退 SW 的量级也可忽略**。本轮真正有效的是主循环休眠（详见 §4.12）。精度：表长 129 点（N=64 翻倍，误差降到 1/4，运行时零成本），
spring 插值最大误差 **1.06e-03**、decay **2.29e-03**，换算到 LVGL 的 0…1024 分辨率约 **2.4 个步进**。
曲线按 `s(1)` 归一到末帧严格 1.0（否则动画最后一拍会跳 0.7 px）。
模块：`pw_motion.{c,h}`（纯算法，可 PC 单测）+ `pw_motion_lvgl.{c,h}`（LVGL path 适配）+
`tools/phywear/gen_motion_table.py`（表的唯一真值源）。**解析式实现始终保留**：
既是"查表 vs 解析"对照计时的基准，也是宏开关 `PW_MOTION_USE_TABLE=0` 的回退路径。
**不做 PID**：弹性来自固定的物理曲线参数（ζ=0.40/ω=12，过冲 ≈25%），不是逐帧迭代调参。

**UI Skill 吸纳**：把开源 `ui-ux-pro-max-skill` 的**数据层分类骨架**（风格/色板/字体配对/推理规则/UX 指南）
搬成本板 LVGL 版 `skills/phywear-lvgl-ui.md`（随固件安装，AI Agent 可读），**栈层与输出层重写**
（HTML+Tailwind → LVGL 样式属性），**Web 专属全部裁掉**（Google Fonts 导入、响应式断点、键盘 focus、
hover、Tailwind JIT、React 等）。同时把本板硬约束写成"性能铁律"：**圆角必须 0**
（EPIC 的 FILL/BORDER 分支 `radius != 0 → return 0`，一律回退 CPU）、渐变仅 2 段 H/V、禁阴影/模糊/变换、
动效只改几何且 ≤320 ms。数据层导入器 `tools/phywear/import_ui_skill.py`（列名自动归类 +
低负载/条件可用/禁用三档筛选 + 色值转 RGB565 与对比度检查）；`--sample` 自检已实测，
**真实 CSV 路径因开发环境无法访问 github 未实测**（如实在文档里标注）。

> 详见 `docs/10_LVGL动效与风格规范.md`（含改造清单、映射表、回退方案与验证口径）。

### 4.12 P0 首轮真机 A/B：主循环休眠 +30.8%，圆角与"全屏推屏"两条旧判断被推翻（2026-09-16）

**口径**：同页面 + 同 30 s 心跳窗口 + 单 GUI/boot；`fps` = `LV_EVENT_RENDER_READY` 计数/秒。
证据：`docs/evidence/p0-20260916/`。

| 页面 | 基线（固定 10 ms 休眠） | 改后（自适应休眠 ≤2 ms） | 圆角 14→0 叠加 | 判定 |
|---|---|---|---|---|
| 实时数值页 | fps **26 / 28**（loops 26 / 28） | fps **34 / 36**（loops 34 / 36） | fps 34 / 36（不变） | 休眠 **+30.8%**；圆角 **零增益** |
| 轨迹页 | fps **8 / 9**（loops 57~59） | fps **10 / 11**（loops 203 / 211） | fps 10 / 11（不变） | 同上 |

**改动（一行宏可回退）**：`phywear.c` 主循环把固定 `usleep(10 ms)` 改成**自适应休眠** ——
取 `lv_timer_handler()` 返回的"下次定时器还有多久"，上限 `PW_LOOP_SLEEP_MAX_MS=2`、下限 1 ms。
机制：固定 10 ms 会给每一帧叠加最多 10 ms 处理延迟，在 35 ms/帧的实时页上约占 30%。
**回退**：`PW_LOOP_SLEEP_MAX_MS` 改回 10 即完全恢复原行为。

**两处旧判断的更正（我此前审查报告的错，已同步改掉 docs/10 与 motion 证据包）**：
① **"每帧全屏推屏 351 KB ≈ 14 ms"不成立** —— `fb_flush_start()`（`drv_lcd_fb.c:393-465`）送给面板的窗口是
   `p_fb->fb_clip`（**脏区**），全屏 `fb.area` 只用于行偏移换算：**脏区推屏 vendor 早已实现**；
② **圆角回退 SW 的量级可忽略** —— 卡片 radius 14→0 在两页的 A/B 都是**零增益**（34/36、10/11 逐项相同），
   故保留 14 不牺牲观感，`PW_CARD_RADIUS` 仅留作实验旋钮。
（"圆角会走 SW 光栅"这一**机制**仍成立，见 `lv_draw_sifli_epic.c` 的 `radius != 0 → return 0`；被推翻的是它的**量级**。）

**第二轮：瓶颈定位（探针 `[PERF]` 每秒一行，`PW_PERF_PROBE`）**

| 页面（数据连续变化） | fps | 单帧窗口 render_avg | 面板一帧（60 Hz） |
|---|---|---|---|
| 水平仪实时页（bench） | **48 / 49 / 48** | **17.1 ms** | 16.7 ms |
| 三轴图线页 | 13 | 19.2 ms | 16.7 ms |
| 轨迹页（bench） | 10~11 | 21.4 ms | 16.7 ms |
| 单摆页（bench） | 22 | 26.7 ms | 16.7 ms |

**结论：单帧成本 17~27 ms 与脏区内容几乎无关，17.1 ms ≈ 一帧面板扫描周期 ⇒ 流水线锁在面板刷新率上，
60 FPS 就是硬上限**（用户看到的 60 FPS 属该状态）；数据连续变化时稳定 **48~49 FPS（面板速率的 82%）**。
四个假设全部实测否决并留档（详见 `docs/evidence/p0-20260916/perf-campaign.md`）：
① "全屏推屏"错（vendor 用脏区 `fb_clip`）；② 跳过防撕行等待**更差**（45 vs 48）；③ 更新错峰**无差异**；
④ 刷新周期对齐 16 ms **无差异**。
**第三轮：LVGL 官方 benchmark 跑分 + 第五个假设否决**

`phywear lvbench`（本队新增入口，调 `lv_demo_benchmark()`）真机跑分，LVGL 9.1.0：
**全场景平均 39 FPS / CPU 83% / render 22 ms / flush 0**；轻场景天花板 **56~63 FPS**
（Empty screen 56、Single rectangle 63）；重场景全是 CPU 光栅瓶颈
（Screen sized text 15 FPS / 58 ms、Widgets demo 16、Containers with opa_layer 18）。
`flush` 列几乎全 0，与"脏区推屏 + 与渲染重叠"一致 —— **面板传输不是瓶颈**。
（跑分入口让 demos 进固件：flash +278 KB → 15.38%，不需要时可删 `lvbench` 分支。）

第五个假设「缩小每帧脏区并集能提速」也被实测否决：把实时页读数聚拢成一条窄带
（卡片 190×150→150×140、三轴值挪到 x=176、字号 XL→MED），脏区并集面积降到 ~55%，
但 **fps 47~49 / 16.9 ms 与聚拢前的 48~49 / 17.1 ms 实质相同** ⇒ 已回退版式（没收益就不牺牲观感）。

**至此五个假设全部被实测否决**（全屏推屏 / 防撕等待 / flush 次数 / 刷新周期 / 脏区并集），
一致指向：有效帧节拍下限约 16~18 ms 是**这套硬件+配置的固有开销**，本队页面在连续变化态
跑到 48~49 FPS（约为轻负载上限的 82%）；突破它需要面板级改动（帧率寄存器/LCDC 时序），
`co5300.c` 里 `frame rate=82` 只是注释 TODO、`syn_mode` 默认 DISABLE，故"锁在 60 Hz"仅属推断，不作为结论。

（以下为第二轮推断）**距 60 FPS 只差 0.4 ms/帧** —— 下一步性价比最高的是**缩小每帧脏区并集**（读数聚拢）；
突破 60 上限只能提高面板扫描率（面板级改动，需数据手册 + 人眼验收，未做）。

（以下为第一轮记录）**下一步疑点**：实时页 34/36 fps ≈ 28 ms/帧，成本落在 **LVGL 软件光栅 + flush 握手**上；
要往下砍必须先有"按页脏区面积 + 渲染耗时"的统计（方案见 `docs/07` 与 `docs/10` §4），
而不是继续猜面板带宽。

### 4.9 惯性标尺 UI：水平仪 + 标定向导（2026-09-15）

4 页横滑屏（无头入口 `phywear cap imu` / `imubias` / `imu6` / `imumag`，AI 可 `open_screen{imu}`；后续增补第 5 页「轨迹」见 §4.8.1，故现在共 5 页）：

| 页 | 内容 | 标定库 | 真值 |
|---|---|---|---|
| 实时数值 | 水平仪（**小球改坐标，不旋转对象**）+ roll/pitch/yaw + 零偏读数 + 磁有无 | `pw_ahrs` | — |
| 零偏标定 | 开始/停止采样 → 零偏 + 离散；**不够静就拒绝** | `pw_calib_bias_*` | 静止角速度为零 |
| 重力标定 | 六面各取 1s（`±X/±Y/±Z 朝下`），第六面取完自动求解 | `pw_calib_six_*` | 重力（±1 g） |
| 磁标定 | 全方向转动采样 → 硬铁中心/平均场强/形状离散 | `pw_calib_mag_*` | 场强恒定（椭球） |

**成本（真机实测，**该批统计时**的数值；当前全量见 §4.8.1）**：链接期 SRAM **+936 B**（491,576→492,512，93.94%）；
**运行时堆 +2,584 B**（`free` 实测 root 333,696 → 水平仪 336,280，即 LVGL 控件开销）；
实时页 **fps 26 / loops 26**（该页 6 标签 + 小球按 50 Hz 刷新，只在打开时消耗）。
flash 2,082,992 B（12.42%）。详细证据与复现见 `docs/evidence/imu-ui-20260915/`。

**标定成功路径的自动化验证**：给 UI 加了 `phywear imubench [页]` —— **注入**已知真值
（加速度零偏/刻度、陀螺零偏、磁硬铁中心）并自动走完标定流程，标题带 `[BENCH]`。
实测对照：零偏 **+572/−458/+687 mdps**（真值 573/−458/688，偏差 ≤1 mdps）、
重力零偏 **+0.020/−0.015/+0.030 g**（真值同，精确）、系数 0.980/1.020/0.990（=1/增益）、
磁中心 **+34/−119/+59 mG**（真值 35/−120/60，偏差 ≤1 mG）。
→ 三条标定链路的"算得对"端到端验通（不只验了拒绝分支）。
⚠️ 期间**改正单测一处口径错误**：`pw_calib_selftest` 把"修正系数"当"增益"比较，
`worst_rel_err` 恒含 ~6.28% 口径差；改为与 `1/gain` 比较后为 **0.0000**。

**交互已验**（模拟器触摸注入真按按钮）：点「开始采样」→ 按钮变「停止采样」、样本数递增；
再点 → 求解。因为模拟器一直在合成摆动，"够不够静"判据**如实拒绝**并显示「未标定」——
这正是我们要的行为（不给假数字）。**真机上的"成功标定"路径需人工按六面摆放与全方向转动**。

⚠️ **两件未完成的事（如实）**：① 主页 tile 入口**故意没接** —— 板块归属（生活/工具）待用户拍板，
落位时改一行即可；② 本批使累计 SRAM 达 **+3,072 B，超出"不超过 ~2 KB"额度约 1 KB**，
已有实测的 −45,752 B 腾挪方案（`docs/03 §5.1`）待用户选择。

## 5、性能实测

**图 3：渲染帧率时序（模拟器 EN/ZH 序列，标注 min 22 / target 43）**

![渲染帧率时序](project/fps_timeline.png)

| 指标 | 真机（SF32LB52 + 官方 EPIC + 本队优化） | 模拟器（goldfish，无 EPIC，软件渲染） |
|---|---|---|
| 优化前 | 约 3 FPS | 每帧 ~43 ms（1280×800 全幅光栅） |
| **优化后（当前口径）** | **平均 41 FPS（render 22 ms / flush 0）** | **22–23 FPS** 稳定 |
| 历史口径 | 43 FPS（**2026-09-12 旧固件**，官方 LVGL benchmark 全场景 avg：81% CPU / 20 ms / flush 0） | — |
| 摆测 g 误差 | 约 **1.8%**（真机，L=0.5 m） | — |
| 稳定性指示 | GUI 主循环心跳 `[phywear] alive t=…s fps=…`（每 30 s，最新固件 63–69 loops/s） | 空闲堆 94.82 MB 恒定（±90 B，无泄漏） |

> **口径说明（如实）**：41 FPS 是本队 PhyWear 固件（2026-09-13）的全场景平均；43 FPS 是 2026-09-12
> 旧固件 + 官方 benchmark 的数字，两者都真机实测、**不可混用**。引用 43 FPS 时必须注明"9/12 旧固件口径"。

> **数据来源声明**：**模拟器没有真实传感器**，模拟器截图中的数值是占位符，仅证明中文界面与字体渲染；
> 用 `phywear <实验>bench` 打开的"说明/数据页"是**注入的合成信号**，不是测量结果；
> 真机 26 张截图（`evidence/realboard-20260912/`）中 16 张主页面数值来自板上实时传感器，但板子静止置于桌面、
> **非受控实验**，同样不可当作测量结论。**可引用的实测数据**：真机帧率（41/43 FPS）、摆测 g 误差 1.8%、
> 声学通路电平（最高 −8.4 dBFS，含弱耦合说明）、启动稳定性（5/5、6/6）、Skill 摘要字节数与 md5。

---


### 5.1 SRAM 已用满时的可选腾挪（2026-09-15 实测）

本板 SRAM 已到 **93.76%**，而"新增不超过 ~2 KB"的额度也已被逼近/超出。为了给 ③ 的 UI 留空间，
我把 SRAM 占用按符号拆开查了一遍（`nuttx.map`，脚本见下方），找到的最大一块**不是我们的代码**：

| 项 | 大小 | 归属 | 用途 | 能否腾 |
|---|---|---|---|---|
| `g_allsyms` | **45,768 B**（`.data`） | `allsyms_final.c.o`（`CONFIG_ALLSYMS`） | **仅**用于 panic 回溯/`%pB` 的符号名解析 | ✅✅ **已解决：改成 `const` 放 `.rodata`（flash）** —— 见下方「实施结果」 |
| `image_stage_data` | 30,000 B | LVGL `lv_draw_sifli_epic_img.c` | EPIC 图像搬运暂存（`pw_scope` 曲线就靠它） | ❌ 要用 |
| `s_tls_raw_buf` | 16,384 B | ai_agent `vela_tls.c` | TLS 收发缓冲；截至 09-18 真机 BT/PPP 已通到 IP 层，该缓冲**可能在真机被用到**（尚未做真机 LLM 联调验证） | ⚠️ 可移到 PSRAM（属官方代码，需小改） |
| `g_lp/hp_work_stack` | 16,096 ×2 | 系统工作队列 | 系统 | ❌ |
| `spec_work` | 4,096 B | 本队 `phywear_spec.c` | 频谱页工作区 | ⚠️ 可移 PSRAM |
| `g_spk_buf` / `g_mic_buf` / `g_mic_data` | 3,200 / 2,048 / 2,048 | 扬声器/麦克风驱动与本队页 | 音频 | ⚠️ 移 PSRAM 要评估延迟 |

**实测（只改 defconfig 第 8 行 `CONFIG_ALLSYMS=y` → 关，其余不动）**：

| | 现在 | 关掉 ALLSYMS | 差 |
|---|---|---|---|
| SRAM | 491,576 B（93.76%） | **445,824 B（85.03%）** | **−45,752 B** |
| flash | 2,076,244 B（12.4%） | 1,929,672 B（11.50%） | −146,572 B |

也就是说：**关这一个开关，就把"超额度 88 B"翻转成"比基线低 43.6 KB"**，③ 的 UI 与后续功能都有余量。
代价只有一条：panic 时的回溯从"函数名+偏移"变成裸地址（项目仍保留 `.map`，可事后反查）。

**实施结果（2026-09-15，已上板验证）**：`g_allsyms` 是一张**只读**符号表，但 NuttX 的生成器
`nuttx/tools/mkallsyms.py` 默认把它生成成**非 const**，于是落进 `.data`：既占 flash 镜像、
又在 SRAM 里复制一份。而消费方 `libs/libc/symtab/symtab_allsyms.c:34` 本来就按
`extern const struct symtab_t g_allsyms[]` 使用 —— 说明生成 `const` 反而更自洽。

改法（本队改动，生成器逻辑原本是反的：`--noconst` 名字表示"不要 const"，默认分支却生成了非 const）：

```python
def print_symbol_tables(self, isnoconst=False):
    noconst = ""
    if not isnoconst:
        noconst = "const"        # ← 默认 const；并把两行 extern 也带上 const 前缀
```

**实测结果**：`g_allsyms` 落到 **`0x12116b1c`（flash 段）**，SRAM **492,560 → 446,608 B**
（93.95% → **85.18%**，**−45,952 B**），**flash 基本不变（−16 B）**，
且**符号表内容一项没少（5742 项）→ panic 回溯的符号名解析能力完全保留**。

也就是说：这件事**不需要牺牲可调试性**，也不需要用"关掉 CONFIG_ALLSYMS"那种有代价的办法；
它也把"SRAM 累计新增超 ~2 KB 额度"这件事直接翻转成**比最初基线低约 43 KB**。

> 复现：改 `nuttx/tools/mkallsyms.py` 两处 → `rm cmake_out/<board>/allsyms_*.c` → 重新 build →
> 看构建末尾 `Memory region`，并 `grep -n g_allsyms nuttx.map` 确认落在 `0x1201xxxx` 段。

## 6、关键问题与修复（现象 → 定位 → 修复 → 验证）

### 6.1 组页点击卡死（悬空指针）

- **现象**：从组页（力学/计时器等）点入实验页即整机卡死，随后被看门狗复位。
- **定位**：实验列表的事件回调把 `&items[i]` 存进 `user_data`，而各 `ui_*_open()` 的实验表是**栈上局部变量**
  （条目用运行期 `PW_STR()` 求值，无法静态初始化）；点击时栈帧早已销毁 → `it->open` 变成垃圾值 → 跳非法地址。
- **修复**：回调只携带**函数指针本身**（`it->open`），不再引用表项地址（2 文件 +14/−5）。
- **验证**：修复固件 md5 `784855320611bafb3580f4808d52847a`（1,766,436 B，读回逐字节一致）；
  12/12 页面点击 + 26/26 截图校验通过；**用户手指实测确认不再卡死**。

### 6.2 真机启动偶发卡死（时钟/晶振建立时序）

- **现象**：镜像微变即"随机"启动失败，只打印 `AB`（或 `ABCD`）后无输出；同一源码重编有时好有时坏。
- **定位**：在 `sifli_start` → `HAL_PreInit` → `BSP_Board_PreInit` → PSRAM 各阶段用 `arm_lowputc` 插桩，
  确认卡点在 `BSP_Board_PreInit` 的**时钟/晶振建立窗口**内。
- **修复**：XTAL32 起振、DLL1 锁定（切 240 MHz 系统时钟前）、DLL2 锁定（切 FLASH2 XIP 时钟前）三处各补 2 ms 等待；
  PSRAM 通路在 1.8 V LDO 使能后同样补 2 ms。
- **排除项（均做过对照实验）**：ftab 分区窗口、`.data/.ramfunc` 加载地址越界、
  flash 烧写不完整（前 512 KB 逐块读回**逐字节一致**）、扬声器驱动本身。
- **验证**：连续 **5/5、6/6** 复位启动正常（含 2.03 MB 全功能镜像）；修复前镜像稳定复现卡死。

### 6.3 10 条 i18n 字符串双重转义

- **现象**：界面显示 `\xe2\x86\x92` 之类字面文本（箭头、±、°）。
- **定位**：生成 i18n 表时把 `\xNN` 又转义了一次，C 字符串里成了字面反斜杠。
- **修复**：改用 ASCII `->` / `+-` 与真实 `°`（字体子集含 0xB0，不含 0xB1/0x2192）。
- **验证**：真机 26 张中文截图逐张目检，无乱码、无缺字方框。

### 6.4 CJK 缺字方框（新增文案）

- **现象**：新增声学页面后按钮/标题出现方框（"播/扬/声/器/频/率/音/量"等）。
- **定位**：CJK 字体子集是按**旧字符串表**生成的静态子集，新字不在其中。
- **修复**：新增 `tools/phywear/gen_font_ranges.py`（从中文表 + 应用字面量收集码点）
  + `gen_fonts.sh`（lv_font_conv 重生成 5 档字号），本次 **534 个码点**。
- **验证**：真机截图中英文案恢复正常（`evidence/acoustic-20260913/tone.png`、`spk.png`、`mic.png`）。

### 6.5 扬声器：第二段声音永远失败（两层原因）

- **现象**：`phywear tone` 只有第一段响，其后全部 `failed`。
- **定位（两层）**：① 驱动反复 `Transmit_DMA/DMAStop`，第二次返回 `HAL_BUSY`（AUDCODEC 状态机不接受）；
  ② 应用侧 `pw_tone.c` 把 fd 缓存进**应用全局变量**（同一份 `.data` 跨多次运行存活），
  第二次运行时 fd 属于**上一个已退出的任务** → `ioctl` 返回 `EBADF`，把真实原因掩盖成 errno=9。
- **修复**：驱动改为 **DMA 常驻循环**（换频率只重填缓冲 + 静音/取消静音切换）；应用每次调用 open/close。
- **验证**：连续 5 段不同频率全部 ok；用户听感确认可出声；默认音量由 0 → **+12 dB**（用户反馈偏小）。
- **附带**：麦克风增益 0 → **+30 dB**——0 dB 时静音与大声都只有 15~40 LSB，+30 dB 后环境噪声约 570 LSB、
  1 kHz 正弦下峰值最高 12,000 LSB（−8.4 dBFS）；常规音量下同板声耦合弱（如实记录，不作闭环标定用途）。

### 6.6 跨任务 fd 导致 Agent 采样为 0

- **现象**：真机让 Agent 跑 `phywear_run_experiment`，返回 `{"samples":0,"error":"no accelerometer samples available"}`。
- **定位**：`pw_sensors_read_imu()` 用的是 PhyWear 任务里打开的 `g_imu_fd`；NuttX 的文件描述符属于任务/任务组，
  **Agent 任务里该 fd 无效** → 每次读取失败（同一进程内读则正常）。
- **处理**：如实记录为架构约束；因此"Agent 长时间采样并自动切页"的主动场景被关闭（见 7.2）。
  后续方向：**由 GUI 线程采样、Agent 只取结果**。

### 6.7 启动期 Agent loop 不启动（真机无网络）

- **现象**：真机重启后 Agent 各线程在跑、工具已注册，但事件进队列无人处理（日志里没有 `Agent loop started`）。
- **定位**：官方 `agent_main.c` 只在 `network_wait_connected()` 成功后才 `agent_loop_start()`；
  真机没有网络栈 → 该等待永远超时 → Agent "活着但聋"。
- **修复**：Agent loop 改为**开机无条件启动**（网络相关通道仍按连通性启动），并加幂等保护避免重复建线程。
- **验证**：真机日志出现 `Agent loop started, free heap: …` + `Buffers/Tools JSON loaded`；
  摆动事件可被 `Processing message from local_client:phywear` 消费。

### 6.8 中文技能摘要被按字节截断 → 云端 400（2026-09-13，LLM 链路）

- **现象**：模拟器上配好 LLM 后端后，每次调用都稳定返回
  `400 {"message":"Invalid JSON in request body"}`；同样的内容从电脑用 curl 发同一端点却是 200。
- **定位**：本地起代理抓设备真实请求体，发现**请求体不是合法 UTF-8**——第 3736 字节是 `0xe8`
  （三字节汉字的第一个字节），紧跟 JSON 的 `\n` 转义，即某个汉字被"按字节截断"切掉了一半。
  根因在 `packages/ai_agent/src/tools/skill_loader.c`：组装技能摘要/标题/描述时用
  `snprintf`/`memcpy` **按固定字节数**截断（`desc[256]`、`title[64]`、摘要缓冲），
  没考虑 UTF-8 字符边界；摘要进入 system prompt 后被 cJSON 原样写入请求体，网关解析失败。
  **任何写中文 Skill 的队伍都会踩到**。
- **修复**：新增 `utf8_safe_len()`，在三处截断点回退到最后一个完整字符（+38 行，1 个文件）。
- **验证（两轮）**：
  ① 修复后同一问题一次通过 —— `trace … llm=ok backend=0`，LLM **自主发起 4 次工具调用**
  （`phywear_list_experiments` → `phywear_open_screen{pendulum}` → `launch_quickapp` →
  `phywear_run_experiment{pendulum,5s}`），5 轮迭代、20 s 完成、中文作答；
  ② **严格复验**：只放 1 个中文 Skill 时摘要 1022 B 未触上限，不足以证明修复生效，于是放入
  **2 个中文 Skill 强制命中 `skills_buf[1024]` 上限**再抓包 —— 请求体 17,654 B 经 UTF-8 解码与
  JSON 解析**双双通过**（tools=40），同会话切回真实 MiMo 端点 `llm=ok`、2 次工具调用。
  证据 `docs/evidence/llm-20260913/`（含 `strict-utf8-verification.txt`）。
  该修复已编入真机固件 `075655ee…` 并完成启动复验。

### 6.10 曲线在多次进出页面后静默消失（`pw_scope` 槽位泄漏，2026-09-14）

- **现象（代码级推断 + 压测复现）**：带曲线的页面（单摆/弹簧/频谱/倾角/标尺/声学等）反复进出若干次后，
  曲线不再绘制，且**没有任何报错**。
- **定位**：`pw_scope.c` 只有 6 个静态槽位（`PW_SCOPE_MAX 6`），分配时 `g_nscope++`，
  **却没有任何释放路径**；而每次进入带图页面都会 `pw_scope_create()` 一次
  （6 个调用点：`phywear_pend.c:752`、`phywear_spring.c:503`、`phywear_spec.c:843/861/880` 等）。
  于是同一个 boot 里进出 6 次之后 `create` 返回 `NULL`，页面静默无图（每块缓冲 354×96×2 ≈ 68KB，
  同时还会持续吃堆）。
- **修复**：槽位加 `used` 标记 + 空闲槽优先复用；把内部结构绑到 LVGL 对象的 `LV_EVENT_DELETE`
  上（`pw_scope_del_cb`），页面被销毁时自动 `lv_free` 缓冲并归还槽位（`+63 行，1 个文件`，
  调用点无需改动）。
- **验证**：模拟器 `phywear demo` 自动巡页压测 —— 日志显示 `slot 0` 被 **acquire → release →
  acquire** 复用，不再单调递增，也未出现 `out of slots` 告警。

### 6.9 ROM 只打印 `SFBL` 不启动（ftab 缺失，2026-09-12）

- **现象**：上电只打印 `SFBL` 便停在串口下载模式，`sftool` 仍能秒连（误导为"硬件/供电问题"）。
- **定位**：flash 起始 `0x12000000` **没有 FlashTable**，ROM 读不到二级 bootloader 的 base/size/xip_base。
- **修复**：先写修正过 `xip_base`（`0x20020000` → `0x12010000`，与链接脚本 `flash ORIGIN` 对齐）的 ftab，再写固件；
  固化为 `flash_with_ftab.sh`（写 ftab → 写固件 → 读回校验）。
- **验证**：启动链 `SFBL → ABCD → NSH`；之后 26 张真机中文截图、43 FPS、PhyWear 应用全部复现。

### 6.11 迷你曲线自动量程抖动 + 根屏重建泄漏 scope 槽位（2026-09-15）

**现象 A：数值标注与曲线量程不一致。** 三轴页数值写 `+/-50 dps`，但曲线像素实测幅度达到
±fs（即按 20 dps 在画）——同一帧里两者对不上。

- **定位**：`raw_axis_curve_update()` 最初用**当前瞬时样本**求峰值 → 正弦过零点时峰值掉到 0，
  自动量程便在 20↔50 dps 两档之间反复跳（`raw_nice_fs()` 的 1-2-5 步进 + 回落阈值半量程）。
- **修复**：改用**整条 80 点窗口**（3 轴共 240 个样本）的峰值。
- **验证**：像素级复算（`docs/evidence/ui-batch2-20260915/verify_curve_scale.py`）——
  陀螺 X 幅度 ±19.13 dps（合成波形 ±20 dps，100 ms 采样最大值理论 ≈19.4），
  与 `+/-50 dps` 自洽；加速度 X/Y/Z 分别为 ±0.62 g / ±0.11 g / +0.69..+1.28 g
  （合成波形 ±0.6 / ±0.12 / 0.7..1.3 g）。

**现象 B：`phywear cap root` 连开几次后，原始传感器页曲线静默消失。**

- **定位**：`pw_ui_root()` 只把 `g_scr_n = 0`，**不删除仍在栈里的旧屏** → 旧屏的
  `pw_scope` 槽位不归还；原始传感器一屏占 3 槽，第 3 次起 `pw_scope: out of slots (6 in use)`。
  （正常导航路径由 `pw_scr_back()` 删屏，故只在反复重建根屏时暴露。）
- **修复**：先遍历栈删屏再重置计数器。
- **验证**：`phywear cap root` 后日志出现 `pw_scope: release slot 0/1/2`，再开页槽位正常。

---

## 7、已知限制（未实现的如实标注）

1. **真机无 WiFi 网卡**（BLE 协议栈已打通；网络改由 PPP over BLE 承载，见第 8 条）：
   镜像内没有网卡驱动，**未配置 LLM Key** →
   真机的 LLM 对话不可用；LLM 路径仅在 goldfish 模拟器（有 virtio-net）验证。真机的 AI 能力表现为
   工具注册 + Skill 装机 + 离线关键词直通。
2. **主动场景已交付，但有两个残余问题**（2026-09-15 起 `PW_WATCH_PROACTIVE 1`）：
   原先"必卡"的真因已定位并修掉（Agent 侧 50Hz 采样抢 I2C → 改成"开页等结果"；另外修掉
   Agent 不在时 `velaclaw_ask` 直接 panic 的断言、以及工具里的 cJSON 双重释放，见 `docs/evidence/proactive-20260915/`）。
   残余：① **"真实晃表 → 可信 g"仍需用户挥手确认**（机器验不了）；② 结果上报的三道有效性门
   在静止/振动桌面上仍会偶尔漏过无效值（实测 70s 内 1 条），根治需改单摆页的运动判据与周期估计。
   后续方向（已讨论、暂搁置）：无障碍播报、首次开机语音引导、结果语音播报。
3. **`Ctrl-C` 杀不掉 `phywear`**（固件未开 `CONFIG_TTY_SIGINT`）→ 退出 GUI 只能复位板子。
4. **同一次开机内第二次启动 GUI**（再敲一次 `phywear`）会挂在 LCD 驱动 → 需复位后再启动。
5. **模拟器没有真实传感器**：模拟器数值是占位符；bench 页面是注入的合成信号，不能作为测量结果。
6. **声学未做完的部分**：多普勒、历史频率、声呐未实现；麦克风为 16 kHz（碰撞实验需 48 kHz 分频改造）。
7. **蓝牙（④）：阶段 A + 阶段 B 的 B1/B2/B3 全部打通**（2026-09-18 晚更新；
   此前"no-go"的结论已被推翻 —— 旧结论的三个断点①`UART_BTH4` ②`CONFIG_NET_BLUETOOTH`
   ③H4 驱动未接 CMakeLists 都已解决，真正的拦路虎是 **NuttX fd 表按 task_group** 导致
   H4 TX 跨组 `write()` 恒 `EBADF`，详见 §4.10b）。当前状态：
   **B1 ✅**（`bt_enable rc=0`）、**B2 ✅**（GATT 服务 + 可发现广播 + 服务端自检 PASS）、
   **B3 ✅ 通过**（连接 / 读 `…a001` 16 B / 订阅 Notify / 写 `…a002` 'ping'，
   设备侧 6/6 + 宿主侧 9/9，见 §4.10c）。
   另有**连接标识 + 手表版蓝牙串口**（§4.10d）：主界面灰/蓝状态点、文本特征
   `e0f1a003`（手机写文本→设备 `[RX]` 并回 `echo:`，手表可主动 notify `[TX]`）、
   手表「蓝牙」页（状态/对端/MTU/计数/10 行收发日志/发送测试）。
   残余：① 本队**没有真机手机实测**，B3/文本串口都由宿主 BlueZ 中心设备
   （Intel AX201）完成，等价于手机上用 nRF Connect 的那几下，但没在 Android/iOS 上点过；
   ② `cmd_count` 因自检会先写一条而从 1 起算；
   ③ 蓝牙页日志为 10 行环形缓冲、文本单条 ≤35 字节（够做连通性测试，不是通用串口终端）。
   **2026-09-18 深夜修掉两个真问题**（用户报"断开很久了还显示已连接"）：
   ① 桌面测试工具测完从不断开（BlueZ 是中心设备、进程退出不断链路）⇒ 手表显示"已连接"
   其实是对的，工具已改为收尾显式 `Disconnect()`；
   ③ **链路层优化尝试（2026-09-18 深夜，详见 docs/16 §13.6d）**：修掉"页面显示 MTU 23 其实只是连接快照"的显示 bug（现按 `att_mtu_updated` 显示真实值 517）；实测**连接参数请求对 BlueZ 是 no-op**（开关对照三次，`itv/lat/sto` 完全一致）；往返延迟 135→90 ms 差异真实但**归因不明**（关掉 MTU 交换后 MTU 仍 517 ⇒ BlueZ 自己会交换），**不记为本次收益**。新增 `pw_ble_central.py --perf` 作为可复现的往返延迟/吞吐尺子（同配置抖动 ±1 ms）。
   ② 断开后手表**再也不会广播**（`bt_le_adv_start` 回 -114 `-EALREADY`，内置
   `bt_le_adv_resume()` 因 `!BT_ADV_ENABLED` 永远为假而不生效，且静默无日志）
   ⇒ 修好后断开即刻重新可被发现，不用复位板子。证据
   `docs/evidence/bt-link-lost-20260918/`，详见 `docs/16 §13.6c`。
8. **真机没有网络栈：WiFi 路径不可达，但 USB-SLIP 是可行替代**（2026-09-18 两次修正）：
   SF32LB52 黄山派**没有无线网卡** —— 运行时 `ifconfig` 直接
   `ifconfig: open failed: 2`（ENOENT：命令在、网卡不在），`/dev` 无任何 wlan/eth，
   defconfig 无 `CONFIG_WIFI`/`CONFIG_DRIVERS_IEEE80211`，厂商树无任何 WiFi 芯片驱动、
   无 `netdev_register`。ai_agent 的 `set_wifi` 实现走 `ifup wlan0` + `wapi`，**需要真实网卡**。
   ⇒ **WiFi 这条路走不通**（不是软件没写完）。
   **但"拿到 IP + 数据发宿主"本身能做，只是要换链路**：SoC USB 设备口 → CDC-ACM
   （`/dev/ttyACM0`，板级已 `cdcacm_initialize`；pinmux `PAD_PA35/36 = USB_DP/DM`）
   → **SLIP**（`nuttx/drivers/net/slip.c` 本来就在树里，`CONFIG_NET_SLIP` 一开即可）。
   **软件侧已就绪并实测**：新增 `phywear net`（`pw_net.c`）调
   `slip_initialize(0, "/dev/ttyACM0")`；当前实测 `rc=-107 = -ENOTCONN`，
   根因是 `cdcacm.c:2140` 在 USB 未被主机枚举前拒绝 `open`（`SETUPNOTCONNECTED`）
   ⇒ **只差一根 USB 数据线**（宿主 `lsusb` 里目前只有 CH340，没有 `38F4`）。
   插上线后 `phywear net` 应返回 rc=0、`ifconfig` 出现 `sl0`，再配宿主 `slattach`
   即可验到 TCP/UDP，那时 N1（拿 IP）+ N2（测量数据发宿主）才算**实测通过**。
   **在此之前不写成已实现。**
   另：测量数据"发宿主"也已有 **BLE** 承载路径，且**已于 2026-09-18 晚实测通过** ——
   B3 的 sensor 特征 + Notify 把测量数据实时送到宿主（16 B/包，500 ms 一包，
   宿主解出 `ax/ay/az`），详见 §4.10c。对一块**没有网卡**的板子，这是唯一物理可行的
   端到端通路；**N2 据此记为"以 BLE 承载部分通过"**，而"WiFi/DHCP 拿到 IP"单列为已知限制。
   **【2026-09-18 深夜第三次修正 —— 任务 3 已达成，不再是限制】**：第三条路 **PPP over BLE**
   打通了。设备侧 `apps/examples/phywear/pw_btppp.c` 用第二个 GATT 服务做 BLE↔pty 字节管道，
   pty 另一头跑**真正的 NuttX pppd**；宿主侧 `tools/phywear/pw_bt_ppp.py --peer` 自己当 PPP
   对端（LCP+IPCP+ICMP，**不需要 root**）。实测：设备 `ifconfig` →
   `ppp0 … at RUNNING mtu 296 / inet addr:192.168.7.2 DRaddr:192.168.7.1`；
   `ping -c 3 192.168.7.1` → **3 sent / 3 received，0% packet loss**；
   一键判定 `python3 tools/phywear/pw_bt_ppp_e2e.py --out <dir>` **6/6**。
   上一轮记的"装不进 SRAM"也查清了：`g_hp_work_stack`/`g_lp_work_stack` 各 **16,096 B**，
   只是继承了板级 `CONFIG_DEFAULT_TASK_STACKSIZE=16096`（NuttX 默认 2 KB）；
   显式改 8192 ⇒ 一次省 **15,808 B**（SRAM 496,452→480,644），最终出货 483,316 B（92.19%）。
   证据：`docs/evidence/bt-ppp-e2e-20260918/`、`docs/evidence/ws-stack-20260918/`；
   根因与 7 个坑见 `docs/16 §13.6e`。⚠️ 残余：hpwork 上的触摸屏 worker 未实测（需人工点屏）。
8. **固件不是可重现构建**：镜像里嵌了构建时间（`strings nuttx.bin | grep "Sep 15 2026"` 可见），
   每次重编 md5 都会变。因此 md5 只能核对"板上固件 vs 同一个产物"，**不能**用来判断两次构建是否一致；
   跨构建可比的是大小 + defconfig + `manifest.json` 里的源文件 md5。
9. **端侧运动识别 / 手势启动 / 自定义实验构建器**未实现（界面占位标注"规划中"）。
10. **演示视频尚未录制**（脚本已重写为 **v4**，含主动场景/惯性标尺；分镜表见 `docs/evidence/demo-20260915/`）。本机不具备自动录屏条件（真机无视频输出、模拟器取帧仅 0.3 fps、无 ffmpeg），需人实拍；提交用材料仍在整理。

---

## 8、附录：开发过程与优化记录

### 8.1 板块与功能清单（现状）

| 板块 | 内容 | 状态 |
|---|---|---|
| 原始传感器 | 加速度 / 陀螺 / 磁力 / 环境光 / **麦克风** / **扬声器**（6 页横滑）；**三轴页 = 一行三列数值 + 358×186 三泳道迷你实时曲线**（2026-09-15） | ✅ 真机 |
| 力学 | 摆测 g、弹簧振子、向心加速度 | ✅（弹性碰撞未做：需 48 kHz 麦克风） |
| 声学 | 声音频谱、**音频发生器（新）**；历史频率 / 多普勒未做 | ✅ 部分 |
| 工具 | 加速度频谱、斜面倾角（**指针式角度盘**，2026-09-15）、磁性标尺、磁场频谱 | ✅ 真机 |
| 计时器 | 运动秒表（**环形计时进度**，2026-09-15）、光学快门、声学快门 | ✅ 真机 |
| 生活 | 掌声计 | ✅ 真机 |
| AI 教练 / 自定义 | 语言交互 Agent + Skill 已落地；端侧运动识别、实验构建器为占位 | ⚠️ 部分 |

### 8.2 性能优化过程（关键结论）

- **摸清 EPIC 边界**：官方 EPIC draw unit 只卸载 `FILL/BORDER/IMAGE/LABEL/LAYER`，**不含 LINE** →
  `lv_chart` 折线 100% 落回软件渲染，是性能黑洞。
- **瓶颈实测**（`phywear pendbench`，avg loops/2s，越大越空闲；lv_chart 灾难线 ≈2–4）：

  | 场景 | 结果 |
  |---|---|
  | 空闲主菜单 | 168 loops/2s，空闲堆 7.7 MB 恒定 |
  | `lv_chart` 300 点（各种刷新策略） | **2 loops/2s（每帧 ~1 s）** |
  | `lv_chart` 96 点 + 240 ms 批量 | 85–100 loops/2s（≈4 fps，勉强可用） |
  | 自研 `pw_scope` 300 点 @25 fps | ~90 loops/2s，流畅 |
  | 自研 `pw_scope` 自相关 251 点 @10 Hz | ~160 loops/2s |

- **解法**：CPU 把曲线光栅化进小块 RGB565 缓冲 → 挂 `lv_image` → 交 EPIC 的 **IMAGE 硬件 blit**；
  缩放/平移只重画视图窗口。`lv_chart` 仅保留给"低频/静态结果图"（重绘间隔 ≳几百 ms）。
- **单点触摸的交互替代**（FT6146 不支持捏合）：图上拖动 = 平移；图下 `X- X+ Y- Y+ / Fit / Auto` 独立缩放；
  图对象声明可滚动 + 关闭滚动链，避免横向拖动被外层翻页容器抢走。
- **12 项 bench 回归**（2026-09-04 上板）：pendbench 84、specbench 145、micspecbench 140、magspecbench 145、
  springbench 97、centribench 156、inclinebench 56、rulerbench 100、timebench 141/140/153、applausebench 149
  （loops/2s），全程无崩溃、空闲堆稳定；大号数值标签节流后 timebench 由 ~56 提升到 ~141。

### 8.3 中文 UI 验证（模拟器，2026-09-04）

| 指标 | 无中文（EN） | 有中文（ZH） | 增量 |
|---|---|---|---|
| 平均 FPS（实时摆页，模拟器） | 23 | 22 | −1（噪声内） |
| 运行空闲堆 | 94.82 MB | 94.82 MB（±90 B 恒定） | 0 |
| 存储 | — | 字体 +298 KB | 16 MB 分区用约 42%，≈9 MB 余量 |

结论：中文显示/切换正常、无额外卡顿、无泄漏；模拟器 FPS 受软件渲染限制，43 FPS（9/12 旧固件口径）是板上 GPU 基线。

### 8.4 板级 / 工具链 / 烧录要点

- 工具链：`arm-none-eabi-gcc ≥ 10.3`、`cmake ≥ 3.22`、`ninja ≥ 1.10`、`python3 ≥ 3.10`。
- 构建：`cmake -B cmake_out/<cfg> -S nuttx -GNinja -DBOARD_CONFIG=../vendor/sifli/boards/sf32lb52/sf32lb52_lchspi_ulp/configs/nsh -DEXTRA_FLAGS="-Wno-cpp -Wno-deprecated-declarations"` + `cmake --build`。
- 烧录：`flash_with_ftab.sh`（先 ftab @0x12000000，再固件 @0x12010000 + 读回校验；**禁止 erase_flash**）。
- 串口：`picocom -b 1000000 --noreset --lower-rts --lower-dtr /dev/ttyUSB0`
  （CH340N 的 RTS 直连复位，普通 `open()` 会让 SoC 一直处于复位态；脚本需"脉冲复位→等 `nsh>`→发命令"）。
- 真机截图：板级无 `/dev/fb0`、NSH 无 `dd/cat` → 本队新增 `phywear --shot/--sweep/--p2`（串口流式 base64 + 逐行 crc16 +
  整帧两遍），主机侧 `tools/phywear/pwshot.py` 校验后存 PNG；本日新增页面名 `tone/mic/spk` 可直接截取。

### 8.5 里程碑与后续

| 日期 | 目标 | 状态 |
|---|---|---|
| 9/3–9/6 | 驱动 bringup + 主菜单/Raw/Pendulum + 弹簧/向心 | ✅ |
| 9/4–9/8 | 频谱页 ×3（accel/mic/mag）+ 秒表 ×3 + 掌声计 + 图表控件 | ✅ |
| 9/10–9/12 | 真机链路（ftab 修复、43 FPS 复现、全中文截图 26 张、AI Agent 工具） | ✅ |
| 9/13 | 声学（扬声器驱动 + 音频发生器 + 麦克风/扬声器页）、启动时序修复、Skill 装机、Agent loop 修复 | ✅ |
| 9/14–9/20 | 演示视频、材料打包、专属仓同步；（可选）安全版主动场景 | ⏳ |
| **9/15** | UI 第二批 (a)(b)(c) + 修订（双视图/每轴量程）· **主动 + 执行场景交付**（修 2 个真崩溃 + Agent 开机自启）· **惯性标尺算法核心**（Mahony MARG + 六面法/磁椭球/零偏，主机单测 + 真机验证）· **蓝牙时间盒探针（no-go，含三条断点）** · 视频脚本 v4 + 分镜表 | ✅ |
| 9/16–9/17 | UI 全面改版 v2 → v3~v8 原型迭代（用户逐轮反馈）· P0 帧率规划结案（面板 60 Hz 为物理上限）· 动效查表 + LVGL 版 UI Skill · UI v2 改版与 BUG 修复 | ✅ |
| **9/18** | **蓝牙阶段 B 打通：B1 `bt_enable rc=0` + B2 GATT 服务/可发现广播 + 服务端自检 PASS**；根因 = NuttX fd 表按 `task_group` 致 H4 TX 跨组 `write()` 恒 `EBADF`（§4.10b）· UI 干净回退到"重置前设计"+ 时间左移 + 语音助手页 · **任务 3 实测判定：本板无无线网卡，N1/N2 物理不可达** | ✅ / B3 待人工 |

### 8.6 AI 辅助开发

| 项目 | 内容 |
|---|---|
| 使用工具 | **Claude Code、Codex**（官方支持工具）；DeepSeek Harness 不在官方工具枚举内，**不计入** `logs/`，仅作补充佐证 |
| 日志 | 日志采集器已安装并通过自检（`TEAM_ID=contest2026_427_xinpingqihe`、`GITHUB_LOGIN=XPQHyue`）；`logs/XPQHyue/` 现有 **43 个会话**（2026-08-26 ~ 09-07，可复核） |
| 沉淀 Skill | 开发流程 Skill `phywear-sf32lb52-devloop`（编译/烧录/截图/bench 流程）；产品侧 Skill `phywear-physics-coach`（见 4.6） |
| 说明 | 本报告所有代码、文档、证据均由 AI 辅助完成 + 人工复核；凡未实测项均已显式标注 |

### 8.7 参考与许可

- 玩法与实验定义参考 **phyphox**（RWTH Aachen，GPL v3），本作以 C + LVGL 独立重实现，未复制其代码体；
  源码许可 Apache-2.0，详见 `app/phywear/NOTICE.md`。
- 板级适配参考立创·黄山派 Wiki 与思澈 SF32LB52 参考设计；官方 EPIC/AI Agent 成果归属见第 1 节。


---

### 4.13 2026-09-18 收尾更新汇总（本节补齐当日"尚未进正文"的成果与口径）

> 蓝牙主体见 §4.10 / §4.10b / §4.10c / §4.10d；本节只补**其余**并统一最新数字与限制口径。

#### （1）CJK 字体流水线 bug（真 bug，影响构建成功率）

`gen_font_ranges.py` 原逻辑把「码点 ≥ 0x2E80」一律当作需要渲染的汉字，而 **U+FE0F（变体选择符）** 恰落在此区间、字体里没有该字形
⇒ `lv_font_conv` 退出码 1，**五个字号一个都不生成**；触发条件只是源码里出现一个 `⚠️`（`pw_skill_blob.c` 中即有一个）。
该脚本还误扫 `*_blob.c`（内嵌 markdown 的数据块，**从不参与渲染**，却贡献 565/725 个"汉字"）与 `*_test_host.c`。
**修复后**：子集 **725 → 508** 码点、5 个字号源文件合计 −385 KB。
**结果**：这一版**新增了整条蓝牙功能，flash 反而下降** —— `2,609,752 → 2,554,544 B（−55,208）`；SRAM `495,688 → 496,372 B（+684，如实报数）`。

#### （2）蓝牙/网络之后的资源与性能口径（最新）

| 项 | 值 | 依据 |
|---|---|---|
| flash | **2,554,544 B / 16 MB（≈15.2%）** | 含蓝牙 host 栈 + BT UI 的出货构建 |
| SRAM | **496,372 B / 512 KB（≈94.7%）** | 同上（+684 B vs 蓝牙 UI 前） |
| 页面帧率 | **12 不变**（`cap raw` 30 s 心跳，`fps=` 字段） | 蓝牙链路活动期间复测 |
| 一键设备侧验收 | **18/18** | `tools/pw_accept.py` |
| 蓝牙 B3（宿主中心设备自动跑） | **设备 6/6 + 宿主 9/9，退出码 0** | `tools/pw_bt_b3.py --central`（含文本回环时 8/8 + 13/13） |
| 桌面 GUI 版测试 | **14/14** | `tools/pw_bt_gui.py`（双击桌面图标即自动跑完） |

#### （3）任务3「网络」结论（如实）

本板（SF32LB52 黄山派）**没有无线网卡**：运行期 `ifconfig` 报 `open failed: 2`、`/dev` 无 wlan/eth、defconfig 无 `CONFIG_WIFI`、厂商树无网卡驱动
⇒ **WiFi 路线在此硬件上不可达**（非软件未做）。
替代路线 **USB CDC-ACM + SLIP** 软件侧已就绪（`CONFIG_NET_SLIP` + `NETUTILS_DHCPC` 已开、新增 `phywear net`），
实测 `slip_initialize rc=-107=-ENOTCONN`（USB 未被主机枚举前拒绝 open）⇒ **只差一根 USB 数据线**；
插线后 `phywear net` 应 `rc=0`、`ifconfig` 出现 `sl0`，再经宿主 `slattach` 验到 TCP 才算通过。**在此之前不写成"已实现"**。

#### （4）AI-Native 台账（口径统一，数字由文件逐行解析）

| 平台 | 区间 | Tokens | 依据文件 |
|---|---|---|---|
| 小米 MiMo（Claude Code） | 2026-08 ~ 2026-09 | **451,358,504**（5,398 请求） | `docs/token_usage/token_plan_usage_data_*.xlsx` |
| DeepSeek Harness（DSH） | 2026-08-20 ~ 2026-09-18 | **4,197,874,171**（成本 327.98 CNY） | `docs/token_usage/dsh/amount|cost-2026-08-20_2026-09-18.csv` |
| **合计** | | **4,649,232,675（约 46.5 亿）** | 同上 |

**AI Coding 日志**：`logs/XPQHyue/` **54 个会话 / 15,761 事件**，`validate-log.py` ✅ ALL OK。
采集要件（本轮踩坑总结）：**① 会话需在 `~/openvela`（含 `.repo/`）内发起；② 必须经 hook / 等价载荷转换**
（直接把 `~/.claude/projects/` 的原始 transcript 拷进 `logs/` 会因缺少 `tool_call_id` 等字段被校验器判 ❌）。

#### （5）已知限制（对齐组委会"实话实说"要求）

- 蓝牙**未在真手机（Android/iOS）上点过**：B3 由宿主 BlueZ（Intel AX201 / hci0）作中心设备自动验收，动作与手机 nRF Connect 同一套；手机路径脚本保留可交叉验证。
- 本板广播为**随机地址**（EXT_ADV + PRIVACY）⇒ **按设备名 `PhyWear` 扫描，不能按 MAC**。
- 网络：如上，**硬件不可达**，替代路线待插线实测。
- 演示视频**待人工拍摄**（真机无视频输出、模拟器 0.3 fps、无 ffmpeg）；报告需导出 `.docx/.pdf`。
