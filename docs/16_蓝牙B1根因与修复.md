# 16 · 蓝牙 B1 根因与修复（NuttX fd 表按 task_group ⇒ H4 TX 跨组 EBADF）

> 2026-09-18 · 任务 1 结案 + 任务 2(B1/B2) 实测通过 + B3 待人工
> 证据目录 `docs/evidence/bt-h4-20260918/`、`docs/evidence/accept-20260918/`（均含 sha256）

---

## 0. 只想验收？三条命令（本文其余部分都是过程记录）

```bash
cd ~/openvela

# ① 判定器本身有没有毛病（不碰板子，合成日志，正反两个方向都测）
python3 tools/pw_bt_b3.py --selftest

# ② 设备侧全量验收（15 项，不需要手机）
python3 tools/pw_accept.py --out docs/evidence/accept-$(date +%Y%m%d-%H%M)

# ③ B3：手机连接读写（需要一部手机；脚本会先确认广播真起来了再让你连）
python3 tools/pw_bt_b3.py --out docs/evidence/bt-b3-$(date +%Y%m%d-%H%M) --wait 240
#    手机上：蓝牙扫描 → 按**名字**找 "PhyWear"（广播是随机地址，别按 MAC）
#            → 连接 → 读 …a001 → 打开 Notify → 往 …a002 写 ping
```

三条各自打印 PASS/FAIL 表并落盘 sha256。**当前实测状态**：
① 5/5 符合预期；② **18/18 全过**；③ 设备侧全证、**空口那一跳仍需人做一次**。

---

## 1. 一句话根因

**zblue 的 NuttX port 把 HCI 命令的 `write()` 放在 Zephyr 系统工作队列线程里执行，
而 NuttX 的文件描述符表挂在 `task_group` 上 —— 该线程与调用 `bt_enable()`
（进而在 `h4_open()` 里 `open("/dev/ttyHCI0")`）的任务**不是同一个组**，
于是每一次 `write()` 都返回 `EBADF(errno=9)`。控制器从头到尾**一个字节都没收到**。**

`bt_enable()` 返回的 `-EIO(-5)` 是**假象**：它是
`hci_core.c` 在 `bt_send()` 失败后调用
`hci_cmd_done(hdev, opcode, BT_HCI_ERR_UNSPECIFIED /*0x1F*/, buf)`
**伪造**出来的一个 status，再被 `bt_hci_cmd_send_sync()` 翻译成 `-EIO`。

---

## 2. 证据链（全部为同一 boot 内的串口原文）

### 2.1 裸探针 vs host 栈（修复前，同一 boot）

`docs/evidence/bt-h4-20260918/02_ab-baseline-before-fix.log`

```
### SEND: phywear bthci
[BTHCI] /dev/ttyHCI0 opened
[BTHCI] write HCI_Reset -> 4
[BTHCI] tx (4 B): 01 03 0c 00
[BTHCI] rx (7 B): 04 0e 04 06 03 0c 00          ← 裸探针：status=0，通了
[BTHCI] 判据③：HCI 链路通（Command Complete, opcode=0x0c03, status=0）
### SEND: phywear cap bt
dev:0: Sending HCI_RESET
[bt] bt_enable rc=-5 (FAILED)                     ← host 栈：-EIO
```

### 2.2 打开 H4 字节级日志后（修复前）—— 第一次看到"发了什么/收到什么"

`docs/evidence/bt-h4-20260918/03_h4-bytelog-before-fix.log`

```
[H4] open /dev/ttyHCI0 -> fd 8
[H4] rx thread started
dev:0: Sending HCI_RESET
[H4] TX (4 B): 01 03 0c 00                        ← 和裸探针**一模一样**的 4 字节
[H4] h4_send_data -> -1 (want 4, errno 9)         ← errno 9 = EBADF
<err> Unable to send to driver (err -22)
<wrn> opcode 0x0c03  status 0x1f                  ← 伪造 status，不是控制器回的
[bt] bt_enable rc=-5 (FAILED) elapsed=4ms         ← 4ms，不是 10s 超时
```

**同一条 `[H4] TX` 证明"发"这一侧没问题；`errno 9` 证明 fd 本身在写的那一刻是无效的。**

### 2.3 决定性证据：打印"谁在写 / 它的 fd 表是谁"

`docs/evidence/bt-h4-20260918/00_rootcause-proof.log`

```
[bt] precheck open /dev/ttyHCI0 -> 8 (ok)
[bt] calling bt_enable(NULL) ...
[H4] open /dev/ttyHCI0 -> fd 8
[H4T] h4_open      tid=9  fdlist=0x600ad1e4 h4->fd=8   ← 打开者
[H4] rx thread started
[H4T] h4_rx_thread tid=21 fdlist=0x600ad1e4 h4->fd=8   ← 同组 ✓
dev:0: Sending HCI_RESET
[H4T] h4_send      tid=5  fdlist=0x20010120 h4->fd=8   ← **不同组** ✗
[H4] TX (4 B): 01 03 0c 00
[H4] h4_send_data -> -1 (want 4, errno 9)
```

`fdlist` 两个地址不同 ⇒ 两个不同的 `task_group` ⇒ fd 8 在 tid=5 那一组里不存在。

---

## 3. 代码级定位（可独立复核）

| # | 事实 | 文件:行 |
|---|---|---|
| 1 | fd 表按 **task_group** 存放 | `nuttx/fs/inode/fs_files.c:826` `file_get2()` → `fdlist_get2(nxsched_get_fdlist(), fd, …)` |
| 2 | `nxsched_get_fdlist()` 取的是 `tcb->group->tg_fdlist` | `nuttx/sched/sched/sched_getfiles.c:88` → `:51` `nxsched_get_fdlist_from_tcb()` `ret = &group->tg_fdlist;` |
| 3 | Zephyr 线程 = NuttX **pthread**，与**创建它的任务**同组 | `external/zblue/zblue/port/kernel/thread.c` `k_thread_create()` → `pthread_create()` |
| 4 | 系统工作队列线程由 `SYS_INIT` 在**板级启动阶段**创建 | `external/zblue/zblue/kernel/system_work_q.c` `SYS_INIT(k_sys_work_q_init, POST_KERNEL, …)`；由 `z_sys_init()` 调用，而 `z_sys_init()` 来自 `vendor/sifli/chips/sf32lb52/sf32lb52_bth4.c` `sf32lb52_bt_zblue_init_once()` |
| 5 | HCI 命令 TX 走系统工作队列 | `hci_core.c` `bt_tx_irq_raise()` → `k_work_submit(&hdev->tx_work)`；`tx_work_handler` → `process_pending_cmd` → `hci_core_send_cmd` → `bt_send` |
| 6 | port 的 `h4_send()` 直接在该线程里 `write(h4->fd,…)` | `external/zblue/zblue/port/drivers/bluetooth/hci/h4.c` `h4_send()`（修复前） |
| 7 | 失败被压成 `-EINVAL`，再被伪造 status 掩盖 | `h4.c` `h4_send()`：`ret != len ⇒ ret = -EINVAL`；`hci_core.c:3177-3184` `hci_cmd_done(… BT_HCI_ERR_UNSPECIFIED …)`；`hci_core.c:509-525` `status → -EIO` |

**三个可疑点的判定（任务 1 要求的按序证伪）**

| 可疑点 | 判定 | 依据 |
|---|---|---|
| ① UART 流控 RTS / 波特率 | **已证伪（不适用）** | `/dev/ttyHCI0` 根本不是物理 UART：`nuttx/drivers/serial/uart_bth4.c` 是**伪串口**，后端是 `vendor/sifli/chips/sf32lb52/sf32lb52_bt_adapter.c` 的 **L2H/H2L 硬件邮箱 + 共享内存环**。无波特率、无 RTS/CTS、无 DMA。`CONFIG_UART_BTH4` 下没有任何流控符号。 |
| ② H4 初始化时序 / LCPU 上电 | **已证伪** | `[BTA] controller_enable enter status=1 queue_open=0` → `lcpu_power_on -> 0`（500 ms 懒上电）→ `host_send_packet -> ret=0`，全程正常。修复前后都走到这里；修复前是**写 fd 就失败**，压根没进厂商驱动（`[BTD]` 一行都没有）。 |
| ③ host 栈重配 UART | **已证伪** | port 的 `h4.c` 只 `open()` + `read()/write()`，不改任何 termios。真正的断点在**进程/任务上下文**，不在传输配置。 |
| **④（新增，真凶）跨 task_group 的 fd** | **已证** | 见 §2.3 与 §3。 |

---

## 4. 修复

**文件**：`external/zblue/zblue/port/drivers/bluetooth/hci/h4.c`

**思路**：让**打开 fd 的那个线程组**独占所有 fd I/O。`h4_open()` 创建 `h4_rx_thread`
（与该组同组），所以把 TX 也交给它：

1. `struct h4_data` 增加 `struct k_fifo tx_fifo;`
2. `h4_open()` → `k_fifo_init(&h4->tx_fifo);`（在创建线程前）
3. `h4_send()`：只 `net_buf_push_u8()` 补 H4 type 字节 + `k_fifo_put(&h4->tx_fifo, buf)`，**不再 write**，返回 0（缓冲所有权移交驱动，符合 Zephyr 驱动契约）
4. 新增 `h4_hw_tx()`：真正的 `btsnoop_log_capture()` + `h4_send_data()` + `net_buf_unref()`
5. `h4_rx_thread()` 每轮循环开头 `while ((buf = k_fifo_get(&h4->tx_fifo, K_NO_WAIT))) h4_hw_tx(h4, buf);`

**代价**：`h4_data` +24 B（k_fifo），TX 最坏 +1 ms 延迟（RX 线程本身 1 ms 轮询节拍）。

**修复后实测**（`01_b1-pass.log`，同一 boot 内）：

```
[H4] open /dev/ttyHCI0 -> fd 8
[H4T] h4_open      tid=9  fdlist=0x600ad1e4 h4->fd=8
[H4T] h4_rx_thread tid=21 fdlist=0x600ad1e4 h4->fd=8   ← 同组
dev:0: Sending HCI_RESET
[H4T] h4_send      tid=5  fdlist=0x20010140 h4->fd=8   ← 只入队
[H4T] h4_hw_tx     tid=21 fdlist=0x600ad1e4 h4->fd=8   ← 同组写
[H4] TX (4 B): 01 03 0c 00
[BTA] controller_enable enter status=1 queue_open=0
 4.116  [BTA] lcpu_power_on -> 0
 4.517  [BTD]   host_send_packet -> ret=0
 4.517  [H4] h4_send_data -> 4 (want 4, errno 0)       ← 写成功（原 errno 9）
 4.517  [H4] RX raw (7 B): 04 0e 04 06 03 0c 00        ← LCPU 真回包 status=0
 ...
[bt] bt_enable rc=0 (host stack up) elapsed=1325ms      ← ★ B1 判据原文
```

---

## 5. 顺带确证的两件事（避免以后重复踩）

1. **`CONFIG_BT_DEBUG_LOG` 本板原先没开** ⇒ `port/include/zephyr/logging/log.h`
   把整个 host 栈 + `h4.c` 的 `LOG_ERR/LOG_WRN/LOG_INFO/LOG_DBG` **全部展开成空语句**。
   这就是为什么 `bt_enable()` 失败时串口上一个字都没有，只剩
   `hci_core.c:3388` 那句裸 `syslog("dev:%d: Sending HCI_RESET")`。
   树里其它所有用 H4 的板（goldfish / r528 / bes）都开了 `BT_DEBUG_LOG`。
   → 诊断期设 `CONFIG_BT_DEBUG_LOG=y` + `CONFIG_BT_DEBUG_LOG_LEVEL=4`（只放 err/wrn）。
2. **控制台提示符是 `vela>`（ai_agent CLI），行结束符必须是 `\n` 不是 `\r`**。
   `\r` 会被当成普通字符，命令不执行（现象：只有回显、没有输出）。
   取证脚本 `tools/phywear/pw_serial.py` 已按此实现。

---

## 6. 环境与铁律（本次实测口径）

- 板子：立创黄山派 SF32LB52-MOD-1-N16R8，`/dev/ttyUSB0` @1000000 8N1
- pyserial 打开后立刻 `dtr=False; rts=False`；端口 `exclusive=True`；禁 `erase_flash`
- 复位方式：RTS 脉冲（CH340N 把 RTS 接到了 SoC 复位脚）
- `phywear` 在 **NSH**（`nsh>`）里执行，不在 `vela>` 里
- `flash_with_ftab.sh` 第 3 步的"固件 ❌ 不一致"是**已知假阴性**（sftool 读回超时被吞 stderr）；
  板上固件是否真的更新，以启动日志里的新版打印为准

---

## 7. 可复现命令

```bash
# 构建
cd ~/openvela
export PATH=$PWD/prebuilts/tools/linux/x86_64:$PWD/prebuilts/gcc/linux-x86_64/aarch64-none-elf/bin:$PWD/vendor/artinchip/tools/scripts:$PATH
# 改了 defconfig 后必须先让 .config 重新生成（否则 cmake --build 不会重跑 Kconfig）
cmake --build cmake_out/sf32lb52_lchspi_ulp_nsh_ai --target resetconfig
cmake --build cmake_out/sf32lb52_lchspi_ulp_nsh_ai -j16 2>&1 | grep -E "error:|fatal error:|undefined reference|flash:|sram:"

# 烧录
./flash_with_ftab.sh cmake_out/sf32lb52_lchspi_ulp_nsh_ai/nuttx.bin /dev/ttyUSB0

# 同 boot 取证（复位 → 等 ai_agent 的网络超时把控制台放开 → 发命令）
python3 tools/phywear/pw_serial.py --out /tmp/btdiag --label b1 \
    --reset --step "30:phywear cap bt" --tail 5
grep -aE "\[bt\]|\[H4\]|\[H4T\]|\[BTD\]|\[BTA\]" /tmp/btdiag/b1.log
```

---

## 8. 可回退

| 项 | 值 |
|---|---|
| 改动前固件 | `~/桌面/PhyWear-bt-diag-20260918-1228/nuttx-before.bin`（2,584,244 B，md5 `65c4bd4a3c7f7fcb8d5c5dcf67a26866`，= 板上原固件） |
| 改动前源码 | 同目录 `h4.c.before` / `pw_bt.c.before` / `defconfig.before` |
| git 回退锚点 | tag `pre-full-redesign-20260917`（UI 线）；蓝牙线回退点 = 本提交的父提交 |
| 回退验证 | 烧回 `nuttx-before.bin` → `phywear cap bt` 应重新出现 `rc=-5 / errno 9` |

---

## 9. 证据文件与 sha256

**全量清单在 `docs/evidence/bt-h4-20260918/SHA256SUMS.txt`：44 个证据文件全部登记、
相对路径、可用 `sha256sum -c SHA256SUMS.txt` 一条命令自校验**（2026-09-18 复核时发现
早先手工分批 `>>` 追加漏登记了 3 个文件 —— `06_…log/.raw`、`23_…raw` —— 且路径混了
绝对/相对，已全部重写为相对路径并补齐）。
下表只列最早、最核心的几份：

| 文件 | 大小 | sha256 | 说明 |
|---|---|---|---|
| `00_rootcause-proof.log` | 10,336 B | `84bdab41…aeb4` | 根因决定性证据（tid/fdlist） |
| `00_rootcause-proof.raw` | 8,593 B | `9da523da…eb2d` | 上者的原始字节 |
| `01_b1-pass.log` | 21,159 B | `d7446769…4296` | **B1 通过**（`rc=0`） |
| `01_b1-pass.raw` | 17,458 B | `36f13021…5e22` | 上者的原始字节 |
| `02_ab-baseline-before-fix.log` | 10,084 B | `2acf034f…0088` | 修复前 A/B 对照 |
| `03_h4-bytelog-before-fix.log` | 10,331 B | `f2f3fd35…3afa` | 修复前 H4 字节日志（errno 9） |

---

## 10. 任务 2/3 进度

| 项 | 状态 | 判据 |
|---|---|---|
| **B1** host 栈起 | ✅ **实测通过** | `[bt] bt_enable rc=0 (host stack up) elapsed=1325ms` |
| B2 注册 GATT 服务（传感器读数 + 接收命令 + 可发现广播） | ⏳ 未做 | 待定义 |
| B3 手机连接读写 | ⏳ 未做（**需人工**） | 待定义 |
| N1 `set_wifi` 拿 DHCP IP | ⏳ 未做 | 串口原文 |
| N2 端到端通路 | ⏳ 未做 | 待定义 |

---

## 11. 追加：B2 打通 + 收尾数字 + 任务 3 结论（同日）

### 11.1 B2 注册 GATT 服务 + 可被发现广播 —— ✅ 实测通过

新增 `apps/examples/phywear/pw_btgatt.c`（`pw_bt_init()` 在 `rc==0` 后调用）：

| 项 | 值 |
|---|---|
| Service | `e0f1a000-1b2c-4d5e-8f90-a1b2c3d4e5f6`（PhyWear Physics） |
| Sensor 特征 | `e0f1a001-…` READ \| NOTIFY，16 B 小端包：`int16 ax,ay,az (mg)` `int16 gx,gy,gz (mdps/10)` `uint16 lux` `uint16 cmd_count`；+ CCC |
| Command 特征 | `e0f1a002-…` WRITE \| WRITE_WITHOUT_RESP，ASCII 命令，串口回显 `[bt] cmd #n (len B): 'xxx'` |
| 广播 | `BT_LE_ADV_CONN`，AD = Flags + Complete Local Name `"PhyWear"`；SD = 128 位服务 UUID |
| 采样 | `k_work_delayable` 500 ms，**只在有订阅者时**才 notify；采样走 `pw_sensors_read_*_oneshot()`（它自己 open/close `/dev`，跨 task_group 安全） |

实测原文（`07_final-build-b1b2.log`）：

```
[bt] bt_enable rc=0 (host stack up) elapsed=1225ms
[bt] B2 gatt register rc=0 (ok)
[bt] B2 adv start rc=0 (ok)
[bt] B2 READY name=PhyWear addr=CD:AB:78:56:34:12
[bt] B2 svc e0f1a000-1b2c-4d5e-8f90-a1b2c3d4e5f6
[bt] B2   sensor e0f1a001 READ|NOTIFY (16B) / cmd e0f1a002 WRITE
```

H4 字节级原文（`06_b2-gatt-adv-pass.log`，`PW_H4_TRACE=1` 的诊断构建）可见真实广播被下发给控制器：

```
TX: 01 36 20 19 ...                      LE Set Advertising Parameters
TX: 01 35 20 07 00 57 e7 96 62 58 5a    LE Set Random Address
TX: 01 37 20 10 00 03 01 0c 02 01 06 08 09 50 68 79 57 65 61 72
                                         LE Set Adv Data：02 01 06=Flags，
                                         08 09 "PhyWear"=Complete Local Name
TX: 01 38 20 16 00 03 01 12 11 07 f6 e5 d4 c3 b2 a1 90 8f 5e 4d 2c 1b 00 a0 f1 e0
                                         LE Set Scan Resp Data：128 位服务 UUID
TX: 01 39 20 06 01 01 00 00 00 00        LE Set Advertising Enable (0x01)
```

### 11.2 B3 手机连接读写 —— ⏳ 未做（**需人工**），已给出可执行判据

> **⚠️ 别按 MAC 找**：实测 H4 原文里 `LE Set Extended Advertising Parameters` 的
> `own_addr_type = 0x01`（RANDOM），且先发了 `LE Set Advertising Set Random Address`
> （`01 35 20 07 00 57 e7 96 62 58 5a` → `5A:58:62:96:E7:57`）。
> 也就是说板上开了 `CONFIG_BT_EXT_ADV` + `CONFIG_BT_PRIVACY`，**广播用的是随机地址**，
> 手机上只能**按名字 `PhyWear`** 找。日志里那个 `identity=CD:AB:78:56:34:12`
> 是控制器 public 地址，只在 `bt_id_get()` 有意义，**不是**广播地址。
> （广播参数实测：interval 100–150 ms，props=connectable|scannable ⇒ 可连接。）

有自动判定器（推荐）：`tools/pw_bt_b3.py`

```bash
python3 tools/pw_bt_b3.py --out docs/evidence/bt-b3-$(date +%Y%m%d-%H%M) --wait 240
```
脚本会复位板子、跑 `phywear cap bt`，然后留 240 s 给你用手机操作，
最后把 ①广播起来 ②手机连上 ③订阅 Notify ④写入命令 ⑤通知无报错
五项自动判 PASS/FAIL 并落盘 sha256。**负对照已做**：
没有手机时会判 4 项 FAIL（`16_b3-verifier-negative-control.log`）⇒ 判定器不是空过。

手动步骤（与脚本判定项一一对应）：

0. **句柄对照表**（手机 App 里能看到 handle，对不上说明连错了服务）：

   | 属性 | handle | UUID | 权限 |
   |---|---|---|---|
   | Service | `0x000E` | `2800`（Primary Service） | — |
   | Sensor 声明 | `0x000F` | `2803` | read |
   | **Sensor 值** | **`0x0010`** | `e0f1a001-…` | READ（NOTIFY 靠它） |
   | **Sensor CCC** | **`0x0011`** | `2902` | READ+WRITE |
   | Command 声明 | `0x0012` | `2803` | read |
   | **Command 值** | **`0x0013`** | `e0f1a002-…` | WRITE |

   （0x0001…0x000D 被 zblue 内建的 GAP / GATT / Service Changed 服务占掉了，属正常。）

1. 扫描 → 应看到 **`PhyWear`**（按名字，不要按 MAC）
2. 连接 → 应看到服务 `e0f1a000-…`，两个特征 `…a001` / `…a002`
3. 读 `…a001` → 16 B，前 6 B 是 ax/ay/az（小端 int16，单位 mg）
4. 对 `…a001` 打开 Notify → 串口应打印 `[bt] ccc changed -> notify ON`，之后每 500 ms 收到一包
5. 往 `…a002` 写 `ping` → 串口应出现 `[bt] cmd #1 (4 B): 'ping'` + `[bt] cmd: pong`；再读 `…a001` 的最后 2 B（cmd_count）应为 1

**通过判据**：直接看 `pw_bt_b3.py` 的判定表全 PASS；或手工确认第 4、5 步在串口上
留下上述原文（人工截图 + 串口 log 一起入 `docs/evidence/`）。

已为 B3 补上**设备侧可观测性**（`pw_btgatt.c` 的 `bt_conn_cb_register`），
这样"手机连上"这件事会在串口留下原文，而不是只能看手机屏幕：

```
[bt] B2 conn cb register rc=0
[bt] B3 connected: XX:XX:XX:XX:XX:XX err=0      ← 手机点连接时出现
[bt] ccc changed -> notify ON                   ← 手机订阅 Notify 时出现
[bt] cmd #1 (4 B): 'ping'                       ← 手机写命令时出现
[bt] B3 disconnected: XX:XX:XX:XX:XX:XX reason=0x13
```

### 11.2b 服务端 GATT 自检（B3 里**不需要空口**的那部分，已自动化）

B3 的"空口"段必须有真实 BLE 中心设备才能验，但**服务端数据库 + 读/写回调**这一段
不需要空口。`pw_btgatt_start()` 末尾会跑 `pw_btgatt_selftest()`，用的就是手机连上后
会走的那几个函数，实测原文（`17_gatt-selftest-pass.log`）：

```
[bt] SELFTEST attr[0] 2800 perm=0x01 read=Y write=-                 Primary Service
[bt] SELFTEST attr[1] 2803 perm=0x01 read=Y write=-                 Chrc Declaration
[bt] SELFTEST attr[2] e0f1a001-1b2c-4d5e-8f90-a1b2c3d4e5f6 perm=0x01 read=Y   Sensor(READ)
[bt] SELFTEST attr[3] 2902 perm=0x03 read=Y write=Y                 CCC(READ|WRITE)
[bt] SELFTEST attr[4] 2803 perm=0x01 read=Y write=-                 Chrc Declaration
[bt] SELFTEST attr[5] e0f1a002-1b2c-4d5e-8f90-a1b2c3d4e5f6 perm=0x02 write=Y  Cmd(WRITE)
[bt] SELFTEST light rc=0 lux=0 ch0=1 ch1=2
[bt] SELFTEST read sensor -> 16 B (want 16)
[bt] SELFTEST decode ax=963 ay=230 az=-4 mg |a|=991 mg lux=0          ← 合矢量 991 mg ≈ 1 g ✓
[bt] SELFTEST write cmd "ping" ->
[bt] cmd #1 (4 B): 'ping'
[bt] cmd: pong
[bt] SELFTEST write ret=4 (want 4)
[bt] SELFTEST PASS (fails=0)
```

⇒ **B3 里除了"空口这一跳"，其余全部已证**：数据库结构/权限/回调齐备、
读路径返回物理合理的 16 B（|a|=991 mg，静止手表就该是 ~1 g）、写路径能收命令并回 pong。
剩下未证的只有：手机能否扫到/连上、ATT 读写经空口往返。

**这个自检立刻抓到过一个真 BUG（值得记）**：第一次跑出来 `|a|≈15.4 g`
（`ax=-13400 ay=7660 az=0`）。查下去发现不是传感器问题，而是**我自己重构时把
`pw_sensors_read_imu_oneshot()` 那一行删掉了**，`imu` 成了未初始化栈变量，
于是把栈上的垃圾当成加速度上报。修好后是 991 mg。
⇒ 教训：**"读数看起来像数"不等于"读数对"**，一定要加物理量纲的合理性判据
（现在是合矢量必须落在 300~3000 mg）。lux 也补了 rc 打印，避免 `lux=0` 分不清
"真的 0"还是"读失败"（实测 `rc=0 ch0=1 ch1=2`，是环境光真的接近 0）。

### 11.2c 采样策略 + B3 判定器的一处假 PASS（都已修）

1. **采样只在有人看的时候做**：`pw_sample_once()` 改成
   - `g_nfy_on`（有订阅者）时才由 500 ms 的 `k_work_delayable` 周期采样；
   - 没有任何人订阅时不采样 —— 别为了没人看的数据每 500 ms 去开一次 `/dev/lsm6dsl0`；
   - **GATT 读的时候现采一次**（`pw_read_sensor()` 里调），这样手机点"读"拿到的是
     当下这一秒的值，而不是上一轮 notify 的旧值。

2. **B3 判定器曾经假 PASS（已修，值得记）**：`cmd_in` 的模式是
   `[bt] cmd #n (x B): '...'`，而**服务端自检自己也会写一条命令**，
   于是"没有手机"也会判"手机写入命令 PASS"。
   两处一起改：自检改用专用命令名 `selftest`（并打印
   `[bt] cmd: selftest ok (设备侧自检，非手机写入)`），判定器把 `notify_on` /
   `cmd_in` 两项**限定在最后一次 `B3 connected` 之后**才算数。
   修后负对照正确变成 3 项 FAIL（`22_b3-verifier-negative-control-v2.log`）。
   ⇒ 教训：**"判定器报 PASS"本身也要有负对照**，否则它只是在骗自己。

### 11.2d 第三轮：限时采样 + 出货固件的 A/B 复证 + 逐页回归

1. **限时采样（避免堵住 BT 工作队列）** —— 这是个真问题：
   GATT 回调与周期通知都跑在 **Zephyr 系统工作队列线程**上
   （`bt_recv` → `k_work_submit(&hdev->rx_work)` → `rx_work_handler`，
   `CONFIG_SYSTEM_WORKQUEUE_STACK_SIZE=4096`），而
   `pw_sensors_read_imu_oneshot()` 内部最坏要走「3 次读 + START + 5 次读」≈160 ms。
   原来我再套 6 次重试 ⇒ 最坏能把工作队列堵 ~1 秒，连带推迟 HCI 命令 TX 与收包处理。
   改法：`pw_sample_once(int strict)`
   - `strict=1`（GATT 读 / 自检）：用户主动触发，最多 4 次重试；
   - `strict=0`（500 ms 周期通知）：**只读一次**，样本不合理就**不更新** `g_pkt`
     （保留上一个好值），绝不为了一条通知去堵队列。

2. **出货固件里复证 A/B 两半**（同一固件）
   - 阶段 A 裸探针（`24_ship-phaseA-probe.log`）：
     `[BTHCI] tx (4 B): 01 03 0c 00` → `[BTHCI] rx (7 B): 04 0e 04 06 03 0c 00`，
     `判据③：HCI 链路通（status=0）`
   - 阶段 B host 栈 + B2 + 自检（`23_ship-selftest-gravity.log`）：
     `bt_enable rc=0` / `gatt register rc=0` / `conn cb register rc=0` / `adv start rc=0` /
     `SELFTEST PASS`，且这次读数非常干净：
     `ax=-20 ay=36 az=-1012 mg |a|=1013 mg` —— 手表平放、重力压在 Z 轴，教科书值。

3. **逐页 UI 回归**（`ui-sweep/`，全部 >5 KB）
   `tools` 20,936 B / `pendulum` 18,570 B / `raw` 14,435 B / `voice` 12,904 B /
   `settings` 9,089 B；`root` 26,608 B 且与改动前 **`cmp` 逐字节相同**。
   **voice 页与旧参考图 `voice-20260918/01_voice_assistant.png` 不同，是预期的**：
   逐像素比对差异只在 (25,79)-(317,179) 这个 292×100 的**文字卡片**区域（2.83% 像素），
   而那张参考图是在 `8ed399c` 提交的 —— **早于** `cff21eb`「文案改用字库内字符
   （避开缺字 好/识）」。即差异就是那次文案修改，不是本次蓝牙改动引起的回归。

### 11.2e 第四轮：185 s 浸泡 + 两处"会不会白折腾人"的坑

**① 浸泡测试（B3 要人在手机前折腾几分钟，先证明设备能扛住）**
`phywear cap bt` 之后连续采集 185 s（`27_soak-185s-bt-alive.log`）：

```
[phywear] alive t=30s  loops/s=297 fps=3
[phywear] alive t=60s  loops/s=322 fps=3
[phywear] alive t=90s  loops/s=322 fps=3
[phywear] alive t=120s loops/s=322 fps=3
[phywear] alive t=150s loops/s=322 fps=3
[phywear] alive t=180s loops/s=322 fps=3
```

**无 panic / 无 assert / 无重启 / 无 watchdog**；`[bt]` 自检后无任何报错；
传感器读数全程稳定在 `az≈-1012 mg |a|≈1013 mg`。
（唯一一条 `nsh: oactive: command not found` 在 3.3 s 的开机阶段，
**改动前的日志里同样有**，与本批改动无关。）
⇒ B3 期间设备可以一直活着并在广播。

**② `phywear cap bt` 会把 GUI 一起带起来**（此前没写清，补上）
`pw_cap_open("bt")` 只做 `pw_bt_init()`、**不打开任何屏**，但它 `return 1` 之后
`main()` 继续往下走进正常的 LVGL 主循环 ⇒ 界面照常在跑。
实测心跳：`[phywear] alive t=30s loops/s=297 fps=3` —— **fps=3 就是 root 页**
（UI 线的已知区间是 3~7，不是退化）。
**含义**：① 这个 boot 的"一个 GUI 额度"被 `cap bt` 用掉了，之后别再想跑 `cap raw`
之类；② 对 demo 反而是好事 —— 表盘亮着 + 在广播，手机连上时界面就在。
**根页 fps=3 是正常值**，别拿它和 `cap raw` 的 12 比。

**③ 控制台抢 stdin 的实证（会让 B3 白折腾一轮）**
同一 boot 内发第二条 `phywear cap bt`，它被 ai_agent 的 `vela>` CLI 吃掉了：

```
 phywear cap bt
Unknown command: phywear (type 'help')
vela> [phywear] alive t=30s loops/s=297 fps=3
```

（`28_console-race-vela-eats-phywear.log`）⇒ 如果判定器只是"发一条就当它生效了"，
人会在**根本没广播**的状态下去手机上找设备，然后以为设备坏了。
已修：`tools/pw_bt_b3.py` 现在**发完必须等到 `[bt] B2 adv start rc=0` 才继续**，
最多重试 5 次，全失败就明确打印"板子没在广播，别再试手机了"。
（实测首条 `cap bt` 在新复位后是稳定生效的，连测 4 次都是第 1 次就中；
重试是保险，不是常态。）

### 11.2f 第五轮：修掉 H4 RX 的一颗静默哑弹（`len == -EAGAIN` 永假）

**问题**（`port/drivers/bluetooth/hci/h4.c` 的 `h4_rx_thread`）：

```c
len = read(h4->fd, ...);
if (len < 0) {
    if (errno == EINTR) continue;
    if (len == -EAGAIN) { usleep(500); continue; }   /* ← 永远为假！ */
    LOG_ERR("Reading hci failed, errno %d", errno);
    close(h4->fd); h4->fd = -1; return;              /* ← 关 fd + 杀线程 */
}
```

`read()` 出错时返回 **-1**、EAGAIN 在 **errno** 里，所以 `len == -EAGAIN` 恒假 ⇒
**任何一次瞬时读错误**都会走到 `close(fd) + return`。
而 B1 的修复把 **TX 也交给了这个线程**（`h4_send` 只入 `tx_fifo`，由该线程写 fd），
所以一旦发生：**RX 线程没了 + fd 关了 + TX 也发不出去 = 整个蓝牙栈静默死亡**。
更要命的是出货配置**没开 `CONFIG_BT_DEBUG_LOG`**，上面那句 `LOG_ERR` 被编译成空语句，
连一行提示都没有 —— 在 B3 现场只会表现为"手机突然连不上/读不到"，几乎无法归因。

**修法**：
- 修正判据：`errno == EAGAIN || errno == EWOULDBLOCK` ⇒ `usleep(500); continue;`
- 其它错误不再当场放弃：累计 `h4->rx_errs`（新增字段，成功一次清零），
  连续 < `PW_H4_RX_ERR_MAX`(=50) 次就打印并重试；
- 真要放弃时用**无条件 `printf`**（不走 `LOG_*`、不走 `PW_H4_TRACE`）留下原文：
  `[H4] rx read 连续失败 %u 次 (errno=%d) —— 放弃并退出 RX 线程；此后 RX / TX 都会停`

**代价**：`h4_data` +4 B（实际被对齐吸收，SRAM 仍 **494,268 B 未变**）。
**验证**：出货固件复跑 —— `bt_enable rc=0` / B2 三行 rc=0 / `SELFTEST PASS`
（`|a|=1015 mg`）/ 全程 `rx read errno` 计数为 **0**（正常路径不受影响）；
fps 12 不变、root 截图与改动前逐字节相同。
（这条路径无法在实验室里"人为触发"，所以只能靠代码审读 + 正常路径无回归来兜；
证据是 `29_rx-err-handling-hardened.log`。）

> 这是本轮"专挑 B3 会走到的路径"审读时挖出的**第二颗**同型哑弹
> （第一颗是 R3 的工作队列阻塞预算）。共同点：**在出货配置下都是静默的**。

### 11.2g 第六轮：把"采样阻塞预算"从次数改成**时间**，并把它量出来

R3 已经把**周期**通知路径压成"只读一次"，但 **GATT 读路径**（手机点"读"触发）当时还是
"最多 4 次"——而 `pw_sensors_read_imu_oneshot()` **一次**最坏就 ~160 ms
（内部 3 次读 + `SNIOC_START` + 5 次读），4 次 ⇒ 最坏 ~640 ms 把 BT 工作队列按住，
手机连着时可能表现成卡顿甚至掉线。

改法：`strict` 路径改成**按时间**限
（`PW_SAMPLE_BUDGET_MS=200`、只有剩余预算 ≥ `PW_ACC_MIN_RETRY_MS=100` 才重试），
拿不到就保留上一个好值、不更新 `g_pkt`。**并且把耗时打印出来**——
"预算 200 ms"不能只是注释里的一句话：

```
[bt] SELFTEST sample took 2 ms (budget 200 ms)
[bt] SELFTEST decode ax=-23 ay=35 az=-1011 mg |a|=1012 mg
[bt] SELFTEST PASS (fails=0)
```

**如实说明**：2 ms 是"传感器有可读样本"时的实测（正常情况第一次就成）；
预算是给"拿不到合理样本"的病态情况兜底的，且**下界仍是单次 oneshot 的量级
（~160 ms）**——那部分没法从外面切断。本轮只保证"不会因为重试而累加到 640 ms"。

### 11.2h 断线后会自动重开广播（代码级已证；空口未验）

B3 现场最怕的是"手机连了一次、断开后再也搜不到"——那 demo 就变成一次性的。
查了 zblue 的广播状态机，结论是**会自动重开**：

| 环节 | 位置 | 事实 |
|---|---|---|
| 持久标志 | `host/adv.c:1115` | `atomic_set_bit_to(adv->flags, BT_ADV_PERSIST, !dir_adv && !(param->options & _BT_LE_ADV_OPT_ONE_TIME))` |
| 我们的参数 | `pw_btgatt.c` | `BT_LE_ADV_CONN` = `BT_LE_ADV_PARAM(BT_LE_ADV_OPT_CONNECTABLE, …)`，peer = NULL ⇒ `dir_adv = false`，且**没有** `ONE_TIME` ⇒ **`BT_ADV_PERSIST = true`** |
| resume 条件 | `host/adv.c:1509` `bt_le_adv_resume()` | 要求 `BT_ADV_PERSIST && !BT_ADV_ENABLED` 且 `BT_ADV_CONNECTABLE` ⇒ 我们全满足，于是执行 `bt_le_adv_set_enable(adv, true)` |
| 触发点 | `host/conn.c:1576` | 外设侧 LE 连接对象被 **deallocated**（即断开后）时调用 `bt_le_adv_resume(hdev)`（需 `CONFIG_BT_PERIPHERAL`，本板 `=1` 已开） |

**结论**：手机断开后广播会自动恢复，可以反复连，**不需要复位板子**。
**如实标注**：这一条是**读源码得出**的（`BT_ADV_PERSIST` 的置位条件 + resume 的触发条件都能逐一核对），
**没有**经过空口实测 —— 只有 B3 那次真实连接/断开才能最终确认。

### 11.2i 第七轮：一键验收脚本 + 它当场抓到的两个"测量口径"问题

新增 `tools/pw_accept.py`：**一条命令**把散在本文里的每条设备侧主张跑一遍，
逐项打 PASS/FAIL 并落盘 sha256。实测 **15/15 全过**（`docs/evidence/accept-20260918/`）：

```
PASS  A 阶段A裸探针 HCI 通            rx=04 0e 04 06 03 0c 00
PASS  B1 bt_enable rc=0               elapsed=1228ms
PASS  B2 gatt / conn cb / adv 三行 rc=0 + READY name=PhyWear
PASS  B2b SELFTEST PASS / |a|=1020 mg / sample took 2 ms / 无 RX 读错误
PASS  R1 帧率 >= 12                   loops/s=200 fps=12
PASS  R2 主页截图 26608B 且 sha256 == 金标（UI 未变）
PASS  R3 浸泡有心跳、无 panic/断言/重启
```

**但它第一次跑就"失败"了两项，而这两次都是我的**测量方法**有问题，不是设备坏了** ——
两条都值得记下来：

**① 帧率是"窗口量"，命令发出时刻决定读数（口径陷阱）**
`[phywear] alive t=30s loops/s=… fps=…` 里的 `fps = 本窗口实际推屏帧数 / 30`，
窗口从 GUI 启动起算。脚本第一版在复位后多静置了 3 s 才发命令，于是窗口整体后移：

| 命令发出时刻 | 观测 | 可重复性 |
|---|---|---|
| 复位后 **t≈3 s**（历史基线口径） | `loops/s=204 fps=12` | 连测 4 次一致 |
| 复位后 **t≈6 s**（脚本第一版） | `loops/s=183 fps=11` | 连测 4 次一致 |

**⇒ 比较帧率必须固定"命令发出时刻"**，否则会把口径差当成性能退化。
脚本已改回 0.5 s（同一口径），并把"最多测 3 次、有一次达标即 PASS"作为保险。

**② 串口是并发写，"证据没抓到" ≠ "功能失败"**
诊断这一项时抓到一次自检输出被冲掉：`read sensor -> 16 B` 与 `decode …` 两行整行消失，
只剩半行 `[bt] St" ->`。而**功能是好的**（同一段里 `SELFTEST PASS` 照常打印）。
原因是 phywear 的 `printf` 与 ai_agent 的 `syslog` 同时在写同一个控制台。
脚本第一版因此把一个正则没匹配到判成 `|a|=0 mg` **FAIL** —— 这是**假失败**。
已改成：B 阶段缺任一证据就重采（最多 3 次），**绝不把"没抓到证据"当成"功能失败"**。

### 11.2j 第八轮：给 B3 判定器补"正方向"自测（此前只测过"没手机"的方向）

`pw_bt_b3.py` 原来**只在"没有手机"的方向上做过负对照**。万一手机那边一切正常、
而脚本的正则/区间写错，人就会看到**假 FAIL**、以为设备坏了 —— 这个方向一直没测过。
现在加了 `--selftest`（不碰板子，用合成日志跑 5 例）：

```
PASS  理想情况：连上→订阅→写 ping                 期望 True  实得 True
PASS  没连上（其余证据齐全）                       期望 False 实得 False
PASS  写入发生在连接之前 —— 那是自检写的，不能算      期望 False 实得 False
PASS  连上后通知报错 —— 要 FAIL                   期望 False 实得 False
PASS  先 OFF 后 ON（手机来回切过订阅）—— 仍应 PASS   期望 True  实得 True
判定器自测：✅ 全部符合预期
```

三个方向都钉住了：**该过的过**、**自检自己写的那条不算手机写入**（区间限定生效）、
**订阅来回切不影响结论**。跑法：`python3 tools/pw_bt_b3.py --selftest`。

### 11.2k 第九轮：把"通知链"也拉进自检（此前它从未被执行过）

`pw_btgatt.c` 里"CCC 订阅 → 周期采样 → `bt_gatt_notify()`"这条链**只在有订阅者时才跑** ——
实验室没有手机 ⇒ **它从来没被执行过**。万一它坏在 B3 现场，表现就是"手机订阅了却一个包都收不到"，
而且没有任何日志可查（同 R6/R7 那两颗哑弹是一个性质）。

自检新增第 ④ 步，直接调一次 `bt_gatt_notify()`：

```
[bt] SELFTEST notify(无订阅者) rc=-107 (期望 -107=-ENOTCONN；手机订阅后这里应变成 0)
[bt] SELFTEST PASS (fails=0)
```

`rc = -107 = -ENOTCONN` 是**核对过源码的期望值**：`host/gatt.c:3053` 里
`gatt_notify_mc()` 把 `data.err` 初值设成 `-ENOTCONN`，而 `notify_cb()` 找不到
`cfg->value == BT_GATT_CCC_NOTIFY` 的订阅者时不会改它。
⇒ 这一步证明**属性句柄能解析、调用链通**，并把 B3 的期望值钉在日志里：
**手机订阅之后，同一行的 rc 应该变成 0**（这就是现场要看的那条判据）。

`pw_accept.py` 已把这条纳入判定，验收项 **15 → 16**，实测 **16/16 全过**
（`docs/evidence/accept-20260918/`，SHA256SUMS 7 条、`-c` 7/7）。

### 11.2l 第十轮：把"服务到底注册上没有"也证下来（句柄结构自检）

此前自检只 dump 了 UUID 与权限 —— **那证明不了服务真的注册成功**：`handle` 是
`bt_gatt_service_register()` 在**注册时**才写进属性的；若注册没生效，handle 会是 0，
手机来的 ATT 请求会**全部找不到东西**，而日志上"UUID/权限都对"看不出任何异常。

自检因此加 ①b 步：dump 每个属性的 `handle`，并断言两条结构不变量：

```
[bt] SELFTEST attr[0] handle=0x000e 2800 perm=0x01 read=Y write=-
[bt] SELFTEST attr[1] handle=0x000f 2803 perm=0x01 read=Y write=-
[bt] SELFTEST attr[2] handle=0x0010 e0f1a001-… perm=0x01 read=Y
[bt] SELFTEST attr[3] handle=0x0011 2902 perm=0x03 read=Y write=Y
[bt] SELFTEST attr[4] handle=0x0012 2803 perm=0x01 read=Y write=-
[bt] SELFTEST attr[5] handle=0x0013 e0f1a002-… perm=0x02 write=Y
[bt] SELFTEST handles: value=0x0010 ccc=0x0011 (ccc 应为 value+1)
```

① **所有 handle ≠ 0** ⇒ 服务确实注册进了 ATT 数据库；
② **CCC 紧跟在特征值后面**（`ccc == value + 1`）⇒ 这正是 zblue 通知路径的前提：
它用 `bt_gatt_attr_value_handle()` 取值句柄、再据它找到对应的 CCC。

顺带产出一张**给 B3 用的句柄对照表**（已写进 §11.2 的 B3 步骤）：手机 App 里
Sensor 值应是 `0x0010`、它的 CCC 是 `0x0011`、Command 值是 `0x0013` ——
对不上就说明连错服务了。验收项 **16 → 18**，实测 **18/18 全过**。

#### 11.3a 宿主侧工具的现实约束（2026-09-18 实测，避免又把人送进墙里）

写 §11.3 的宿主步骤时我默认了 `slattach`/`ifconfig` 可用 —— **实测本机没有**：
`which slattach` → 未找到；`dpkg -l net-tools` → 空（`ifconfig`/`route` 同样没有）。
所以那条 `sudo slattach …` 直接抄会失败。已在 `docs/07 §5.0b` 补上前置检查
（`which slattach || sudo apt install -y net-tools`）。

两条候选路径的取舍（**都没实测，因为都卡在同一根 USB 数据线上**）：

| | 板子侧 | 宿主侧 | 备注 |
|---|---|---|---|
| **SLIP**（当前代码走这条） | ✅ 已就绪（`CONFIG_NET_SLIP=y` + `phywear net`），唯一未知是 USB 枚举 | 需装 `net-tools`（`slattach`）+ root | **只剩一个变量**，且失败模式已能一行判别（`-107`） |
| **PPP** | 需另开 `CONFIG_NETUTILS_PPPD=y`（`select NETUTILS_CHAT` + `NET_TUN`）+ chat 脚本 | ✅ 本机已有 `pppd`，无需安装 | 复杂度从宿主搬到板子；板子侧完全未验 |

**当前建议**：先走 SLIP（变量最少）。若不愿装 net-tools，再考虑 PPP。

#### 11.2m ⚠️ B3 的分工要说清楚：脚本在**电脑**上跑，手机只当扫描工具

2026-09-18 用户反馈"手机跑不了 PY 文件啊" —— 这是**我的说明有歧义**造成的误解。
`pw_bt_b3.py` **从来不是给手机用的**，它是电脑上的脚本；手机只承担"用蓝牙 App 连一下"这个动作：

| 谁 | 干什么 |
|---|---|
| **电脑（本机）** | 跑 `python3 tools/pw_bt_b3.py … --wait 240`。脚本自己复位板子、发 `phywear cap bt`、**确认广播真起来了**、等 240 s、然后自动判 6 项 PASS/FAIL 并落盘 sha256 |
| **手机** | 装一个通用 BLE 调试 App（**nRF Connect** / **LightBlue**，商店直接搜），或系统蓝牙设置。**不装我们的东西、不跑脚本、不输命令**，就是点几下：按名字连 `PhyWear` → 读 `…a001` → 开 Notify → 往 `…a002` 写 `ping` |

已把这段分工写进 `docs/07 §5.0`（含手机上的分步点法）。**B3 的唯一前置就是"有一部手机 + 一个 BLE 调试 App"**。

### 11.3 任务 3（网络 N1→N2）—— ❌ **被硬件缺失阻断，不是软件问题**

| 证据 | 内容 |
|---|---|
| 运行时 `/dev` 全表 | `adc0 buttons config0 console gpio0/1/2 i2c0 i2c1 input0 lcd0 lcdlight light0 lsm6dsl0 mag0 mic0 mmcsd0 null pwm0 ram0 rtc0 spi1 spk0 timer0 ttyACM0 ttyHCI0 ttyS0 urandom watchdog0` —— **没有任何 wlan/eth 设备**（`10_no-wifi-hardware-dev-list.raw`） |
| 构建配置 | `defconfig` 里没有 `CONFIG_WIFI` / `CONFIG_DRIVERS_IEEE80211`；`CONFIG_NET=y` 只为满足 ai_agent 编译依赖（工作区 CLAUDE.md 原文："真机没有网络硬件驱动，NET 只用于满足 ai_agent 的编译依赖"） |
| BSP | `vendor/sifli/boards/sf32lb52/` 下无任何 `netdev_register` / `ieee80211` 调用 |
| `set_wifi` 实测 | 命令在 **NSH** 里回 `nsh: set_wifi: command not found`（`set_wifi` 是 ai_agent `vela>` CLI 的命令，见 §5 第 2 条；本机 `vela>` CLI 与 NSH 抢 stdin，命中不确定） |
| `set_wifi` 实现 | `packages/ai_agent/src/infra/network_manager.c` 走 `ifup wlan0` + `wapi` shell 命令 —— 需要真实无线网卡驱动 |
| **运行时终判（最强证据）** | 在 `nsh>` 执行 `ifconfig` → **`ifconfig: open failed: 2`**（ENOENT：命令在、网卡不在）。`help` 里 `ifconfig / ifup / ifdown / arp / nslookup` **都注册了** ⇒ netutils 编进来了，只是没有任何 netdev 可打开。全文里 `wlan` / `eth0` / `Link encap` / `inet addr` 出现次数均为 **0**（`15_n1-no-netdev-runtime.log`） |

**结论（2026-09-18 第二次修正，重要）**：

1. **WiFi 路径确实不可达**：本板没有无线网卡，`set_wifi`（内部 `ifup wlan0` + `wapi`）无从执行。
2. **但"让手表拿到 IP、把数据发到宿主"本身可以做** —— 换一条**不需要网卡**的链路：
   **SoC USB 设备口 → CDC-ACM → SLIP**。依据：
   - 板级 pinmux `bsp_pinmux.c:178-179` 把 `PAD_PA35/36` 配成 **USB_DP/USB_DM**；
   - 板级 `sifli_ap.c:693` 已 `cdcacm_initialize(0, NULL)`，板上有 `/dev/ttyACM0`；
   - `nuttx/drivers/net/slip.c` 本来就在树里，`slip_initialize()` 是公开 API
     （`nuttx/include/nuttx/net/slip.h:101`），只差 `CONFIG_NET_SLIP=y`。

   **软件这一侧本轮已补齐并实测**：defconfig 加 `CONFIG_NET_SLIP=y` + `CONFIG_NETUTILS_DHCPC=y`，
   新增 `phywear net`（`pw_net.c`）调 `slip_initialize(0, "/dev/ttyACM0")`。实测：

   ```
   [net] 本板无无线网卡（ifconfig: open failed: 2 = ENOENT）⇒ set_wifi 不可达
   [net] 改走 USB CDC-ACM + SLIP：slip_initialize(0, /dev/ttyACM0)
   [net] slip_initialize rc=-107 (FAILED)
   ```

   `-107 = -ENOTCONN`，**根因已定位**：`slip_initialize()` 只可能在 `file_open()`
   失败时返回错，而 `file_open("/dev/ttyACM0")` 被 cdcacm 拒了 ——
   `nuttx/drivers/usbdev/cdcacm.c:2140`：`if (priv->config == CDCACM_CONFIGIDNONE)
   return -ENOTCONN;`（注释就叫 `SETUPNOTCONNECTED`）。
   ⇒ **板的 USB 设备口没有被任何 USB 主机枚举**（宿主机 `lsusb` 里只有 CH340
   `1a86:7523`，没有 `38F4`）。

   **所以只差一根 USB 数据线**（把表的 USB 数据口插到本机）：
   ```
   # 板子侧
   phywear net                      # 期望变成 rc=0，sl0 注册成功
   ifconfig                         # 应出现 sl0
   ifconfig sl0 192.168.7.2         # 先静态验证 IP 层通
   # 宿主侧（对端）
   sudo slattach -p slip -s 115200 /dev/ttyACM0 &
   sudo ifconfig sl0 192.168.7.1 pointopoint 192.168.7.2 up
   # 板子侧再 renew sl0 走 DHCP（宿主起 dhcpd 或 dnsmasq）
   ```
   插上线以后，我就能把这条链路一路验到 TCP（`ping` / 一个小 UDP 发送）——
   那才是真正意义上的 **N1（拿到 IP）+ N2（测量数据发宿主）**。

**不允许把未实测写成已通过**：上面标注"期望 rc=0"的部分**尚未实测**，因为线还没插。

**2026-09-18 最终结论（经用户确认）**：用户确认**无法把表的 USB 数据口插到宿主**
（愿意装 `net-tools` / 用 PPP，但缺的是**物理链路**，不是宿主软件）。因此在本板现有条件下：

| 路径 | 状态 |
|---|---|
| WiFi（`set_wifi` → DHCP） | ❌ 硬件不可达（本板无无线网卡）—— **已证** |
| USB CDC-ACM + SLIP | 🔶 软件五环齐备、失败模式可一行判别，但**缺一根 USB 数据线** —— 链路不通 |
| PPP（宿主已有 pppd） | 🔶 同上：宿主侧无需安装，但同样需要那条 USB 数据链路；板子侧还要开 `NETUTILS_PPPD` |

⇒ **任务 3 的 N1/N2 在本板 + 现有接线条件下无法收尾**，如实计入「已知限制」。
若将来能接上表的 USB 数据口，`docs/07 §5.0b` 里的步骤可直接照做（届时只需把
`phywear net` 的 `rc=-107` 变成 `rc=0`，再配宿主 `slattach`/`pppd`）。

**可以部分替代 N2 的路径（已交付，待人工确认）**：N2 的措辞是「测量数据/Agent 消息
发宿主或局域网端点」。B2 的 sensor 特征 + Notify 已经做到「**测量数据实时发到宿主
（手机）**」，只是承载在 BLE 而不是 WiFi 上 —— 对一块没有网卡的板子，这是唯一
物理可行的端到端通路。等 B3 人工确认后，可据此把 N2 记为「以 BLE 承载部分通过」，
把「WiFi/DHCP」单列为已知限制。

### 11.4 收尾数字（最终出货构建）

| 项 | 改动前 | 最终 | Δ |
|---|---|---|---|
| flash | 2,584,244 B | **2,590,748 B** | **+6,504 B**（B2 GATT/广播/连接回调 + H4 tx_fifo） |
| SRAM | 493,872 B（94.20%） | **494,268 B（94.27%）** | **+396 B** = H4 `tx_fifo` 24 + B2 GATT/广播 ~340 + conn_cb 32 |
| fps（`phywear lang zh cap raw` 30 s 心跳） | 12 | **12**（loops 202） | 0 |
| 主页截图 sha256 | `d552c57b…` | **`d552c57b…`（`cmp` 逐字节相同）** | 0 |
| 固件 md5 | `65c4bd4a…` | `2f…`（见 `SHA256SUMS.txt` 与回退包） | — |

> **SRAM「不新增」已做到**：一度为排障打开 `CONFIG_BT_DEBUG_LOG`（+2,096 B），
> 根因定位后已关闭并重新验证 —— 现在只剩**新功能本身**的 396 B。
> 排障时若要再开：加回 `CONFIG_BT_DEBUG_LOG=y` + `=4` 后**必须**跑
> `--target resetconfig` 再 `cmake --build`。
> （11.5 第 4 条那个 cmake 翻车是**我自己手改 `.config` 去掉空行**造成的连带伤害，
> 不是"删符号"本身；事后用 `cmake -P` 单独跑 `nuttx_export_kconfig()` 验证：
> 带/不带这两个符号**都返回 OK**。）

### 11.5 本次踩到的新坑（记下来）

1. **本板控制台行结束符是 `\n`，不是 `\r`**：发 `help\r` 只会回显不执行；
   `pwshot.py` 能用是因为它发的是 `command + "\r\n"`。
2. **`vela>`（ai_agent CLI）与 NSH 抢同一个 stdin**，谁先阻塞在 read 谁拿到那一行；
   所以 `set_wifi` / `net_status` 这类 `vela>` 命令命中不确定。开机 ~30 s 后
   ai_agent 的 netmgr 超时会把控制台放开，此后 NSH 通常赢。
3. **改 defconfig 后必须 `--target resetconfig`**，否则 `cmake --build` 不会重跑 Kconfig
   （现象：`.config` 里还是旧值，白编译一轮）。
4. **别手改 `cmake_out/.../.config`**：我为"去掉空行"手工重写过它，结果
   `process_config.py` 链路被带坏，后续 configure 报
   `nuttx_export_kconfig(): string(REGEX MATCH) needs at least 5 arguments`
   → `Configuring incomplete`，我一度误判成"删 CONFIG_ 符号会翻车"。
   事后用 `cmake -P` 单独跑 `nuttx_export_kconfig()` 证伪：带/不带
   `CONFIG_BT_DEBUG_LOG*` **都 OK**。**改配置只走 defconfig + `--target resetconfig`。**
5. `flash_with_ftab.sh` 第 3 步"固件 ❌ 不一致"是**假阴性**（sftool 读回超时、stderr 被吞）；
   以启动日志里的新版打印为准。
6. `pwshot.py shot` 偶发 `lines missing / rejected`（串口分帧被启动日志冲掉）；
   加 `--settle 9000` 让开机日志先流完即可稳定出图。
