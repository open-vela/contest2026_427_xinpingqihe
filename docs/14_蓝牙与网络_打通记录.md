# 14 · 蓝牙与网络打通记录（主线收尾）

## B1 · 起 zblue host 栈（进行中）

### 已确证的硬事实
| 项 | 值 |
|---|---|
| 真公共头 | `external/zblue/zblue/include/zephyr/bluetooth/bluetooth.h` |
| host 实现 | `external/zblue/zblue/subsys/bluetooth/host/hci_core.c`（`bt_enable_mc()` 等） |
| **app 的 CMake target 名** | **`apps_phywear`**（**不是** `phywear`）—— 猜错会导致 `Configuring incomplete` |
| 接线范式 | 同文件里已有 `target_include_directories(apps_phywear PRIVATE …)`；`../` 三级到树根 |
| 配置现状 | `CONFIG_BT=y` · `BT_H4` · `BT_EXT_ADV` · `BT_GATT_CLIENT` · `BT_PRIVACY` · `BT_CLASSIC` 已开 |
| 阶段 A 结论 | HCI 通：`/dev/ttyHCI0` 回 `04 0E 04 01 03 0C 00`（`pw_btprobe.c`） |

### 本轮改动（已编译通过）
- `apps/examples/phywear/CMakeLists.txt`：`SRCS` 加 `pw_bt.c`；新增
  `if(CONFIG_BT) target_include_directories(apps_phywear PRIVATE …/external/zblue/zblue/{port/,}include)` +
  `target_link_libraries(apps_phywear PRIVATE zblue)`。
- `apps/examples/phywear/pw_bt.c`：`pw_bt_init()` = `bt_enable(NULL)` + 打印 `rc`。

### 状态
- **编译：✅ 真实错误数 0**（用 `<zephyr/bluetooth/bluetooth.h>` 可解析 ⇒ 接线正确）
- **链接：尚未进固件**（flash 未变 ⇒ 无人调用 `pw_bt_init()`，被 gc 回收）→ **下一步：加 `phywear bt` 子命令触发**
- 真机：未跑（等 CLI 入口）

### 踩坑记录（本轮）
1. **target 名猜错**：写 `target_include_directories(phywear …)` → CMake configure 失败；正确名是 `apps_phywear`（从文件内既有范式读出）。
2. **错误计数器误判**：我用 `grep -cE "error"` 统计 → 把源码行 `LOG_E("LCDC error code %x")` 也算成错误（25 个假阳性）⇒ **白回滚一次**。
   **判据修正：只认 `error:` / `fatal error:` / `Configuring incomplete` / `undefined reference`。**

## 下一步（B1 收尾 → B2）
1. `phywear.c` 加 `bt` 子命令 → 调 `pw_bt_init()`（让它进固件并可远程触发）
2. 烧录 → 串口抓 `[bt] bt_enable rc=0 (host stack up)` 作为 **B1 判据原文**
3. B2：注册 GATT 服务（参考 `external/zblue/zblue/tests/bluetooth/gatt/src/main.c` 的 service 写法），暴露传感器读数 + 接收命令，开广播
4. B3：手机连接验证（需人工）
5. 网络 N1：`set_wifi <ssid> <pass>` → DHCP 拿 IP（串口原文为证）；N2 端到端通路
