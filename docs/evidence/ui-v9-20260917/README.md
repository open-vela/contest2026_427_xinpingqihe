# UI 重置版 S1–S3a 真机证据（2026-09-17）

| 项 | 值 |
|---|---|
| 构建 | flash **2,489,092 B**（14.84%）/ SRAM 489,136 B（未变）/ md5 `759e3f10164477e72c9091cceb3be6f6` |
| 已上板 | S1 主题层（`pw_theme.{c,h}`，默认玫瑰）+ S2 顶栏（时间 `-66`、麦克风按钮 `-20` + 指令面板）+ **S3a 底晕**（`pw_scr_new()` 里两层 2 段竖直渐变，全 App 生效） |
| 截图 | `01_home_wash_mic.png`（主页：麦克风按钮 + 底晕）· `02_board_single_accent.png`（板块页：强调轨已统一为主题色） |
| 本轮改动 | 主页图标块 + 板块行强调轨 + 板块页头强调条 **3 处从"8 种板块色"统一为主题单一强调色**（符合定稿 §3 Color Consistency Lock） |
| 帧率 | **197 → 190 loops/s（−3.6%）· fps 12 不变**（同口径 `cap raw`，30s 心跳；197 为 S3a 之前） |
| 自审结论 | 底晕代价 −3.6% **低于自设 5% 阈值** ⇒ 保留；逃生开关：`PW_WASH_BOTTOM` 设 0 即只留顶部一层（少一次全屏填充） |

## 口径说明

- 本次改动**没有任何动画/循环**：底晕是静态渐变填充（EPIC 原生），麦克风按钮是静态控件。
- 因此预期 fps 不变；`loops/s` 可能因每帧多两次全屏渐变填充而略降（渐变在 EPIC 上属 FILL 路径，radius=0 直通）。


## S3b（主页方形格）— 2026-09-17 第 2 轮

| 项 | 值 |
|---|---|
| 改动 | 主页 2×4 宽条 → **4 列 × 2 行方形格**（86×92）· 图标块居中(32px) · 名称居中折行 · 说明行隐藏 → 由**顶部聚焦卡**承担 · **按住某格即更新聚焦卡**（`LV_EVENT_PRESSED`，字符串走 i18n 静态表，不传栈上 `&boards[i]`） |
| 编译 | ✅ 通过（flash 2,489,556 B / 14.84%） |
| SRAM | **489,304 → 489,376（+72 B，已精确归因）**：`g_focus_name/info` 8 B + `g_bname/binfo` 8×8=64 B |
| 帧率 | **192 loops/s · fps 12**（S3b 前 190/12；改动无退化） |
| 截图 | `03_home_s3b_grid.png`（与 HTML `v8-01-home.png` 同构：聚焦卡 + 4×2 方形格 + 主题色描边） |
| 踩坑 | 补丁锚点 `int col = i % 2;` 在文件里**有两处**，第一次断言拦下（x2）；改为"只改后跟 `MENU_TILE_W` 的那一处"才成功 —— 已写入改码纪律 |


## S3c（光斑）— 2026-09-17 第 3 轮

| 项 | 值 |
|---|---|
| 实现 | **`pw_glow.c`：96×96 A8 灰度遮罩**（radius² 柔化生成，9,216 B flash）+ LVGL `lv_image` + `image_recolor`=主题色 + `image_opa`；主机板放两张（scale ×4 / ×3，opa 40% / 20%），`lv_obj_move_background()` 压到最底层（底晕之上、内容之下） |
| 为何用 A8 | 一张位图用 recolor 适配 4 套主题；比 ARGB8888 省 4 倍 flash；EPIC 上走 image 路径（EPIC 不支持径向渐变，这是唯一可行的"光流体"落法） |
| 编译 | ✅ flash 2,489,556 → **2,499,364 B（+9,808 B）** |
| SRAM | **489,376 → 489,384 B（+8 B，精确归因 = 两个对象句柄）** |
| 帧率 | 见下（本轮实测） |
| 截图 | `04_home_s3c_glow.png` |
| 行为 | 按住不同模块 → 两张光斑随聚焦列漂移（只改位置，无每帧开销） |
