# 回退到「UI 重置前」的设计 — 2026-09-18（用户决定）

## 决定
用户反馈"重置版当前 BUG 多"，要求**回退到重置前的设计**（即当初认可的 UI v2）。

## 做法（不改写历史）
- 用 tag **`pre-full-redesign-20260917`** 恢复 `app/phywear/*`（10 个文件）与 `tools/pwshot.py`；
  重置期新增的 `pw_theme.*` / `pw_glow.*` 一并移除，`CMakeLists.txt` 回到不含它们的版本。
- 以**一个新的正向提交**记录这次回退（不 rebase、不改写历史、仍只提交本地）。

## 校验
| 项 | 值 | 说明 |
|---|---|---|
| flash | **2,487,452 B** | **与重置前那版固件字节数逐字节一致** ⇒ 回退干净 |
| SRAM | 489,296 B | 重置版为 489,416 B |
| 真机截图 | `01_home_pre_redesign.png` | 主页恢复"旧版 2×4 宫格 + 旧顶栏" |

## 保留物（仅文档，不影响固件）
- 重置版原型与静图：`docs/design/phywear-v8-prototype.html`、`v3/v4/v5/v6/v7/v71/v8-*.png`
- 设计与实施记录：`docs/13_UI重置版_定稿.md`（含 §11 已完成/未做/已知限制）
- 重置期真机证据：`docs/evidence/ui-v9-20260917/`（11 张 + 踩坑记录）
- 容忍模式抓帧工具改进留在历史提交 `918bb03980c5`（本次一并回退，需要时可单独挑回）

## 已知：这次回退带走的改进
- 顶栏时间从 -66 回到 -18；麦克风/指令面板、主题层、底晕/光斑、方形格主页、细线分组板块页/设置页/关于页、
  24 处强调条统一 —— **全部不再生效**（代码在 git 历史里，随时可挑回）。
