# UI 第二批 (a)(b)(c)：原始传感器迷你曲线 / 倾角指针盘 / 秒表环形进度（2026-09-15）

> 截图为**模拟器**（goldfish-arm64-v8a-ap-phywear，390×450，与真机同分辨率同布局）与**真机**（黄山派 SF32LB52）两套。
> 模拟器 IMU 是**合成波形**（`phywear_sensors.c`：`ax=600·sin`、`ay=120·sin`、
> `az=1000+300·sin` [mg]、`gx=20000·sin` [mdps]，0.8 Hz），**数值不得当测量结果引用**。
> 倾角/秒表页的截图用了 `inclinebench` / `timebench` 的**注入波形**，同样是合成数据。
>
> ⚠️ **2026-09-15 当天按用户反馈做过一次修订**：三轴页改为**双视图**（默认数值视图 = 原版大数字风格，
> 轻点切图线）且曲线改**每轴独立量程**；同时修掉主页 tile 过期文案。**修订内容以 §7 为准**，§1–§4 描述的是修订前的形态。

## 0. 本批最终固件

| 项 | 值 |
|---|---|
| md5 | `0bcf92781d2615de11eb1aa7fac5f100` |
| 大小 | 2,062,680 B（flash 12.29%） |
| SRAM | **490,648 B / 512 KB（93.58%）**，较改动前 489,440 B **+1,208 B** |
| 读回校验 | 6 段 16 KB 采样窗口（首块 + 256K/512K/1M/1.5M + 末块）**全部逐字节一致** |
| 启动 | `SFBL → ABCD → ADC calibration → 3 个传感器注册 → NuttShell (NSH)` |

## 1. 截图清单

| 文件 | 内容 |
|---|---|
| `before-raw-accel.png` | **改动前**加速度页：每轴一个大数字（48 px）+ 右侧大片留白 |
| `sim-raw-accel.png` / `realboard-raw-accel.png` | 加速度页：一行三列数值 + 358×186 三泳道实时曲线 |
| `sim-incline-dial.png` / `realboard-incline-dial.png` | 倾角页：13 根刻度 + 指针 + 盘心（0° 在正上方，±90° 满偏） |
| `sim-stopwatch-ring.png` / `realboard-stopwatch-ring.png` | 秒表页：环形计时进度 + 环心实时秒数 |
| `sim-three-pages.png` | 三页并排（模拟器） |
| `realboard-raw-gyro.png` / `realboard-raw-mag.png` / `raw-axis-3pages.png` | 陀螺/地磁页（(a) 阶段截图；地磁在模拟器无驱动，由真机补齐） |
| `raw-mic-regression.png` | 麦克风页（未改动，回归对照） |
| `verify_curve_scale.py` | 曲线幅度像素级校验脚本（可复现） |
| `realboard-boot.log` | 真机启动日志 |

## 2. 改了什么

### (a) 原始传感器三轴页 —— 数值 + 迷你实时曲线

- **布局**（`phywear_raw.c`）：3 行「轴字母 + 48 px 大数字」→ **一行三列数值**
  （轴字母 20 px 用 X/Y/Z 系列色、数值 28 px）+ 下方 **358×186** 曲线区，
  竖直切成 X/Y/Z 三条泳道（各 62 px），与数值列左右对齐；左下 `+/-满量程`、右下 `8 s`。
  数值不再带单位（列宽 126 px 放不下 `+1234.5 dps`），单位统一标在右上角。
- **复用 `pw_scope`**：新增 `pw_scope_set_lanes_i16()`（多泳道 + int16 样本 + 单块缓冲），
  每页只占 **1 个 scope 槽位**，**不新增 lv_malloc 缓冲**。
- **历史样本**：`int16_t g_raw_hist[3][80]`（80 × 100 ms = 8.0 s），**三轴页共用一份**，
  进页清零（曲线只表示"本次进入后连续采样的 8 s"）。
- **自动量程**：取**整条窗口**峰值按 1-2-5 步进（加速度 1..8 g、陀螺 20..2000 dps、地磁 100..30000 mG）。

### (b) 倾角页 —— 指针式角度盘

- **13 根刻度短线**（`lv_line`，-90°..+90° 每 15°，每 30° 一根长粗刻度）+ **指针**（`lv_line`）
  + 盘心圆点；0° 在正上方，±90° 满偏，正值向右。
- 刻度按当前角度**逐格点亮**（只对状态变化的刻度写颜色）；指针 12.5 Hz 节流 +
  位移 <1° 不重绘（静止的手表零绘制开销）。
- `lv_line` 的**对象框收紧到线段自身包围盒**：`lv_line_set_points()` 失效的是整个对象框，
  保持 168×168 会让每次移针重画整盘。
- **不用 `lv_arc`**：实测软件圆弧绘制把本页帧率从 ~26 拉到 23（见 §4）。

### (c) 秒表页 —— 环形计时进度

- `lv_arc` 满环 + **环心实时秒数**（本页原来没有实时读数）：满环 = "上次间隔"
  （还没有就取 2.0 s），跑动中按 `elapsed/ref` 填充；超过上次间隔 → 满环 + 换"偏慢"色（橙）。
- 停下时满环但用暗色（读作"上一次间隔已完成"，不是"正在进行"）。
- **只在值与颜色真的变了才写对象**：`set_style_arc_color()` 每次调用都会让整个圆弧对象
  重绘（156×156），闲时每 100 ms 白刷一次很吃亏。

## 3. 曲线幅度校验（像素级，可复现）

```bash
python3 verify_curve_scale.py sim-raw-accel.png accel
python3 verify_curve_scale.py sim-raw-accel.png gyro
```

按系列色取出曲线像素，用泳道几何把行号反算成物理量，与合成波形对照：

| 轴 | 实测（从截图反算） | 合成波形期望 | 结论 |
|---|---|---|---|
| 加速度 X | −0.62 .. +0.62 g | ±0.60 g | ✅ |
| 加速度 Y | −0.11 .. +0.11 g | ±0.12 g | ✅ |
| 加速度 Z | +0.69 .. +1.28 g | 0.7 .. 1.3 g | ✅ |
| 陀螺 X | −19.13 .. +19.13 dps | ±20 dps（100 ms 采样最大值 ≈19.4） | ✅ |
| 陀螺 Y/Z | 泳道中线（反算 −0.91 dps） | 0 | ✅（±0.5 px 取整） |

> 曾用临时插桩日志验证 `hist[79] == round(v*k)` 与送给数值标签的 `v` 完全一致
> （日志：`[phywear][dbg] ... v=-2224/-444/2884 h79=-2224/-444/2884`）。模拟器截图里偶尔看到的
> "曲线比数字提前/滞后一个采样（100 ms）"是 **adb 在 LVGL 正在写 `/dev/fb0` 时读帧**造成的
> 读撕裂，不是渲染逻辑问题。

## 4. 帧率与内存（真机实测，同页同 bench，30 s 窗口）

`fps` = 本窗口 LVGL 实际推屏帧数/秒；`loops/s` = GUI 主循环次数（**不是**帧率）。
本批为此把原来"只在 `bench` 模式打印"的帧率探针接进了 30 s 心跳。

| 页面 | fps 改动前 | fps 改动后 | loops/s 前→后 |
|---|---|---|---|
| 原始传感器（a，曲线） | 12（1 次） | **12 / 12** | 63 → 59 |
| 倾角（b，指针盘） | 27 / 27 / 25 | **25 / 25 / 25** | 27 → 25 |
| 秒表（c，环形进度） | 11 / 10 / 10 | **9 / 9 / 9** | 61 → 59 |

- **(a) 无帧率退化**（12 → 12）。
- **(b) 落在改动前区间内**（改动前 25~27，改动后稳定 25）。过程中对比过三种实现：
  `lv_arc` 圆弧 → 23；去掉圆弧改刻度 → 24；修正构建后 → **25**。
- **(c) 稳定少 1 帧（10.3 → 9）**。原因：环心读数落在圆弧对象的包围盒内，读数每 100 ms
  变一次就会连带重绘圆弧。**已知可选修法**：把圆弧也改成"分段刻度环"（与 (b) 同一手法），
  预计回到 ~10 fps，代价约 +320~384 B SRAM —— 本批**未做**，留作后续按需取舍。
- 主循环次数全线 −3~−4（每帧 CPU 变多），但帧率由 100 ms / 50 ms 的 tick 节拍决定，未受影响。

> ⚠️ **帧率探针在真机上是本批新接的**：原来的 `pw_fps_init()` 只在 `/dev/fb0` 可 mmap 时
> 装回调，而**真机没有 `/dev/fb0`（只有 `/dev/lcd0`）**，所以真机 `fps` 恒为 0。
> 现改为挂 `LV_EVENT_RENDER_READY`（`lv_refr.c` 只在本轮真的有区域重绘时才发），
> 模拟器/真机都成立，且不再替换 flush 回调（不干扰板级推屏路径）。

## 5. 本批发现并修复的三个问题

1. **自动量程抖动（本批新引入，已修）**：最初用"当前瞬时样本"算峰值 → 正弦过零点时
   量程掉档，出现"数值写 `+/-50 dps`、曲线却按 20 dps 画"。改为取整条 80 点窗口峰值后自洽。
2. **`pw_ui_root()` 屏幕栈泄漏（既有隐患，顺手修）**：只把 `g_scr_n` 归零、不删除栈里的旧屏
   → 旧屏的 `pw_scope` 槽位不归还；`phywear cap root` 反复重建到第 3 次起
   `pw_scope: out of slots (6 in use)`，曲线静默消失。现先删屏再重置。
3. **`restore_code.py` 后必须动一下源文件再编译（本次踩坑，已写进 `docs/07`）**：
   恢复快照会**保留快照里的 mtime**，若比构建目录里的 `.o` 旧，ninja 会**跳过重编** ——
   本次因此烧出一个"倾角页有指针盘、原始传感器页却还是旧版式"的混合固件，
   靠 `nm <obj> | grep pw_scope_set_lanes_i16` 才发现。修法：`touch apps/examples/phywear/*.c *.h` 再构建。

## 6. 已知限制（如实）

- 模拟器**无地磁驱动**（`/dev/mag0` 打开失败），地磁页数值恒为 `--`、曲线为空；
  该页已由**真机**补齐（−1308/+431/+234 mG，量程 `+/-2000 mG`）。
- 三轴页曲线**只在当前页采样**（省 I2C），切页清空重来；不保留跨页历史。
- 秒表环形进度的"参考值"是**上次间隔**（没有上次间隔时取 2.0 s 常数）—— 这是本批
  选定的语义，不是用户可配置的"目标时间"。
- 本批**未做**：3 个 FFT 频谱页、LVGL 矢量图形、逐页动效、圆角/阴影大铺开、任何渐变。
- 秒表页 fps 少 1 帧（见 §4），可选修法已列出但未实施。

---

## 7. 当天修订（2026-09-15，按用户反馈）

> 用户原话要点：①"体验不如原来的风格…因为当前曲线优化不行，变化不明显"；②"可以保留曲线，也就是通过触摸切换到有曲线模式"；
> ③主页"原始传感器"仍写 4 页实时数据、声学同理。

### 7.1 三轴页改成双视图

| 视图 | 内容 | 默认 |
|---|---|---|
| **数值** | 3 行「轴字母（28 px 暗色）+ 大数字（48 px 带单位）」，与「环境光」页同风格 | ✅ 每次进入 Raw Sensors 都从这一视图开始 |
| **图线** | 一行三列数值 + 358×186 三泳道曲线 + 每轴满量程 + `8 s` | 轻点页面任意处 / 右下角按钮进入 |

- 切换按钮用**自绘图标**：折线（点它去看图线）/ 三横条（点它回去看数值）。
  **不能用"曲线"二字**——本队 CJK 子集里没有「曲」(U+66F2)，写了会显示方框，而重生成子集有 ~0.5 天成本。
- 两种视图**复用同一批标签对象**（只改字体/位置/宽度/颜色），不新增控件、不吃 LVGL 堆。

### 7.2 "变化不明显"的根因与修法（有前后数字）

根因是**三轴共用一个满量程**，量程被最大的那根顶住：加速度页 Z≈1 g 让量程停在 2 g，
于是 Y 轴 0.12 g 的波动在 62 px 泳道里只有 **峰峰 ~3 px**，看着就是直线。

修法：`pw_scope_set_lanes_i16()` 改为收 **`inv[]` 数组（每泳道独立量程）**，并压低量程下限
（加速度 1→**0.2 g**、陀螺 20→**5 dps**、地磁 100→**20 mG**；下限太高会把小信号又压回直线）。

| 轴 | 修订前（共用 2 g） | 修订后（独立量程） |
|---|---|---|
| 加速度 X（0.6 g 摆幅） | ±8 px | ±16 px（量程 1 g） |
| **加速度 Y（0.12 g 摆幅）** | **±1.7 px（≈直线）** | **±16 px（量程 0.20 g）** |
| 加速度 Z（绕 1 g 起伏 0.3 g） | ±8 px | ±8 px（量程 2 g） |

底部的量程标注相应变成**每轴一个**：`+/-1 / 0.20 / 2 g`（按 X/Y/Z 顺序，与泳道颜色/轴字母可对照）。
修的时候顺带发现 `(int)fs` 会把 0.2 打成 0，已改用分数友好格式（`raw_fs_str()`）。

### 7.3 主页 tile 过期文案

| 板块 | 原 | 现 | 依据 |
|---|---|---|---|
| 原始传感器 | `4 页实时数据` / `4 live pages` | **`6 页实时数据`** | 实际 6 页：加速度/陀螺/地磁/环境光/麦克风/扬声器，全可用 |
| 声学 | `1 个可用 + 3 个规划` | **`2 个可用 + 2 个规划`** | 声音频谱 + 音频发生器已可用；音高/多普勒未做（09-13 加音频发生器时漏改） |

其余 6 个板块的文案核对无误。改后 **EN/ZH 各 238 条且 ID 集合完全一致**；
ZH 表 418 个非 ASCII 码点**字体缺字 0 个**（"6""2" 是 ASCII 数字），**因此没有触发字体重生成**。

### 7.4 无头验证交互（新增工具）

- `phywear cap rawcurve` —— 直接以图线视图打开三轴页（`pwshot.py shot rawcurve` 出真机截图）。
- `phywear tap <x> <y>` —— **模拟器专用**触摸注入：往 `/dev/utouch`（UInput, `CONFIG_UINPUT_TOUCH=y`）
  写 `touch_sample_s`，先 `TOUCH_DOWN`，隔 150 ms 再 `TOUCH_UP`；分两次是为了让 LVGL 的 indev
  轮询能分别看到按下与抬起（一次同时带 `DOWN|UP` 的样本不可靠）。
  → **本次"轻点切换视图"是注入真实事件验证过的**，不是只看两张静态截图。
- **真机点击仍需手指验证**：真机固件不开 `CONFIG_UINPUT_TOUCH`，`/dev/utouch` 不存在，无法注入。

### 7.5 修订后的真机实测

| 项 | 值 |
|---|---|
| 固件 | md5 `c6bf6e688b34c8da162788acf61d8a92`，**2,064,640 B**（flash 12.31%） |
| SRAM | **490,824 B / 512 KB（93.62%）**，较最初基线 489,440 B **+1,384 B** |
| 读回校验 | 6 段 16 KB 采样窗口**全部逐字节一致** |
| 帧率 | 数值视图 **fps 12 / loops 57**；图线视图 **fps 12 / loops 57**（加视图切换无额外帧率代价） |
| 对照 | 改动前原始页 loops 63 / fps 12 |

### 7.6 本轮截图

| 文件 | 内容 |
|---|---|
| `rev-real-home.png` | 真机主页：`6 页实时数据` / `2 个可用 + 2 个规划` |
| `rev-real-raw-num.png` / `rev-sim-raw-num.png` | 数值视图（默认，原版大数字风格）+ 右下角折线图标 |
| `rev-real-raw-curve.png` / `rev-sim-raw-curve.png` | 图线视图（每轴独立量程，量程标注 `+/-1 / 0.20 / 1 g`）+ 三横条图标 |
| `rev-sim-both-views.png` | 模拟器两种视图并排（同一台"表"） |
| `rev-sim-gyro-curve.png` | 陀螺页图线视图（量程 `+/-50 / 5 / 5 dps`，验证每轴量程跨页生效） |
| `rev-sim-mic-notoggle.png` | 麦克风页：**不显示**切换按钮（非三轴页没有第二视图） |

> ⚠️ 真机截图上曲线是平线，因为**板子静止放在桌上**（数值 -0.52/-0.15/-0.87 g 也印证它没在动）；
> 要看曲线动，请手拿手表晃动/倾斜，或看模拟器截图（模拟器有 0.8 Hz 合成摆动）。
